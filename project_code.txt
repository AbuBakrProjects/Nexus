NEXUS OS PROJECT FILES
================================================================================


================================================================================
FILE: app.py
================================================================================

from flask import Flask, render_template, request, jsonify
from datetime import datetime
from system.state import game_state
from challenges.challenge01 import run_challenge01

app = Flask(__name__)

filesystem = {
    "/": {
        "type": "directory",
        "children": ["home", "etc", "logs", "missions"]
    },

    "/home": {
        "type": "directory",
        "children": ["nexus"]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "notes.txt",
            "readme.txt"
        ]
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Remember to check the network logs."
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to Nexus OS."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": (
            "interface=eth0\n"
            "address=192.168.1.24\n"
            "gateway=192.168.1.1"
        )
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/access.log": {
        "type": "file",
        "content": (
            "[03:02] 192.168.1.12 LOGIN SUCCESS\n"
            "[03:04] 192.168.1.18 LOGIN SUCCESS\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:12] 192.168.1.44 LOGIN SUCCESS"
        )
    },

    "/logs/system.log": {
        "type": "file",
        "content": (
            "[03:02] system started\n"
            "[03:04] network interface eth0 online\n"
            "[03:17] security service started"
        )
    },

    "/missions": {
        "type": "directory",
        "children": ["mission01"]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": ["brief.txt"]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": (
            "MISSION 01\n\n"
            "Something is wrong with the network.\n"
            "Investigate the access logs."
        )
    }
}

def normalize_path(path):
    if not path:
        return "/"

    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    result = "/" + "/".join(parts)

    return result if result else "/"

def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )

def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help        Show available commands\n"
                "clear       Clear terminal\n"
                "pwd         Show current directory\n"
                "ls          List files\n"
                "cd          Change directory\n"
                "cat         Read a file\n"
                "whoami      Show current user\n"
                "hostname    Show system hostname\n"
                "echo        Print text\n"
                "date        Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"ls: cannot access '{target}': "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(
                item["children"]
            ),
            "cwd": current_dir
        }

    if cmd == "cd":
        target = "/"

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cd: no such file or directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": (
                    f"cd: not a directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(
            current_dir,
            args[0]
        )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "Is a directory"
                ),
                "cwd": current_dir
            }

        return {
            "output": item["content"],
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime(
                "%a %b %d %H:%M:%S %Y"
            ),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }

@app.route("/")
def index():
    return render_template("index.html")

@app.route("/panel")
def panel():
    return render_template("panel.html")

@app.route("/desktop")
def desktop():
    return render_template("desktop.html")

@app.route("/apps/terminal")
def terminal():
    return render_template("apps/terminal.html")

@app.route("/apps/messages")
def messages():
    return render_template("apps/messages.html")

@app.route("/nova")
def nova():
    return render_template("nova.html")

@app.route("/api/messages")
def get_messages():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "access_log_found": game_state["access_log_found"],
        "network_config_found": game_state["network_config_found"],
        "system_log_found": game_state["system_log_found"],
        "challenge01_complete": game_state["challenge01_complete"],
        "current_challenge": game_state["current_challenge"]
    })
@app.route("/api/state")
def get_state():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "system_log_found": game_state["system_log_found"],
        "access_log_found": game_state.get("access_log_found", False),
        "network_config_found": game_state.get("network_config_found", False),
        "challenge01_complete": game_state["challenge01_complete"]
    })

@app.route("/api/terminal", methods=["POST"])
def terminal_command():
    data = request.get_json()

    command = data.get(
        "command",
        ""
    ).strip()

    current_dir = data.get(
        "cwd",
        "/home/nexus"
    )

    result = execute_command(
        command,
        current_dir
    )

    challenge_result = run_challenge01(
        command,
        resolve_path(
            current_dir,
            command.split()[1]
        ) if (
            command.startswith("cat ")
            and len(command.split()) > 1
        ) else ""
    )

    new_message = False
    message_stage = 0

    notification = challenge_result.get(
            "notification"
        )

    if notification:
        new_message = True
        message_stage = notification.get(
            "stage",
            0
        )

    result["new_message"] = new_message
    result["message_stage"] = message_stage
    result["challenge_complete"] = (
        challenge_result["complete"]
    )

    return jsonify(result)

if __name__ == "__main__":
    app.run(debug=True)

================================================================================
FILE: backupp\export_project.py
================================================================================

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()

================================================================================
FILE: backupp\FIRST\css\desktop.css
================================================================================

#desktop {
    height: calc(100vh - 40px);

    background: #d7d7d7;

    position: relative;
}

.desktop-icons {
    display: flex;
    gap: 25px;
    flex-direction: column;
    padding: 20px;
}

.desktop-icon {
    width: 70px;
    display: flex;
    flex-direction: column;
    align-items: center;

    user-select: none;
}

.desktop-icon button {
    background: none;
    border: none;

    font-size: 35px;
    cursor: pointer;
}

.desktop-icon p {
    margin: 4px 0 0;

    font-size: 14px;
}

================================================================================
FILE: backupp\FIRST\css\panel.css
================================================================================

.panel {
    height: 40px;
    background: #333;
    color: white;

    display: flex;
    align-items: center;

    padding: 0 20px;
}

.menu-button {
    height: 30px;
    padding: 0 15px;

    background: #444;
    color: white;

    border: none;
}

.panel-apps {
    display: flex;
    margin-left: 10px;
}

.panel-apps button {
    height: 30px;

    padding: 0 12px;

    background: transparent;
    color: white;

    border: none;
}

.panel-apps button:hover {
    background: #555;
}

.system-tray {
    margin-left: auto;

    display: flex;
    gap: 15px;
    align-items: center;
}

================================================================================
FILE: backupp\FIRST\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
}

================================================================================
FILE: backupp\FIRST\html\apps\terminal.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-700 flex items-center justify-center h-[1000px] flex-col">
    <div class="w-[700px] h-[450px] bg-black border rounded-xl overflow-hidden">
        <div class="h-8 border-b flex items-center gap-2 bg-gray-400">

            <button class="w-3 h-3 rounded-full bg-[#ff5f57] flex items-center justify-center text-[9px]">×</button>
            <button class="h-3 w-3 rounded-full bg-[#febc2e] flex items-center justify-center text-[9px]">×</button>
            <button class="w-3 h-3 rounded-full bg-[#28c840] flex items-center justify-center text-[9px]">×</button>
            <h1 class="absolute left-1/2 -translate-x-1/2 flex items-center justify-center">Terminal</h1>
        </div>
        <div id="terminalOutput" class=" font-mono text-green-500 pl-3">
        </div>
        <div class="flex gap-2 ">
            <p class=" text-green-500 font-mono pl-3">user@nexus:~$</p>
            <input id="commandInput" class="bg-black text-green-500 caret-green-500 outline-none border-none" type="text">
        </div>
    </div>
</body>
    <script src="terminal.js"></script>

</html>


================================================================================
FILE: backupp\FIRST\html\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">
        <button>📁</button>
        <p>Files</p>
    </div>

    <div class="desktop-icon">
        <button>🌐</button>
        <p>Browser</p>
    </div>

</div>

================================================================================
FILE: backupp\FIRST\html\panel.html
================================================================================

<div class="panel">

    <button id="menuButton" class="menu-button">
        Menu
    </button>

    <div class="panel-apps">

        <button>
            Terminal
        </button>

        <button>
            Files
        </button>

        <button>
            Browser
        </button>

    </div>

    <div class="system-tray">

        <span>WiFi</span>
        <span>🔊</span>
        <span id="clock">00:00</span>

    </div>

</div>

================================================================================
FILE: backupp\FIRST\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Nexus OS</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/desktop.css">
    <link rel="stylesheet" href="css/panel.css">
</head>

<body>

    <div id="panel"></div>

    <main id="desktop"></main>

    <script src="js/desktop.js"></script>

</body>

</html>

================================================================================
FILE: backupp\FIRST\js\apps\terminal.js
================================================================================

const output = document.getElementById("terminalOutput");
const input = document.getElementById("commandInput");

input.addEventListener("keydown", async function (event) {

    if (event.key == "Enter") {

        const response = await fetch("/command", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                command: input.value
            })
        });
        if (output.innerHTML === "") {
            output.innerHTML = "user@nexus:~$ " + input.value;
        } else {
            output.innerHTML += "<br>user@nexus:~$ " + input.value;
        }

        const data = await response.json();
        if(data.clear) {
            output.innerHTML = ""
            input.value = "";
            return;
        }
        if (data.commands) {

            data.commands.forEach(command => {

                output.innerHTML += `
                    <br>
                    <span class="text-green-400">${command.usage}</span>
                    - ${command.description}
                `;

            });

            input.value = "";
            return;
        }
        console.log(data);
        output.innerHTML += "<br>" + data.output;
        input.value = "";

    }

});

================================================================================
FILE: backupp\FIRST\js\desktop.js
================================================================================

async function loadHTML(element, file) {

    const response = await fetch(file);

    const html = await response.text();

    document.getElementById(element).innerHTML = html;
}


async function startDesktop() {

    await loadHTML("panel", "html/panel.html");

    await loadHTML("desktop", "html/desktop.html");

}


function updateClock() {

    const clock = document.getElementById("clock");

    if (!clock) return;

    const now = new Date();

    const hours = now.getHours().toString().padStart(2, "0");
    const minutes = now.getMinutes().toString().padStart(2, "0");

    clock.textContent = `${hours}:${minutes}`;
}


startDesktop();

setInterval(updateClock, 1000);

================================================================================
FILE: backupp\FIRST\python\system\main.py
================================================================================

from flask import Flask, request, jsonify, send_from_directory
import os
from datetime import datetime

app = Flask(__name__)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

current_path = "/home/nexus"


@app.route("/")
def index():
    return send_from_directory(BASE_DIR, "index.html")


@app.route("/terminal.js")
def terminal_js():
    return send_from_directory(BASE_DIR, "terminal.js")


@app.route("/command", methods=["POST"])
def command():

    global current_path

    data = request.get_json()

    cmd = data.get("command", "")

    command = cmd.split()

    if not command:
        return jsonify({
            "output": ""
        })

    name = command[0]
    arguments = command[1:]

    # clear
    if name == "clear":

        if len(arguments) > 0:
            return jsonify({
                "output": "clear: too many arguments"
            })

        return jsonify({
            "output": "",
            "clear": True
        })

    # help
    elif name == "help":

        if len(arguments) > 0:
            return jsonify({
                "output": "help: too many arguments"
            })

        return jsonify({
            "commands": [
                {
                    "name": "clear",
                    "usage": "clear",
                    "description": "Clear the terminal screen"
                },
                {
                    "name": "help",
                    "usage": "help",
                    "description": "Show available commands"
                },
                {
                    "name": "pwd",
                    "usage": "pwd",
                    "description": "Print the current working directory"
                },
                {
                    "name": "cd",
                    "usage": "cd <directory>",
                    "description": "Change the current directory"
                },
                {
                    "name": "date",
                    "usage": "date",
                    "description": "Display the current date and time"
                },
                {
                    "name": "whoami",
                    "usage": "whoami",
                    "description": "Display the current user"
                }
            ]
        })

    # pwd
    elif name == "pwd":

        if len(arguments) > 0:
            return jsonify({
                "output": "pwd: too many arguments"
            })

        return jsonify({
            "output": current_path
        })

    # date
    elif name == "date":

        if len(arguments) > 0:
            return jsonify({
                "output": "date: too many arguments"
            })

        return jsonify({
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y")
        })

    # cd
    elif name == "cd":

        if len(arguments) == 0:
            return jsonify({
                "output": "cd: missing argument"
            })

        if len(arguments) > 1:
            return jsonify({
                "output": "cd: too many arguments"
            })

        path = arguments[0]

        if path.startswith("/"):
            current_path = path
        else:
            current_path = current_path.rstrip("/") + "/" + path

        return jsonify({
            "output": current_path
        })

    # whoami
    elif name == "whoami":

        if len(arguments) > 0:
            return jsonify({
                "output": "whoami: too many arguments"
            })

        return jsonify({
            "output": "nexus"
        })

    # unknown command
    else:

        return jsonify({
            "output": f"{name}: command not found"
        })


if __name__ == "__main__":
    app.run(
        host="127.0.0.1",
        port=2026,
        debug=False
    )

================================================================================
FILE: backupp\FIRST\static\css\input.css
================================================================================

@import "tailwindcss";

================================================================================
FILE: backupp\story board wokring\app.py
================================================================================

from flask import Flask, render_template, request, jsonify
from datetime import datetime

app = Flask(__name__)

game_state = {
    "message_unlocked": False,
    "system_log_found": False,
    "challenge01_complete": False
}

filesystem = {
    "/": {
        "type": "directory",
        "children": ["home", "etc", "logs", "missions"]
    },

    "/home": {
        "type": "directory",
        "children": ["nexus"]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "notes.txt",
            "readme.txt"
        ]
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Remember to check the network logs."
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to Nexus OS."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": (
            "interface=eth0\n"
            "address=192.168.1.24\n"
            "gateway=192.168.1.1"
        )
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/access.log": {
        "type": "file",
        "content": (
            "[03:02] 192.168.1.12 LOGIN SUCCESS\n"
            "[03:04] 192.168.1.18 LOGIN SUCCESS\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:12] 192.168.1.44 LOGIN SUCCESS"
        )
    },

    "/logs/system.log": {
        "type": "file",
        "content": (
            "[03:02] system started\n"
            "[03:04] network interface eth0 online\n"
            "[03:17] security service started"
        )
    },

    "/missions": {
        "type": "directory",
        "children": ["mission01"]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": ["brief.txt"]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": (
            "MISSION 01\n\n"
            "Something is wrong with the network.\n"
            "Investigate the access logs."
        )
    }
}


def normalize_path(path):
    if not path:
        return "/"

    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    result = "/" + "/".join(parts)

    return result if result else "/"


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help        Show available commands\n"
                "clear       Clear terminal\n"
                "pwd         Show current directory\n"
                "ls          List files\n"
                "cd          Change directory\n"
                "cat         Read a file\n"
                "whoami      Show current user\n"
                "hostname    Show system hostname\n"
                "echo        Print text\n"
                "date        Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"ls: cannot access '{target}': "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        target = "/"

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cd: no such file or directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": (
                    f"cd: not a directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(
            current_dir,
            args[0]
        )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "Is a directory"
                ),
                "cwd": current_dir
            }

        return {
            "output": item["content"],
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime(
                "%a %b %d %H:%M:%S %Y"
            ),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


@app.route("/")
def index():
    return render_template("index.html")


@app.route("/panel")
def panel():
    return render_template("panel.html")


@app.route("/desktop")
def desktop():
    return render_template("desktop.html")


@app.route("/apps/terminal")
def terminal():
    return render_template("apps/terminal.html")


@app.route("/apps/messages")
def messages():
    return render_template("apps/messages.html")


@app.route("/api/messages")
def get_messages():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "system_log_found": game_state["system_log_found"],
        "challenge01_complete": game_state["challenge01_complete"]
    })


@app.route("/api/terminal", methods=["POST"])
def terminal_command():
    data = request.get_json()

    command = data.get(
        "command",
        ""
    ).strip()

    current_dir = data.get(
        "cwd",
        "/home/nexus"
    )

    result = execute_command(
        command,
        current_dir
    )

    new_message = False
    message_stage = 0

    if command == "cat /logs/access.log":

        if not game_state["message_unlocked"]:
            game_state["message_unlocked"] = True
            new_message = True
            message_stage = 1

    elif command == "cat /logs/system.log":

        if not game_state["system_log_found"]:
            game_state["system_log_found"] = True
            new_message = True
            message_stage = 2

    result["new_message"] = new_message
    result["message_stage"] = message_stage
    result["challenge_complete"] = False

    return jsonify(result)


if __name__ == "__main__":
    app.run(debug=True)



================================================================================
FILE: backupp\story board wokring\challenges\__init__.py
================================================================================




================================================================================
FILE: backupp\story board wokring\challenges\challenge01.py
================================================================================

from system.state import game_state


TITLE = "NEXUS NODE 01"


INTRO = """UNKNOWN

NEXUS NODE 01.

Remember that name.

This machine wasn't supposed to be online.

Someone brought it back.

And someone connected to it.

Find out why."""


OBJECTIVE = """CHALLENGE 01

Something is wrong with the network.

Investigate the access logs.

Find the suspicious login activity."""


SUCCESS = """CHALLENGE 01 COMPLETE.

Someone was here.

And they didn't want to be noticed."""


MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:17",
    "text": (
        "You're looking in the right place.\n\n"
        "But you're not the first person to access this machine.\n\n"
        "Keep digging."
    )
}


def run_challenge01(command, target):
    if game_state["challenge01_complete"]:
        return {
            "complete": True,
            "notification": None
        }

    if (
        command == "cat /logs/access.log"
        and target == "/logs/access.log"
    ):
        game_state["challenge01_complete"] = True
        game_state["unread_messages"] += 1
        game_state["messages"].append(MESSAGE)

        return {
            "complete": True,
            "notification": {
                "title": "NEW MESSAGE",
                "text": "A new message has been received.",
                "sender": "UNKNOWN"
            }
        }

    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\challenges\challenge02.py
================================================================================

TITLE = "THE SECOND TRACE"


INTRO = """UNKNOWN

You found the first trace.

It leads somewhere deeper.

The node is hiding more than old login attempts."""


OBJECTIVE = """CHALLENGE 02

Find the next piece of evidence."""


SUCCESS = """CHALLENGE 02 COMPLETE.

The trail continues."""


def run_challenge02(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\challenges\challenge03.py
================================================================================

TITLE = "THE WATCHER"


INTRO = """UNKNOWN

The system knows you're here now.

Be careful."""


OBJECTIVE = """CHALLENGE 03

Investigate the next clue."""


SUCCESS = """CHALLENGE 03 COMPLETE."""


def run_challenge03(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\export_project.py
================================================================================


from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()



================================================================================
FILE: backupp\story board wokring\extract_projects.py
================================================================================

from pathlib import Path
import re

SOURCE_FILE = Path("project_code.txt")
OUTPUT_FOLDER = Path("extracted_project")


def extract_files():
    if not SOURCE_FILE.exists():
        print("ERROR: project_code.txt was not found.")
        return

    text = SOURCE_FILE.read_text(
        encoding="utf-8"
    )

    pattern = re.compile(
        r"^FILE:\s*(.+?)\s*$"
        r"\n^=+\s*$"
        r"\n(.*?)"
        r"(?=^=+\s*$\n^FILE:|\Z)",
        re.MULTILINE | re.DOTALL
    )

    matches = list(
        pattern.finditer(text)
    )

    if not matches:
        print("ERROR: No project files found.")
        return

    created = 0

    for match in matches:
        original_path = match.group(1).strip()
        content = match.group(2)

        original_path = original_path.replace(
            "\\",
            "/"
        )

        if original_path.startswith(
            "backupp/V 0.1/"
        ):
            relative_path = original_path[
                len("backupp/V 0.1/"):
            ]

        elif original_path.startswith(
            "V 0.1/"
        ):
            relative_path = original_path[
                len("V 0.1/"):
            ]

        elif original_path.startswith(
            "backupp/"
        ):
            relative_path = original_path[
                len("backupp/"):
            ]

        else:
            relative_path = original_path

        relative_path = relative_path.strip("/")

        if not relative_path:
            continue

        destination = (
            OUTPUT_FOLDER /
            relative_path
        )

        destination.parent.mkdir(
            parents=True,
            exist_ok=True
        )

        content = content.rstrip()

        destination.write_text(
            content + "\n",
            encoding="utf-8"
        )

        print(
            f"Created: {destination}"
        )

        created += 1

    print()
    print(
        f"Done. Created {created} files."
    )

    print(
        f"Project folder: "
        f"{OUTPUT_FOLDER.resolve()}"
    )


if __name__ == "__main__":
    extract_files()

================================================================================
FILE: backupp\story board wokring\extracted_project\app.py
================================================================================

from flask import Flask, render_template, request, jsonify
from datetime import datetime

app = Flask(__name__)

game_state = {
    "message_unlocked": False,
    "system_log_found": False,
    "challenge01_complete": False
}

filesystem = {
    "/": {
        "type": "directory",
        "children": ["home", "etc", "logs", "missions"]
    },

    "/home": {
        "type": "directory",
        "children": ["nexus"]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "notes.txt",
            "readme.txt"
        ]
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Remember to check the network logs."
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to Nexus OS."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": (
            "interface=eth0\n"
            "address=192.168.1.24\n"
            "gateway=192.168.1.1"
        )
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/access.log": {
        "type": "file",
        "content": (
            "[03:02] 192.168.1.12 LOGIN SUCCESS\n"
            "[03:04] 192.168.1.18 LOGIN SUCCESS\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:12] 192.168.1.44 LOGIN SUCCESS"
        )
    },

    "/logs/system.log": {
        "type": "file",
        "content": (
            "[03:02] system started\n"
            "[03:04] network interface eth0 online\n"
            "[03:17] security service started"
        )
    },

    "/missions": {
        "type": "directory",
        "children": ["mission01"]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": ["brief.txt"]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": (
            "MISSION 01\n\n"
            "Something is wrong with the network.\n"
            "Investigate the access logs."
        )
    }
}


def normalize_path(path):
    if not path:
        return "/"

    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    result = "/" + "/".join(parts)

    return result if result else "/"


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help        Show available commands\n"
                "clear       Clear terminal\n"
                "pwd         Show current directory\n"
                "ls          List files\n"
                "cd          Change directory\n"
                "cat         Read a file\n"
                "whoami      Show current user\n"
                "hostname    Show system hostname\n"
                "echo        Print text\n"
                "date        Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"ls: cannot access '{target}': "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        target = "/"

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cd: no such file or directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": (
                    f"cd: not a directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(
            current_dir,
            args[0]
        )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "Is a directory"
                ),
                "cwd": current_dir
            }

        return {
            "output": item["content"],
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime(
                "%a %b %d %H:%M:%S %Y"
            ),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


@app.route("/")
def index():
    return render_template("index.html")


@app.route("/panel")
def panel():
    return render_template("panel.html")


@app.route("/desktop")
def desktop():
    return render_template("desktop.html")


@app.route("/apps/terminal")
def terminal():
    return render_template("apps/terminal.html")


@app.route("/apps/messages")
def messages():
    return render_template("apps/messages.html")


@app.route("/api/messages")
def get_messages():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "system_log_found": game_state["system_log_found"],
        "challenge01_complete": game_state["challenge01_complete"]
    })


@app.route("/api/terminal", methods=["POST"])
def terminal_command():
    data = request.get_json()

    command = data.get(
        "command",
        ""
    ).strip()

    current_dir = data.get(
        "cwd",
        "/home/nexus"
    )

    result = execute_command(
        command,
        current_dir
    )

    new_message = False
    message_stage = 0

    if command == "cat /logs/access.log":

        if not game_state["message_unlocked"]:
            game_state["message_unlocked"] = True
            new_message = True
            message_stage = 1

    elif command == "cat /logs/system.log":

        if not game_state["system_log_found"]:
            game_state["system_log_found"] = True
            new_message = True
            message_stage = 2

    result["new_message"] = new_message
    result["message_stage"] = message_stage
    result["challenge_complete"] = False

    return jsonify(result)


if __name__ == "__main__":
    app.run(debug=True)


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\export_project.py
================================================================================

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\css\desktop.css
================================================================================

#desktop {
    height: calc(100vh - 40px);

    background: #d7d7d7;

    position: relative;
}

.desktop-icons {
    display: flex;
    gap: 25px;
    flex-direction: column;
    padding: 20px;
}

.desktop-icon {
    width: 70px;
    display: flex;
    flex-direction: column;
    align-items: center;

    user-select: none;
}

.desktop-icon button {
    background: none;
    border: none;

    font-size: 35px;
    cursor: pointer;
}

.desktop-icon p {
    margin: 4px 0 0;

    font-size: 14px;
}

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\css\panel.css
================================================================================

.panel {
    height: 40px;
    background: #333;
    color: white;

    display: flex;
    align-items: center;

    padding: 0 20px;
}

.menu-button {
    height: 30px;
    padding: 0 15px;

    background: #444;
    color: white;

    border: none;
}

.panel-apps {
    display: flex;
    margin-left: 10px;
}

.panel-apps button {
    height: 30px;

    padding: 0 12px;

    background: transparent;
    color: white;

    border: none;
}

.panel-apps button:hover {
    background: #555;
}

.system-tray {
    margin-left: auto;

    display: flex;
    gap: 15px;
    align-items: center;
}

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
}

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\html\apps\terminal.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-700 flex items-center justify-center h-[1000px] flex-col">
    <div class="w-[700px] h-[450px] bg-black border rounded-xl overflow-hidden">
        <div class="h-8 border-b flex items-center gap-2 bg-gray-400">

            <button class="w-3 h-3 rounded-full bg-[#ff5f57] flex items-center justify-center text-[9px]">×</button>
            <button class="h-3 w-3 rounded-full bg-[#febc2e] flex items-center justify-center text-[9px]">×</button>
            <button class="w-3 h-3 rounded-full bg-[#28c840] flex items-center justify-center text-[9px]">×</button>
            <h1 class="absolute left-1/2 -translate-x-1/2 flex items-center justify-center">Terminal</h1>
        </div>
        <div id="terminalOutput" class=" font-mono text-green-500 pl-3">
        </div>
        <div class="flex gap-2 ">
            <p class=" text-green-500 font-mono pl-3">user@nexus:~$</p>
            <input id="commandInput" class="bg-black text-green-500 caret-green-500 outline-none border-none" type="text">
        </div>
    </div>
</body>
    <script src="terminal.js"></script>

</html>


================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\html\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">
        <button>📁</button>
        <p>Files</p>
    </div>

    <div class="desktop-icon">
        <button>🌐</button>
        <p>Browser</p>
    </div>

</div>

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\html\panel.html
================================================================================

<div class="panel">

    <button id="menuButton" class="menu-button">
        Menu
    </button>

    <div class="panel-apps">

        <button>
            Terminal
        </button>

        <button>
            Files
        </button>

        <button>
            Browser
        </button>

    </div>

    <div class="system-tray">

        <span>WiFi</span>
        <span>🔊</span>
        <span id="clock">00:00</span>

    </div>

</div>

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Nexus OS</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/desktop.css">
    <link rel="stylesheet" href="css/panel.css">
</head>

<body>

    <div id="panel"></div>

    <main id="desktop"></main>

    <script src="js/desktop.js"></script>

</body>

</html>

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\js\apps\terminal.js
================================================================================

const output = document.getElementById("terminalOutput");
const input = document.getElementById("commandInput");

input.addEventListener("keydown", async function (event) {

    if (event.key == "Enter") {

        const response = await fetch("/command", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                command: input.value
            })
        });
        if (output.innerHTML === "") {
            output.innerHTML = "user@nexus:~$ " + input.value;
        } else {
            output.innerHTML += "<br>user@nexus:~$ " + input.value;
        }

        const data = await response.json();
        if(data.clear) {
            output.innerHTML = ""
            input.value = "";
            return;
        }
        if (data.commands) {

            data.commands.forEach(command => {

                output.innerHTML += `
                    <br>
                    <span class="text-green-400">${command.usage}</span>
                    - ${command.description}
                `;

            });

            input.value = "";
            return;
        }
        console.log(data);
        output.innerHTML += "<br>" + data.output;
        input.value = "";

    }

});

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\js\desktop.js
================================================================================

async function loadHTML(element, file) {

    const response = await fetch(file);

    const html = await response.text();

    document.getElementById(element).innerHTML = html;
}


async function startDesktop() {

    await loadHTML("panel", "html/panel.html");

    await loadHTML("desktop", "html/desktop.html");

}


function updateClock() {

    const clock = document.getElementById("clock");

    if (!clock) return;

    const now = new Date();

    const hours = now.getHours().toString().padStart(2, "0");
    const minutes = now.getMinutes().toString().padStart(2, "0");

    clock.textContent = `${hours}:${minutes}`;
}


startDesktop();

setInterval(updateClock, 1000);

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\python\system\main.py
================================================================================

from flask import Flask, request, jsonify, send_from_directory
import os
from datetime import datetime

app = Flask(__name__)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

current_path = "/home/nexus"


@app.route("/")
def index():
    return send_from_directory(BASE_DIR, "index.html")


@app.route("/terminal.js")
def terminal_js():
    return send_from_directory(BASE_DIR, "terminal.js")


@app.route("/command", methods=["POST"])
def command():

    global current_path

    data = request.get_json()

    cmd = data.get("command", "")

    command = cmd.split()

    if not command:
        return jsonify({
            "output": ""
        })

    name = command[0]
    arguments = command[1:]

    # clear
    if name == "clear":

        if len(arguments) > 0:
            return jsonify({
                "output": "clear: too many arguments"
            })

        return jsonify({
            "output": "",
            "clear": True
        })

    # help
    elif name == "help":

        if len(arguments) > 0:
            return jsonify({
                "output": "help: too many arguments"
            })

        return jsonify({
            "commands": [
                {
                    "name": "clear",
                    "usage": "clear",
                    "description": "Clear the terminal screen"
                },
                {
                    "name": "help",
                    "usage": "help",
                    "description": "Show available commands"
                },
                {
                    "name": "pwd",
                    "usage": "pwd",
                    "description": "Print the current working directory"
                },
                {
                    "name": "cd",
                    "usage": "cd <directory>",
                    "description": "Change the current directory"
                },
                {
                    "name": "date",
                    "usage": "date",
                    "description": "Display the current date and time"
                },
                {
                    "name": "whoami",
                    "usage": "whoami",
                    "description": "Display the current user"
                }
            ]
        })

    # pwd
    elif name == "pwd":

        if len(arguments) > 0:
            return jsonify({
                "output": "pwd: too many arguments"
            })

        return jsonify({
            "output": current_path
        })

    # date
    elif name == "date":

        if len(arguments) > 0:
            return jsonify({
                "output": "date: too many arguments"
            })

        return jsonify({
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y")
        })

    # cd
    elif name == "cd":

        if len(arguments) == 0:
            return jsonify({
                "output": "cd: missing argument"
            })

        if len(arguments) > 1:
            return jsonify({
                "output": "cd: too many arguments"
            })

        path = arguments[0]

        if path.startswith("/"):
            current_path = path
        else:
            current_path = current_path.rstrip("/") + "/" + path

        return jsonify({
            "output": current_path
        })

    # whoami
    elif name == "whoami":

        if len(arguments) > 0:
            return jsonify({
                "output": "whoami: too many arguments"
            })

        return jsonify({
            "output": "nexus"
        })

    # unknown command
    else:

        return jsonify({
            "output": f"{name}: command not found"
        })


if __name__ == "__main__":
    app.run(
        host="127.0.0.1",
        port=2026,
        debug=False
    )

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\backupp\FIRST\static\css\input.css
================================================================================

@import "tailwindcss";

================================================================================


================================================================================
FILE: backupp\story board wokring\extracted_project\challenges\__init__.py
================================================================================




================================================================================
FILE: backupp\story board wokring\extracted_project\challenges\challenge01.py
================================================================================

from system.state import game_state


TITLE = "NEXUS NODE 01"


INTRO = """UNKNOWN

NEXUS NODE 01.

Remember that name.

This machine wasn't supposed to be online.

Someone brought it back.

And someone connected to it.

Find out why."""


OBJECTIVE = """CHALLENGE 01

Something is wrong with the network.

Investigate the access logs.

Find the suspicious login activity."""


SUCCESS = """CHALLENGE 01 COMPLETE.

Someone was here.

And they didn't want to be noticed."""


MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:17",
    "text": (
        "You're looking in the right place.\n\n"
        "But you're not the first person to access this machine.\n\n"
        "Keep digging."
    )
}


def run_challenge01(command, target):
    if game_state["challenge01_complete"]:
        return {
            "complete": True,
            "notification": None
        }

    if (
        command == "cat /logs/access.log"
        and target == "/logs/access.log"
    ):
        game_state["challenge01_complete"] = True
        game_state["unread_messages"] += 1
        game_state["messages"].append(MESSAGE)

        return {
            "complete": True,
            "notification": {
                "title": "NEW MESSAGE",
                "text": "A new message has been received.",
                "sender": "UNKNOWN"
            }
        }

    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\extracted_project\challenges\challenge02.py
================================================================================

TITLE = "THE SECOND TRACE"


INTRO = """UNKNOWN

You found the first trace.

It leads somewhere deeper.

The node is hiding more than old login attempts."""


OBJECTIVE = """CHALLENGE 02

Find the next piece of evidence."""


SUCCESS = """CHALLENGE 02 COMPLETE.

The trail continues."""


def run_challenge02(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\extracted_project\challenges\challenge03.py
================================================================================

TITLE = "THE WATCHER"


INTRO = """UNKNOWN

The system knows you're here now.

Be careful."""


OBJECTIVE = """CHALLENGE 03

Investigate the next clue."""


SUCCESS = """CHALLENGE 03 COMPLETE."""


def run_challenge03(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: backupp\story board wokring\extracted_project\export_project.py
================================================================================

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\css\desktop.css
================================================================================

#desktop {
    height: calc(100vh - 40px);

    background: #d7d7d7;

    position: relative;
}

.desktop-icons {
    display: flex;
    gap: 25px;
    flex-direction: column;
    padding: 20px;
}

.desktop-icon {
    width: 70px;
    display: flex;
    flex-direction: column;
    align-items: center;

    user-select: none;
}

.desktop-icon button {
    background: none;
    border: none;

    font-size: 35px;
    cursor: pointer;
}

.desktop-icon p {
    margin: 4px 0 0;

    font-size: 14px;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\css\panel.css
================================================================================

.panel {
    height: 40px;
    background: #333;
    color: white;

    display: flex;
    align-items: center;

    padding: 0 20px;
}

.menu-button {
    height: 30px;
    padding: 0 15px;

    background: #444;
    color: white;

    border: none;
}

.panel-apps {
    display: flex;
    margin-left: 10px;
}

.panel-apps button {
    height: 30px;

    padding: 0 12px;

    background: transparent;
    color: white;

    border: none;
}

.panel-apps button:hover {
    background: #555;
}

.system-tray {
    margin-left: auto;

    display: flex;
    gap: 15px;
    align-items: center;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\html\apps\terminal.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-700 flex items-center justify-center h-[1000px] flex-col">
    <div class="w-[700px] h-[450px] bg-black border rounded-xl overflow-hidden">
        <div class="h-8 border-b flex items-center gap-2 bg-gray-400">

            <button class="w-3 h-3 rounded-full bg-[#ff5f57] flex items-center justify-center text-[9px]">×</button>
            <button class="h-3 w-3 rounded-full bg-[#febc2e] flex items-center justify-center text-[9px]">×</button>
            <button class="w-3 h-3 rounded-full bg-[#28c840] flex items-center justify-center text-[9px]">×</button>
            <h1 class="absolute left-1/2 -translate-x-1/2 flex items-center justify-center">Terminal</h1>
        </div>
        <div id="terminalOutput" class=" font-mono text-green-500 pl-3">
        </div>
        <div class="flex gap-2 ">
            <p class=" text-green-500 font-mono pl-3">user@nexus:~$</p>
            <input id="commandInput" class="bg-black text-green-500 caret-green-500 outline-none border-none" type="text">
        </div>
    </div>
</body>
    <script src="terminal.js"></script>

</html>


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\html\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">
        <button>📁</button>
        <p>Files</p>
    </div>

    <div class="desktop-icon">
        <button>🌐</button>
        <p>Browser</p>
    </div>

</div>


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\html\panel.html
================================================================================

<div class="panel">

    <button id="menuButton" class="menu-button">
        Menu
    </button>

    <div class="panel-apps">

        <button>
            Terminal
        </button>

        <button>
            Files
        </button>

        <button>
            Browser
        </button>

    </div>

    <div class="system-tray">

        <span>WiFi</span>
        <span>🔊</span>
        <span id="clock">00:00</span>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Nexus OS</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/desktop.css">
    <link rel="stylesheet" href="css/panel.css">
</head>

<body>

    <div id="panel"></div>

    <main id="desktop"></main>

    <script src="js/desktop.js"></script>

</body>

</html>


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\js\apps\terminal.js
================================================================================

const output = document.getElementById("terminalOutput");
const input = document.getElementById("commandInput");

input.addEventListener("keydown", async function (event) {

    if (event.key == "Enter") {

        const response = await fetch("/command", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                command: input.value
            })
        });
        if (output.innerHTML === "") {
            output.innerHTML = "user@nexus:~$ " + input.value;
        } else {
            output.innerHTML += "<br>user@nexus:~$ " + input.value;
        }

        const data = await response.json();
        if(data.clear) {
            output.innerHTML = ""
            input.value = "";
            return;
        }
        if (data.commands) {

            data.commands.forEach(command => {

                output.innerHTML += `
                    <br>
                    <span class="text-green-400">${command.usage}</span>
                    - ${command.description}
                `;

            });

            input.value = "";
            return;
        }
        console.log(data);
        output.innerHTML += "<br>" + data.output;
        input.value = "";

    }

});


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\js\desktop.js
================================================================================

async function loadHTML(element, file) {

    const response = await fetch(file);

    const html = await response.text();

    document.getElementById(element).innerHTML = html;
}


async function startDesktop() {

    await loadHTML("panel", "html/panel.html");

    await loadHTML("desktop", "html/desktop.html");

}


function updateClock() {

    const clock = document.getElementById("clock");

    if (!clock) return;

    const now = new Date();

    const hours = now.getHours().toString().padStart(2, "0");
    const minutes = now.getMinutes().toString().padStart(2, "0");

    clock.textContent = `${hours}:${minutes}`;
}


startDesktop();

setInterval(updateClock, 1000);


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\python\system\main.py
================================================================================

from flask import Flask, request, jsonify, send_from_directory
import os
from datetime import datetime

app = Flask(__name__)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

current_path = "/home/nexus"


@app.route("/")
def index():
    return send_from_directory(BASE_DIR, "index.html")


@app.route("/terminal.js")
def terminal_js():
    return send_from_directory(BASE_DIR, "terminal.js")


@app.route("/command", methods=["POST"])
def command():

    global current_path

    data = request.get_json()

    cmd = data.get("command", "")

    command = cmd.split()

    if not command:
        return jsonify({
            "output": ""
        })

    name = command[0]
    arguments = command[1:]

    # clear
    if name == "clear":

        if len(arguments) > 0:
            return jsonify({
                "output": "clear: too many arguments"
            })

        return jsonify({
            "output": "",
            "clear": True
        })

    # help
    elif name == "help":

        if len(arguments) > 0:
            return jsonify({
                "output": "help: too many arguments"
            })

        return jsonify({
            "commands": [
                {
                    "name": "clear",
                    "usage": "clear",
                    "description": "Clear the terminal screen"
                },
                {
                    "name": "help",
                    "usage": "help",
                    "description": "Show available commands"
                },
                {
                    "name": "pwd",
                    "usage": "pwd",
                    "description": "Print the current working directory"
                },
                {
                    "name": "cd",
                    "usage": "cd <directory>",
                    "description": "Change the current directory"
                },
                {
                    "name": "date",
                    "usage": "date",
                    "description": "Display the current date and time"
                },
                {
                    "name": "whoami",
                    "usage": "whoami",
                    "description": "Display the current user"
                }
            ]
        })

    # pwd
    elif name == "pwd":

        if len(arguments) > 0:
            return jsonify({
                "output": "pwd: too many arguments"
            })

        return jsonify({
            "output": current_path
        })

    # date
    elif name == "date":

        if len(arguments) > 0:
            return jsonify({
                "output": "date: too many arguments"
            })

        return jsonify({
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y")
        })

    # cd
    elif name == "cd":

        if len(arguments) == 0:
            return jsonify({
                "output": "cd: missing argument"
            })

        if len(arguments) > 1:
            return jsonify({
                "output": "cd: too many arguments"
            })

        path = arguments[0]

        if path.startswith("/"):
            current_path = path
        else:
            current_path = current_path.rstrip("/") + "/" + path

        return jsonify({
            "output": current_path
        })

    # whoami
    elif name == "whoami":

        if len(arguments) > 0:
            return jsonify({
                "output": "whoami: too many arguments"
            })

        return jsonify({
            "output": "nexus"
        })

    # unknown command
    else:

        return jsonify({
            "output": f"{name}: command not found"
        })


if __name__ == "__main__":
    app.run(
        host="127.0.0.1",
        port=2026,
        debug=False
    )


================================================================================
FILE: backupp\story board wokring\extracted_project\FIRST\static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: backupp\story board wokring\extracted_project\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\apps\messages.css
================================================================================

.messages-window {
    position: fixed;
    top: 100px;
    left: 50%;
    z-index: 500;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #101410;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.messages-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
}

.messages-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.messages-close {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
    background: #ff5f57;
}

.messages-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100% - 42px);
}

.message-list {
    overflow-y: auto;
    background: #0c100c;
    border-right: 2px solid #303830;
}

.message-item {
    position: relative;
    padding: 18px;
    border-bottom: 1px solid #292e29;
}

.message-item.active {
    background: #171d17;
}

.message-sender {
    margin-bottom: 5px;
    color: #b8ffb8;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.message-preview {
    max-width: 210px;
    overflow: hidden;
    color: #888;
    font-size: 19px;
    white-space: nowrap;
    text-overflow: ellipsis;
}

.message-unread {
    position: absolute;
    top: 17px;
    right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #b6ff72;
    color: #111;
    font-family: "Press Start 2P", monospace;
    font-size: 7px;
}

.message-view {
    min-width: 0;
    padding: 28px;
    overflow-y: auto;
}

.message-header {
    margin-bottom: 5px;
    color: #baffba;
    font-family: "Press Start 2P", monospace;
    font-size: 11px;
}

.message-time {
    margin-bottom: 25px;
    color: #666;
    font-size: 17px;
}

.message-body {
    max-width: 650px;
    color: #d1d1d1;
    font-size: 23px;
    line-height: 1.1;
    white-space: pre-wrap;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\apps\terminal.css
================================================================================

.terminal-window {
    position: fixed;
    top: 90px;
    left: 50%;
    z-index: 500;
    display: flex;
    flex-direction: column;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #050805;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.terminal-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    min-height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
    cursor: grab;
}

.terminal-titlebar:active {
    cursor: grabbing;
}

.terminal-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.terminal-buttons {
    display: flex;
    align-items: center;
    gap: 8px;
}

.terminal-buttons button {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
}

.close-button {
    background: #ff5f57;
}

.maximize-button {
    background: #28c840;
}

.terminal-body {
    flex: 1;
    min-height: 0;
    padding: 18px 20px;
    overflow-y: auto;
    background: #050805;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
    line-height: 1.05;
    cursor: text;
}

.terminal-body::-webkit-scrollbar {
    width: 10px;
}

.terminal-body::-webkit-scrollbar-track {
    background: #0c100c;
}

.terminal-body::-webkit-scrollbar-thumb {
    background: #384438;
}

.terminal-body::-webkit-scrollbar-thumb:hover {
    background: #526052;
}

.terminal-line {
    min-height: 24px;
    white-space: pre-wrap;
    word-break: break-word;
}

.terminal-system {
    color: #71c871;
}

.terminal-prompt {
    color: #aaffaa;
}

.terminal-input-line {
    display: flex;
    align-items: baseline;
    width: 100%;
    min-height: 27px;
}

#commandInput {
    flex: 1;
    min-width: 0;
    margin-left: 8px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #9cff9c;
    caret-color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
}

#commandInput::selection {
    background: #496949;
    color: white;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\desktop.css
================================================================================

#desktop {
    background:
        linear-gradient(
            rgba(4, 10, 5, 0.45),
            rgba(4, 10, 5, 0.45)
        ),
        url("/static/assets/wallpapers/nexus-bg.png");

    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
}

.desktop-icons {
    display: grid;
    grid-template-columns: repeat(4, 120px);
    gap: 24px;
    padding: 38px;
}

.desktop-icon {
    position: relative;
    width: 120px;
    text-align: center;
}

.desktop-icon button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 76px;
    height: 76px;
    margin: 0 auto 8px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #baffba;
    font-size: 42px;
}

.desktop-icon button:hover {
    border-color: #405240;
    background: rgba(120, 255, 120, 0.06);
}

.desktop-icon p {
    margin: 0;
    color: #c7c7c7;
    font-size: 19px;
    letter-spacing: 1px;
}

.notification-badge {
    position: absolute;
    top: -4px;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 21px;
    height: 21px;
    border-radius: 50%;
    background: #b6ff72;
    color: #101010;
    font-family: "Press Start 2P", monospace;
    font-size: 8px;
}

.hidden {
    display: none;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\panel.css
================================================================================

.panel {
    position: relative;
    z-index: 1000;
    display: flex;
    align-items: center;
    width: 100%;
    height: 48px;
    padding: 0 18px;
    background: #111;
    border-bottom: 2px solid #333;
    color: #d7ffd7;
}

.menu-button {
    padding: 5px 12px;
    border: 1px solid #555;
    background: #181818;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.menu-button:hover {
    background: #222;
    border-color: #8cff8c;
}

.panel-apps {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: 20px;
}

.panel-apps button {
    padding: 5px 9px;
    border: 1px solid transparent;
    background: transparent;
    color: #aaa;
    font-family: "VT323", monospace;
    font-size: 19px;
}

.panel-apps button:hover {
    color: #9cff9c;
    border-color: #444;
    background: #181818;
}

.system-tray {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-left: auto;
    color: #888;
    font-size: 19px;
}

.network-status {
    color: #75d675;
}


================================================================================
FILE: backupp\story board wokring\extracted_project\static\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

html,
body {
    width: 100%;
    height: 100%;
    margin: 0;
}

body {
    background: #080b08;
    color: #d7ffd7;
    font-family: "VT323", monospace;
    overflow: hidden;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}

#bootScreen {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    background: #020502;
    color: #8cff8c;
    font-size: 24px;
    line-height: 1.25;
    text-shadow: 0 0 8px rgba(80, 255, 80, 0.35);
}

#bootText {
    max-width: 900px;
}

#bootCursor {
    display: inline-block;
    margin-top: 8px;
    animation: blink 0.8s infinite;
}

.boot-hidden {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

#panel {
    position: relative;
    z-index: 1000;
}

#desktop {
    position: relative;
    width: 100vw;
    height: calc(100vh - 48px);
}

#notificationContainer {
    position: fixed;
    top: 62px;
    right: 22px;
    z-index: 5000;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}

.system-notification {
    display: flex;
    align-items: center;
    width: 350px;
    min-height: 92px;
    padding: 15px 18px;
    background: #171b17;
    border: 2px solid #5c705c;
    box-shadow: 6px 6px 0 #000;
    color: #d8ffd8;
    cursor: pointer;
    pointer-events: auto;
    animation: notification-in 0.25s ease-out;
}

.notification-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    min-width: 48px;
    height: 48px;
    margin-right: 14px;
    color: #b6ff72;
    font-size: 27px;
}

.notification-text {
    min-width: 0;
}

.notification-title {
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.notification-from {
    margin-top: 6px;
    color: #ddd;
    font-size: 21px;
}

.notification-preview {
    margin-top: 2px;
    color: #888;
    font-size: 18px;
}

.system-notification:hover {
    background: #202620;
    border-color: #8cff8c;
}

@keyframes notification-in {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}


================================================================================
FILE: backupp\story board wokring\extracted_project\static\js\boot.js
================================================================================

const bootMessages = [
    "NEXUS OS",
    "",
    "INITIALIZING SYSTEM...",
    "LOADING NODE 01...",
    "CHECKING NETWORK...",
    "NETWORK: ONLINE",
    "SECURITY SERVICE: ACTIVE",
    "",
    "SYSTEM READY."
];


async function runBootSequence() {
    const bootScreen =
        document.getElementById(
            "bootScreen"
        );


    const bootText =
        document.getElementById(
            "bootText"
        );


    if (!bootScreen || !bootText) {
        return;
    }


    for (const message of bootMessages) {

        bootText.innerHTML +=
            message + "<br>";


        await new Promise(
            resolve =>
                setTimeout(
                    resolve,
                    180
                )
        );
    }


    await new Promise(
        resolve =>
            setTimeout(
                resolve,
                700
            )
    );


    bootScreen.classList.add(
        "boot-hidden"
    );


    setTimeout(
        function() {
            bootScreen.remove();
        },
        900
    );
}


runBootSequence();


================================================================================
FILE: backupp\story board wokring\extracted_project\static\js\desktop.js
================================================================================

async function loadHTML(elementId, url) {
    const element =
        document.getElementById(elementId);

    if (!element) {
        console.error(`Missing element: #${elementId}`);
        return false;
    }

    try {

        const response =
            await fetch(url);

        if (!response.ok) {
            throw new Error(
                `${url} returned ${response.status}`
            );
        }

        element.innerHTML =
            await response.text();

        return true;

    } catch (error) {

        console.error(
            `Failed to load ${url}:`,
            error
        );

        return false;
    }
}


function updateClock() {

    const clock =
        document.getElementById("clock");

    if (!clock) {
        return;
    }

    const now =
        new Date();

    clock.textContent =
        `${String(now.getHours()).padStart(2, "0")}:` +
        `${String(now.getMinutes()).padStart(2, "0")}`;
}


function showNewMessageNotification(messageStage = 0) {

    const container =
        document.getElementById(
            "notificationContainer"
        );

    const badge =
        document.getElementById(
            "messageBadge"
        );

    if (badge) {
        badge.classList.remove("hidden");
    }

    if (!container) {
        return;
    }

    const oldNotification =
        document.querySelector(
            ".system-notification"
        );

    if (oldNotification) {
        oldNotification.remove();
    }

    let preview =
        "You have a new message.";

    if (messageStage === 0) {
        preview =
            "If you're reading this...";
    }

    if (messageStage === 1) {
        preview =
            "You found it.";
    }

    if (messageStage === 2) {
        preview =
            "Look at the network.";
    }

    const notification =
        document.createElement("div");

    notification.className =
        "system-notification";

    notification.innerHTML = `
        <div class="notification-icon">✉</div>

        <div class="notification-text">

            <div class="notification-title">
                NEW MESSAGE
            </div>

            <div class="notification-from">
                UNKNOWN
            </div>

            <div class="notification-preview">
                ${preview}
            </div>

        </div>
    `;

    notification.addEventListener(
        "click",
        function () {

            notification.remove();

            openMessages();

        }
    );

    container.appendChild(
        notification
    );

    const sound =
        document.getElementById(
            "notificationSound"
        );

    if (sound) {

        sound.currentTime = 0;

        sound.play().catch(() => {});

    }

    setTimeout(
        () => {

            if (notification.parentElement) {
                notification.remove();
            }

        },
        7000
    );
}


async function openTerminal() {

    const existing =
        document.getElementById(
            "terminalWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        const input =
            document.getElementById(
                "commandInput"
            );

        if (input) {
            input.focus();
        }

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/terminal"
            );

        if (!response.ok) {
            throw new Error(
                `Terminal returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const terminal =
            document.getElementById(
                "terminalWindow"
            );

        if (!terminal) {
            throw new Error(
                "terminalWindow was not created."
            );
        }

        setupWindow(terminal);

        initializeTerminal();

    } catch (error) {

        console.error(
            "Terminal failed to open:",
            error
        );
    }
}


async function openMessages() {

    const existing =
        document.getElementById(
            "messagesWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/messages"
            );

        if (!response.ok) {
            throw new Error(
                `Messages returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const messagesWindow =
            document.getElementById(
                "messagesWindow"
            );

        if (!messagesWindow) {
            throw new Error(
                "messagesWindow was not created."
            );
        }

        setupWindow(
            messagesWindow
        );

        await updateMessages();

        const badge =
            document.getElementById(
                "messageBadge"
            );

        if (badge) {
            badge.classList.add("hidden");
        }

        const listBadge =
            document.getElementById(
                "messageListBadge"
            );

        if (listBadge) {
            listBadge.classList.add("hidden");
        }

    } catch (error) {

        console.error(
            "Messages failed to open:",
            error
        );
    }
}


async function openFiles() {

    const existing =
        document.getElementById(
            "filesWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/files"
            );

        if (!response.ok) {
            throw new Error(
                `Files returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const filesWindow =
            document.getElementById(
                "filesWindow"
            );

        if (!filesWindow) {
            throw new Error(
                "filesWindow was not created."
            );
        }

        setupWindow(
            filesWindow
        );

        if (
            typeof initializeFiles ===
            "function"
        ) {
            initializeFiles();
        }

    } catch (error) {

        console.error(
            "Files failed to open:",
            error
        );
    }
}


async function updateMessages() {

    try {

        const response =
            await fetch(
                "/api/messages"
            );

        if (!response.ok) {
            throw new Error(
                "Message API failed."
            );
        }

        const data =
            await response.json();

        const messageBody =
            document.querySelector(
                ".message-body"
            );

        const messagePreview =
            document.querySelector(
                ".message-preview"
            );

        if (!messageBody) {
            return;
        }

        if (data.system_log_found) {

            messageBody.textContent =
`Why are you looking at the system log?

The important part isn't what happened at 03:17.

It's what happened before it.

Look at the network.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Look at the network.";
            }

        } else if (data.message_unlocked) {

            messageBody.textContent =
`You found it.

I was hoping you wouldn't.

Look at the last successful login.

192.168.1.44

That address shouldn't exist on this network.

Someone used it to get into this machine.

Don't touch anything else yet.

Check:

/logs/system.log

Then come back here.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "You found it.";
            }

        } else {

            messageBody.textContent =
`If you're reading this, you've connected to Nexus Node 01.

This machine was shut down.

But something brought it back online.

I need you to find out what happened.

Start here:

/logs/access.log

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Start here.";
            }
        }

    } catch (error) {

        console.error(
            "Could not update messages:",
            error
        );
    }
}


document.addEventListener(
    "click",
    function (event) {

        if (
            event.target.closest(
                "#terminalButton"
            ) ||
            event.target.closest(
                "#terminalDesktopIcon"
            )
        ) {

            openTerminal();

            return;
        }

        if (
            event.target.closest(
                "#messagesButton"
            ) ||
            event.target.closest(
                "#messagesIcon"
            )
        ) {

            openMessages();

            return;
        }

        if (
            event.target.closest(
                "#filesIcon"
            )
        ) {

            openFiles();

            return;
        }
    }
);


async function startDesktop() {

    const panelLoaded =
        await loadHTML(
            "panel",
            "/panel"
        );

    const desktopLoaded =
        await loadHTML(
            "desktop",
            "/desktop"
        );

    if (!panelLoaded || !desktopLoaded) {

        console.error(
            "Desktop failed to initialize."
        );

        return;
    }

    updateClock();

    setTimeout(
        function () {
            showNewMessageNotification(0);
        },
        1200
    );
}


startDesktop();


setInterval(
    updateClock,
    1000
);


================================================================================
FILE: backupp\story board wokring\extracted_project\static\js\terminal.js
================================================================================

function initializeTerminal() {
    const input =
        document.getElementById("commandInput");

    const output =
        document.getElementById("terminalOutput");

    const body =
        document.getElementById("terminalBody");

    const prompt =
        document.getElementById("terminalPrompt");

    if (!input || !output || !body || !prompt) {
        console.error(
            "Terminal initialization failed."
        );

        return;
    }

    let currentDirectory =
        "/home/nexus";

    function displayPath() {
        if (currentDirectory === "/home/nexus") {
            return "~";
        }

        return currentDirectory;
    }

    function updatePrompt() {
        prompt.textContent =
            `nexus@node:${displayPath()}$`;
    }

    function escapeHTML(text) {
        const element =
            document.createElement("div");

        element.textContent = text;

        return element.innerHTML;
    }

    function addCommand(command, result) {
        const commandLine =
            document.createElement("div");

        commandLine.className =
            "terminal-line";

        commandLine.innerHTML =
            `<span class="terminal-prompt">nexus@node:${displayPath()}$</span> ${escapeHTML(command)}`;

        output.appendChild(commandLine);

        if (result) {
            const resultLine =
                document.createElement("div");

            resultLine.className =
                "terminal-line";

            resultLine.textContent =
                result;

            output.appendChild(resultLine);
        }

        body.scrollTop =
            body.scrollHeight;
    }

    async function runCommand(command) {
        try {
            const response =
                await fetch(
                    "/api/terminal",
                    {
                        method: "POST",
                        headers: {
                            "Content-Type":
                                "application/json"
                        },
                        body: JSON.stringify({
                            command: command,
                            cwd: currentDirectory
                        })
                    }
                );

            if (!response.ok) {
                throw new Error(
                    "Terminal backend error."
                );
            }

            const data =
                await response.json();

            if (data.output === "__CLEAR__") {
                output.innerHTML = "";
            } else {
                addCommand(
                    command,
                    data.output
                );
            }

            currentDirectory =
                data.cwd;

            updatePrompt();

            if (data.new_message) {
                showNewMessageNotification(
                    data.message_stage
                );
            }

            input.focus();

        } catch (error) {
            console.error(error);

            addCommand(
                command,
                "NEXUS: connection to system failed."
            );

            input.focus();
        }
    }

    input.addEventListener(
        "keydown",
        function (event) {
            if (event.key !== "Enter") {
                return;
            }

            event.preventDefault();

            const command =
                input.value.trim();

            if (!command) {
                return;
            }

            input.value = "";

            runCommand(command);
        }
    );

    body.addEventListener(
        "click",
        function () {
            input.focus();
        }
    );

    updatePrompt();

    input.focus();
}


window.initializeTerminal =
    initializeTerminal;


================================================================================
FILE: backupp\story board wokring\extracted_project\static\js\window.js
================================================================================

function setupWindow(windowElement) {
    if (!windowElement) {
        return;
    }

    const titlebar =
        windowElement.querySelector(
            ".terminal-titlebar, .messages-titlebar, .files-titlebar"
        );

    if (!titlebar) {
        console.error("Window titlebar not found.");
        return;
    }

    let dragging = false;
    let offsetX = 0;
    let offsetY = 0;

    titlebar.addEventListener(
        "mousedown",
        function (event) {

            if (event.target.closest("button, input")) {
                return;
            }

            if (windowElement.dataset.maximized === "true") {
                return;
            }

            dragging = true;

            const rect =
                windowElement.getBoundingClientRect();

            offsetX =
                event.clientX - rect.left;

            offsetY =
                event.clientY - rect.top;

            windowElement.style.transform = "none";

            windowElement.style.left =
                `${rect.left}px`;

            windowElement.style.top =
                `${rect.top}px`;

            windowElement.style.zIndex = "3000";

            document.body.style.userSelect = "none";
        }
    );

    document.addEventListener(
        "mousemove",
        function (event) {

            if (!dragging) {
                return;
            }

            let x =
                event.clientX - offsetX;

            let y =
                event.clientY - offsetY;

            const panel =
                document.getElementById("panel");

            const panelHeight =
                panel
                    ? panel.offsetHeight
                    : 48;

            const maxX =
                window.innerWidth -
                windowElement.offsetWidth;

            const maxY =
                window.innerHeight -
                windowElement.offsetHeight;

            x = Math.max(
                0,
                Math.min(x, maxX)
            );

            y = Math.max(
                panelHeight,
                Math.min(y, maxY)
            );

            windowElement.style.left =
                `${x}px`;

            windowElement.style.top =
                `${y}px`;
        }
    );

    document.addEventListener(
        "mouseup",
        function () {

            if (!dragging) {
                return;
            }

            dragging = false;

            document.body.style.userSelect = "";
        }
    );

    const closeButton =
        windowElement.querySelector(
            "#terminalClose, .messages-close, #filesClose"
        );

    if (closeButton) {

        closeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                windowElement.remove();

            }
        );
    }

    const maximizeButton =
        windowElement.querySelector(
            "#terminalMaximize"
        );

    if (maximizeButton) {

        maximizeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                const maximized =
                    windowElement.dataset.maximized === "true";

                if (!maximized) {

                    const rect =
                        windowElement.getBoundingClientRect();

                    windowElement.dataset.oldLeft =
                        `${rect.left}px`;

                    windowElement.dataset.oldTop =
                        `${rect.top}px`;

                    windowElement.dataset.oldWidth =
                        `${rect.width}px`;

                    windowElement.dataset.oldHeight =
                        `${rect.height}px`;

                    windowElement.style.transform =
                        "none";

                    windowElement.style.left =
                        "0px";

                    windowElement.style.top =
                        "48px";

                    windowElement.style.width =
                        "100vw";

                    windowElement.style.height =
                        "calc(100vh - 48px)";

                    windowElement.dataset.maximized =
                        "true";

                } else {

                    windowElement.style.left =
                        windowElement.dataset.oldLeft;

                    windowElement.style.top =
                        windowElement.dataset.oldTop;

                    windowElement.style.width =
                        windowElement.dataset.oldWidth;

                    windowElement.style.height =
                        windowElement.dataset.oldHeight;

                    windowElement.dataset.maximized =
                        "false";
                }

            }
        );
    }

    windowElement.addEventListener(
        "mousedown",
        function () {

            const currentZ =
                parseInt(
                    windowElement.style.zIndex || "500",
                    10
                );

            if (currentZ < 3000) {
                windowElement.style.zIndex = "3000";
            }

        }
    );
}


window.setupWindow = setupWindow;


================================================================================
FILE: backupp\story board wokring\extracted_project\system\commands.py
================================================================================

from datetime import datetime

from challenges.challenge01 import run_challenge01
from system.filesystem import FILESYSTEM


def normalize_path(path):
    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    if not parts:
        return "/"

    return "/" + "/".join(parts)


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help       Show available commands\n"
                "clear      Clear terminal\n"
                "pwd        Show current directory\n"
                "ls         List files\n"
                "cd         Change directory\n"
                "cat        Read a file\n"
                "whoami     Show current user\n"
                "hostname   Show system hostname\n"
                "echo       Print text\n"
                "date       Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y"),
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"ls: cannot access '{target}': No such file or directory",
                "cwd": current_dir
            }

        if item["type"] == "file":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        if not args:
            target = "/home/nexus"
        else:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cd: no such file or directory: {args[0]}",
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": f"cd: not a directory: {args[0]}",
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cat: {args[0]}: No such file or directory",
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": f"cat: {args[0]}: Is a directory",
                "cwd": current_dir
            }

        challenge_result = run_challenge01(
            command,
            target
        )

        return {
            "output": item["content"],
            "cwd": current_dir,
            "challenge_complete": challenge_result["complete"],
            "notification": challenge_result["notification"]
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


================================================================================
FILE: backupp\story board wokring\extracted_project\system\filesystem.py
================================================================================

FILESYSTEM = {
    "/": {
        "type": "directory",
        "children": [
            "home",
            "etc",
            "logs",
            "missions"
        ]
    },

    "/home": {
        "type": "directory",
        "children": [
            "nexus"
        ]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "readme.txt",
            "notes.txt"
        ]
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to NEXUS OS."
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Check the system logs.\nSomething doesn't look right."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": "interface=eth0\naddress=192.168.1.24\ngateway=192.168.1.1"
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/system.log": {
        "type": "file",
        "content": "[03:02] system started\n[03:04] network interface eth0 online\n[03:17] security service started"
    },

    "/logs/access.log": {
        "type": "file",
        "content": "[03:02] 192.168.1.12 LOGIN SUCCESS\n[03:04] 192.168.1.18 LOGIN SUCCESS\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:12] 192.168.1.44 LOGIN SUCCESS"
    },

    "/missions": {
        "type": "directory",
        "children": [
            "mission01"
        ]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": [
            "brief.txt"
        ]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": "MISSION 01\n\nSomething is wrong with the network.\nInvestigate the access logs."
    }
}


================================================================================
FILE: backupp\story board wokring\extracted_project\system\state.py
================================================================================

game_state = {
    "current_challenge": 1,

    "challenge01_complete": False,
    "challenge02_complete": False,
    "challenge03_complete": False,

    "unread_messages": 0,

    "messages": []
}


================================================================================
FILE: backupp\story board wokring\extracted_project\templates\apps\messages.html
================================================================================

<div
    id="messagesWindow"
    class="messages-window"
>

    <div class="messages-titlebar">

        <button
            class="messages-close"
            title="Close"
        ></button>

        <h1>
            MESSAGES
        </h1>

    </div>


    <div class="messages-content">

        <aside class="message-list">

            <div class="message-item active">

                <div class="message-sender">
                    UNKNOWN
                </div>

                <div class="message-preview">
                    No new messages.
                </div>

                <span
                    id="messageListBadge"
                    class="message-unread"
                >
                    !
                </span>

            </div>

        </aside>


        <section class="message-view">

            <div class="message-header">
                UNKNOWN
            </div>

            <div class="message-time">
                JUST NOW
            </div>

            <div class="message-body">
                Loading...
            </div>

        </section>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\extracted_project\templates\apps\terminal.html
================================================================================

<div id="terminalWindow" class="terminal-window">

    <div id="terminalTitlebar" class="terminal-titlebar">

        <div class="terminal-buttons">

            <button
                id="terminalClose"
                class="close-button"
                title="Close"
            ></button>

            <button
                id="terminalMaximize"
                class="maximize-button"
                title="Maximize"
            ></button>

        </div>

        <h1>TERMINAL</h1>

    </div>

    <div id="terminalBody" class="terminal-body">

        <div id="terminalOutput">

            <div class="terminal-line terminal-system">
                NEXUS NODE 01
            </div>

            <div class="terminal-line terminal-system">
                SYSTEM READY.
            </div>

            <div class="terminal-line terminal-system">
                Type 'help' for available commands.
            </div>

        </div>

        <div class="terminal-input-line">

            <span
                id="terminalPrompt"
                class="terminal-prompt"
            >
                nexus@node:~$
            </span>

            <input
                id="commandInput"
                type="text"
                autocomplete="off"
                spellcheck="false"
            >

        </div>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\extracted_project\templates\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">

        <button id="filesIcon">
            📁
        </button>

        <p>FILES</p>

    </div>


    <div class="desktop-icon">

        <button id="browserIcon">
            🌐
        </button>

        <p>BROWSER</p>

    </div>


    <div class="desktop-icon">

        <button id="messagesIcon">

            ✉

            <span
                id="messageBadge"
                class="notification-badge hidden"
            >
                1
            </span>

        </button>

        <p>MESSAGES</p>

    </div>


    <div class="desktop-icon">

        <button id="terminalDesktopIcon">
            ▣
        </button>

        <p>TERMINAL</p>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\extracted_project\templates\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>


================================================================================
FILE: backupp\story board wokring\extracted_project\templates\panel.html
================================================================================

<header class="panel">

    <button
        id="menuButton"
        class="menu-button"
    >
        NEXUS
    </button>

    <div class="panel-apps">

        <button id="terminalButton">
            TERMINAL
        </button>

        <button id="messagesButton">
            MESSAGES
        </button>

    </div>

    <div class="system-tray">

        <span class="network-status">
            ● ONLINE
        </span>

        <span id="clock">
            00:00
        </span>

    </div>

</header>


================================================================================
FILE: backupp\story board wokring\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
    <link
    rel="stylesheet"
    href="{{ url_for('static', filename='css/nova.css') }}"
>
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

<script src="{{ url_for('static', filename='js/window.js') }}"></script>
<script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
<script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
<script src="{{ url_for('static', filename='js/boot.js') }}"></script>
<script src="{{ url_for('static', filename='js/nova.js') }}"></script>
</body>

</html>

================================================================================
FILE: backupp\story board wokring\static\css\apps\messages.css
================================================================================

.messages-window {
    position: fixed;
    top: 100px;
    left: 50%;
    z-index: 500;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #101410;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.messages-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
}

.messages-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.messages-close {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
    background: #ff5f57;
}

.messages-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100% - 42px);
}

.message-list {
    overflow-y: auto;
    background: #0c100c;
    border-right: 2px solid #303830;
}

.message-item {
    position: relative;
    padding: 18px;
    border-bottom: 1px solid #292e29;
}

.message-item.active {
    background: #171d17;
}

.message-sender {
    margin-bottom: 5px;
    color: #b8ffb8;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.message-preview {
    max-width: 210px;
    overflow: hidden;
    color: #888;
    font-size: 19px;
    white-space: nowrap;
    text-overflow: ellipsis;
}

.message-unread {
    position: absolute;
    top: 17px;
    right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #b6ff72;
    color: #111;
    font-family: "Press Start 2P", monospace;
    font-size: 7px;
}

.message-view {
    min-width: 0;
    padding: 28px;
    overflow-y: auto;
}

.message-header {
    margin-bottom: 5px;
    color: #baffba;
    font-family: "Press Start 2P", monospace;
    font-size: 11px;
}

.message-time {
    margin-bottom: 25px;
    color: #666;
    font-size: 17px;
}

.message-body {
    max-width: 650px;
    color: #d1d1d1;
    font-size: 23px;
    line-height: 1.1;
    white-space: pre-wrap;
}


================================================================================
FILE: backupp\story board wokring\static\css\apps\terminal.css
================================================================================

.terminal-window {
    position: fixed;
    top: 90px;
    left: 50%;
    z-index: 500;
    display: flex;
    flex-direction: column;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #050805;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.terminal-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    min-height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
    cursor: grab;
}

.terminal-titlebar:active {
    cursor: grabbing;
}

.terminal-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.terminal-buttons {
    display: flex;
    align-items: center;
    gap: 8px;
}

.terminal-buttons button {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
}

.close-button {
    background: #ff5f57;
}

.maximize-button {
    background: #28c840;
}

.terminal-body {
    flex: 1;
    min-height: 0;
    padding: 18px 20px;
    overflow-y: auto;
    background: #050805;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
    line-height: 1.05;
    cursor: text;
}

.terminal-body::-webkit-scrollbar {
    width: 10px;
}

.terminal-body::-webkit-scrollbar-track {
    background: #0c100c;
}

.terminal-body::-webkit-scrollbar-thumb {
    background: #384438;
}

.terminal-body::-webkit-scrollbar-thumb:hover {
    background: #526052;
}

.terminal-line {
    min-height: 24px;
    white-space: pre-wrap;
    word-break: break-word;
}

.terminal-system {
    color: #71c871;
}

.terminal-prompt {
    color: #aaffaa;
}

.terminal-input-line {
    display: flex;
    align-items: baseline;
    width: 100%;
    min-height: 27px;
}

#commandInput {
    flex: 1;
    min-width: 0;
    margin-left: 8px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #9cff9c;
    caret-color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
}

#commandInput::selection {
    background: #496949;
    color: white;
}


================================================================================
FILE: backupp\story board wokring\static\css\desktop.css
================================================================================

#desktop {
    background:
        linear-gradient(
            rgba(4, 10, 5, 0.45),
            rgba(4, 10, 5, 0.45)
        ),
        url("/static/assets/wallpapers/nexus-bg.png");

    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
}

.desktop-icons {
    display: grid;
    grid-template-columns: repeat(4, 120px);
    gap: 24px;
    padding: 38px;
}

.desktop-icon {
    position: relative;
    width: 120px;
    text-align: center;
}

.desktop-icon button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 76px;
    height: 76px;
    margin: 0 auto 8px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #baffba;
    font-size: 42px;
}

.desktop-icon button:hover {
    border-color: #405240;
    background: rgba(120, 255, 120, 0.06);
}

.desktop-icon p {
    margin: 0;
    color: #c7c7c7;
    font-size: 19px;
    letter-spacing: 1px;
}

.notification-badge {
    position: absolute;
    top: -4px;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 21px;
    height: 21px;
    border-radius: 50%;
    background: #b6ff72;
    color: #101010;
    font-family: "Press Start 2P", monospace;
    font-size: 8px;
}

.hidden {
    display: none;
}


================================================================================
FILE: backupp\story board wokring\static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: backupp\story board wokring\static\css\nova.css
================================================================================

#novaAssistant {
    position: fixed;
    right: 24px;
    bottom: 24px;
    width: 300px;
    z-index: 5000;
    background: rgba(10, 14, 10, 0.96);
    border: 1px solid #4f7d4f;
    box-shadow: 0 0 24px rgba(70, 255, 70, 0.12);
    font-family: "VT323", monospace;
    color: #d7e8d7;
}

.nova-header {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 12px;
    border-bottom: 1px solid #304830;
}

.nova-avatar {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #76b876;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 12px;
}

.nova-name {
    color: #9cff9c;
    font-size: 20px;
}

.nova-status {
    color: #668866;
    font-size: 14px;
}

#novaMinimize {
    margin-left: auto;
    border: 0;
    background: transparent;
    color: #8aaa8a;
    font-size: 22px;
    cursor: pointer;
}

#novaBody {
    padding: 14px;
}

#novaMessage {
    min-height: 80px;
    font-size: 18px;
    line-height: 1.3;
    white-space: pre-line;
}

#novaHint {
    width: 100%;
    margin-top: 12px;
    padding: 9px;
    border: 1px solid #4f7d4f;
    background: #111911;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 17px;
    cursor: pointer;
}

#novaHint:hover {
    background: #192219;
}

#novaAssistant.nova-minimized #novaBody {
    display: none;
}

================================================================================
FILE: backupp\story board wokring\static\css\panel.css
================================================================================

.panel {
    position: relative;
    z-index: 1000;
    display: flex;
    align-items: center;
    width: 100%;
    height: 48px;
    padding: 0 18px;
    background: #111;
    border-bottom: 2px solid #333;
    color: #d7ffd7;
}

.menu-button {
    padding: 5px 12px;
    border: 1px solid #555;
    background: #181818;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.menu-button:hover {
    background: #222;
    border-color: #8cff8c;
}

.panel-apps {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: 20px;
}

.panel-apps button {
    padding: 5px 9px;
    border: 1px solid transparent;
    background: transparent;
    color: #aaa;
    font-family: "VT323", monospace;
    font-size: 19px;
}

.panel-apps button:hover {
    color: #9cff9c;
    border-color: #444;
    background: #181818;
}

.system-tray {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-left: auto;
    color: #888;
    font-size: 19px;
}

.network-status {
    color: #75d675;
}


================================================================================
FILE: backupp\story board wokring\static\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

html,
body {
    width: 100%;
    height: 100%;
    margin: 0;
}

body {
    background: #080b08;
    color: #d7ffd7;
    font-family: "VT323", monospace;
    overflow: hidden;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}
/* =========================================================
   CINEMATIC INTRO
   ========================================================= */

#introScreen {
    position: fixed;
    inset: 0;
    z-index: 10000;

    display: flex;
    align-items: center;
    justify-content: center;

    background: #000000;
    color: #e8e8e8;

    padding: 40px;

    opacity: 1;

    transition:
        opacity 1.8s ease;
}


#introText {
    width: min(850px, 80vw);

    text-align: center;

    font-family: "VT323", monospace;

    font-size: clamp(
        24px,
        3vw,
        38px
    );

    line-height: 1.5;

    letter-spacing: 1px;

    color: #dedede;

    text-shadow:
        0 0 12px rgba(
            255,
            255,
            255,
            0.08
        );
}


.intro-line {
    opacity: 0;

    transform:
        translateY(8px);

    filter: blur(2px);

    transition:
        opacity 1.6s ease,
        transform 1.6s ease,
        filter 1.6s ease;
}


.intro-line.visible {
    opacity: 1;

    transform:
        translateY(0);

    filter: blur(0);
}


.intro-line.fade {
    opacity: 0;

    transition:
        opacity 1.2s ease;
}


#introScreen.intro-hidden {
    opacity: 0;

    pointer-events: none;
}

#bootScreen {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    background: #020502;
    color: #8cff8c;
    font-size: 24px;
    line-height: 1.25;
    text-shadow: 0 0 8px rgba(80, 255, 80, 0.35);
}

#bootText {
    max-width: 900px;
}

.boot-line {
    min-height: 1.25em;
    margin-bottom: 4px;
    white-space: pre-wrap;
}

.boot-title {
    margin-bottom: 20px;
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 32px;
    letter-spacing: 2px;
    text-shadow:
        0 0 8px rgba(80, 255, 80, 0.45),
        0 0 18px rgba(80, 255, 80, 0.2);
}

.boot-story {
    color: #a8d8a8;
}

.boot-system {
    color: #75d675;
    font-family: "VT323", monospace;
}

.boot-warning {
    color: #d8ff9b;
    text-shadow:
        0 0 8px rgba(180, 255, 100, 0.35);
}

.boot-node {
    margin: 12px 0;
    color: #ffffff;
    font-family: "Press Start 2P", monospace;
    font-size: 18px;
    letter-spacing: 1px;
    text-shadow:
        0 0 10px rgba(255, 255, 255, 0.25);
}

#bootCursor {
    display: inline-block;
    margin-top: 8px;
    animation: blink 0.8s infinite;
}

.boot-hidden {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

#panel {
    position: relative;
    z-index: 1000;
}

#desktop {
    position: relative;
    width: 100vw;
    height: calc(100vh - 48px);
}

#notificationContainer {
    position: fixed;
    top: 62px;
    right: 22px;
    z-index: 5000;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}

.system-notification {
    display: flex;
    align-items: center;
    width: 350px;
    min-height: 92px;
    padding: 15px 18px;
    background: #171b17;
    border: 2px solid #5c705c;
    box-shadow: 6px 6px 0 #000;
    color: #d8ffd8;
    cursor: pointer;
    pointer-events: auto;
    animation: notification-in 0.25s ease-out;
}

.notification-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    min-width: 48px;
    height: 48px;
    margin-right: 14px;
    color: #b6ff72;
    font-size: 27px;
}

.notification-text {
    min-width: 0;
}

.notification-title {
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.notification-from {
    margin-top: 6px;
    color: #ddd;
    font-size: 21px;
}

.notification-preview {
    margin-top: 2px;
    color: #888;
    font-size: 18px;
}

.system-notification:hover {
    background: #202620;
    border-color: #8cff8c;
}

@keyframes notification-in {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}


================================================================================
FILE: backupp\story board wokring\static\js\boot.js
================================================================================

let skipIntro = false;
const keysDown = new Set();

document.addEventListener(
    "keydown",
    function (event) {
        keysDown.add(
            event.key.toLowerCase()
        );

        if (
            keysDown.has("z") &&
            keysDown.has("9")
        ) {
            skipIntro = true;
        }
    }
);

document.addEventListener(
    "keyup",
    function (event) {
        keysDown.delete(
            event.key.toLowerCase()
        );
    }
);

const introMessages = [
    "In 2018, something went wrong.",
    "A system was built to watch.",
    "It learned.",
    "It remembered.",
    "Then they shut it down.",
    "Or so they thought.",
    "Eight years passed.",
    "The machine stayed silent.",
    "Until tonight."
];

const bootMessages = [
    "NEXUS NODE 01",
    "----------------",
    "> POWER ........ ONLINE",
    "> STORAGE ...... OK",
    "> NETWORK ...... CONNECTED",
    "> SYSTEM ....... READY",
    "",
    "> LAST USER .... UNKNOWN",
    "> LAST SHUTDOWN  NOT FOUND",
    "",
    "> BOOT COMPLETE"
];

function sleep(milliseconds) {
    return new Promise(
        resolve => setTimeout(
            resolve,
            milliseconds
        )
    );
}

async function runIntroSequence() {
    const introScreen =
        document.getElementById(
            "introScreen"
        );

    const introText =
        document.getElementById(
            "introText"
        );

    if (!introScreen || !introText) {
        return;
    }

    for (const message of introMessages) {
        if (skipIntro) {
            break;
        }

        const line =
            document.createElement(
                "div"
            );

        line.className =
            "intro-line";

        line.textContent =
            message;

        introText.appendChild(
            line
        );

        await sleep(350);

        if (skipIntro) {
            break;
        }

        line.classList.add(
            "visible"
        );

        await sleep(1900);

        if (skipIntro) {
            break;
        }

        line.classList.add(
            "fade"
        );

        await sleep(900);

        if (line.parentNode) {
            line.parentNode.removeChild(
                line
            );
        }
    }

    introText.innerHTML = "";

    await sleep(
        skipIntro ? 200 : 1200
    );

    introScreen.classList.add(
        "intro-hidden"
    );

    await sleep(1800);

    if (introScreen.parentNode) {
        introScreen.remove();
    }
}

async function runBootSequence() {
    const bootScreen =
        document.getElementById(
            "bootScreen"
        );

    const bootText =
        document.getElementById(
            "bootText"
        );

    if (!bootScreen || !bootText) {
        return;
    }

    for (const message of bootMessages) {
        if (message === "") {
            bootText.innerHTML += "<br>";
        } else {
            bootText.innerHTML +=
                message + "<br>";
        }

        await sleep(110);
    }

    await sleep(900);

bootScreen.classList.add(
    "boot-hidden"
);

await sleep(900);

showNewMessageNotification(0);

    setTimeout(
        function () {
            if (bootScreen.parentNode) {
                bootScreen.remove();
            }
        },
        900
    );
}

async function startOpening() {
    await runIntroSequence();
    await runBootSequence();
}

startOpening();

================================================================================
FILE: backupp\story board wokring\static\js\desktop.js
================================================================================

async function loadHTML(elementId, url) {
    const element =
        document.getElementById(elementId);

    if (!element) {
        console.error(`Missing element: #${elementId}`);
        return false;
    }

    try {
        const response =
            await fetch(url);

        if (!response.ok) {
            throw new Error(
                `${url} returned ${response.status}`
            );
        }

        element.innerHTML =
            await response.text();

        return true;

    } catch (error) {
        console.error(
            `Failed to load ${url}:`,
            error
        );

        return false;
    }
}

function updateClock() {
    const clock =
        document.getElementById("clock");

    if (!clock) {
        return;
    }

    const now =
        new Date();

    clock.textContent =
        `${String(now.getHours()).padStart(2, "0")}:` +
        `${String(now.getMinutes()).padStart(2, "0")}`;
}

function playMessageSound() {
    const sound =
        document.getElementById(
            "notificationSound"
        );

    if (!sound) {
        return;
    }

    sound.currentTime = 0;

    sound.play().catch(() => {
        const playSound =
            function () {
                sound.currentTime = 0;

                sound.play().catch(() => {});

                document.removeEventListener(
                    "click",
                    playSound
                );

                document.removeEventListener(
                    "keydown",
                    playSound
                );
            };

        document.addEventListener(
            "click",
            playSound,
            { once: true }
        );

        document.addEventListener(
            "keydown",
            playSound,
            { once: true }
        );
    });
}

function showNewMessageNotification(messageStage = 0) {
    const container =
        document.getElementById(
            "notificationContainer"
        );

    const badge =
        document.getElementById(
            "messageBadge"
        );

    if (badge) {
        badge.classList.remove("hidden");
    }

    if (!container) {
        return;
    }

    const oldNotification =
        document.querySelector(
            ".system-notification"
        );

    if (oldNotification) {
        oldNotification.remove();
    }

    let preview =
        "You have a new message.";

    if (messageStage === 0) {
        preview =
            "If you're reading this...";
    }

    if (messageStage === 1) {
        preview =
            "You found it.";
    }

    if (messageStage === 2) {
        preview =
            "Look at the network.";
    }

    const notification =
        document.createElement("div");

    notification.className =
        "system-notification";

    notification.innerHTML = `
        <div class="notification-icon">✉</div>

        <div class="notification-text">

            <div class="notification-title">
                NEW MESSAGE
            </div>

            <div class="notification-from">
                UNKNOWN
            </div>

            <div class="notification-preview">
                ${preview}
            </div>

        </div>
    `;

    notification.addEventListener(
        "click",
        function () {
            notification.remove();
            openMessages();
        }
    );

    container.appendChild(
        notification
    );

    playMessageSound();

    setTimeout(
        () => {
            if (notification.parentElement) {
                notification.remove();
            }
        },
        7000
    );
}

async function openTerminal() {
    const existing =
        document.getElementById(
            "terminalWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";

        const input =
            document.getElementById(
                "commandInput"
            );

        if (input) {
            input.focus();
        }

        return;
    }

    try {
        const response =
            await fetch(
                "/apps/terminal"
            );

        if (!response.ok) {
            throw new Error(
                `Terminal returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const terminal =
            document.getElementById(
                "terminalWindow"
            );

        if (!terminal) {
            throw new Error(
                "terminalWindow was not created."
            );
        }

        setupWindow(terminal);
        initializeTerminal();

    } catch (error) {
        console.error(
            "Terminal failed to open:",
            error
        );
    }
}

async function openMessages() {
    const existing =
        document.getElementById(
            "messagesWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";

        await updateMessages();

        return;
    }

    try {
        const response =
            await fetch(
                "/apps/messages"
            );

        if (!response.ok) {
            throw new Error(
                `Messages returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const messagesWindow =
            document.getElementById(
                "messagesWindow"
            );

        if (!messagesWindow) {
            throw new Error(
                "messagesWindow was not created."
            );
        }

        setupWindow(
            messagesWindow
        );

        await updateMessages();

        const badge =
            document.getElementById(
                "messageBadge"
            );

        if (badge) {
            badge.classList.add("hidden");
        }

        const listBadge =
            document.getElementById(
                "messageListBadge"
            );

        if (listBadge) {
            listBadge.classList.add("hidden");
        }

    } catch (error) {
        console.error(
            "Messages failed to open:",
            error
        );
    }
}

async function openFiles() {
    const existing =
        document.getElementById(
            "filesWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";
        return;
    }

    try {
        const response =
            await fetch(
                "/apps/files"
            );

        if (!response.ok) {
            throw new Error(
                `Files returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const filesWindow =
            document.getElementById(
                "filesWindow"
            );

        if (!filesWindow) {
            throw new Error(
                "filesWindow was not created."
            );
        }

        setupWindow(
            filesWindow
        );

        if (
            typeof initializeFiles ===
            "function"
        ) {
            initializeFiles();
        }

    } catch (error) {
        console.error(
            "Files failed to open:",
            error
        );
    }
}

async function updateMessages() {
    try {
        const response =
            await fetch(
                "/api/messages"
            );

        if (!response.ok) {
            throw new Error(
                "Message API failed."
            );
        }

        const data =
            await response.json();

        const messageBody =
            document.querySelector(
                ".message-body"
            );

        const messagePreview =
            document.querySelector(
                ".message-preview"
            );

        if (!messageBody) {
            return;
        }

        if (data.system_log_found) {
            messageBody.textContent =
                `Why are you looking at the system log?

The important part isn't what happened at 03:17.

It's what happened before it.

Look at the network.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Look at the network.";
            }

        } else if (data.message_unlocked) {
            messageBody.textContent =
                `You found it.

I was hoping you wouldn't.

Look at the last successful login.

192.168.1.44

That address shouldn't exist on this network.

Someone used it to get into this machine.

Don't touch anything else yet.

Check:

/logs/system.log

Then come back here.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "You found it.";
            }

        } else {
            messageBody.textContent =
                `If you're reading this, you've connected to Nexus Node 01.

This machine was shut down.

But something brought it back online.

I need you to find out what happened.

Start here:

/logs/access.log

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Start here.";
            }
        }

    } catch (error) {
        console.error(
            "Could not update messages:",
            error
        );
    }
}

document.addEventListener(
    "click",
    function (event) {
        if (
            event.target.closest(
                "#terminalButton"
            ) ||
            event.target.closest(
                "#terminalDesktopIcon"
            )
        ) {
            openTerminal();
            return;
        }

        if (
            event.target.closest(
                "#messagesButton"
            ) ||
            event.target.closest(
                "#messagesIcon"
            )
        ) {
            openMessages();
            return;
        }

        if (
            event.target.closest(
                "#filesIcon"
            )
        ) {
            openFiles();
            return;
        }
    }
);

async function startDesktop() {
    const panelLoaded =
        await loadHTML(
            "panel",
            "/panel"
        );

    const desktopLoaded =
        await loadHTML(
            "desktop",
            "/desktop"
        );

    if (!panelLoaded || !desktopLoaded) {
        console.error(
            "Desktop failed to initialize."
        );

        return;
    }

    updateClock();
}

startDesktop();

setInterval(
    updateClock,
    1000
);

================================================================================
FILE: backupp\story board wokring\static\js\nova.js
================================================================================

let novaState = null;

async function getNovaState() {
    try {
        const response =
            await fetch("/api/state");

        if (!response.ok) {
            return null;
        }

        novaState =
            await response.json();

        return novaState;
    } catch (error) {
        console.error(error);
        return null;
    }
}

function setNovaMessage(message) {
    const element =
        document.getElementById("novaMessage");

    if (element) {
        element.textContent = message;
    }
}

function getNovaMessage(state) {
    if (!state) {
        return "I'm still connecting to NEXUS. Give me a second.";
    }

    if (!state.message_unlocked) {
        return "Hey. Start with your messages. There's something waiting for you there.";
    }

    if (
        state.message_unlocked &&
        !state.system_log_found
    ) {
        return "Good. Now explore the system. The message is pointing you somewhere. Start looking through the available logs.";
    }

    if (
        state.system_log_found &&
        !state.challenge01_complete
    ) {
        return "You're getting close. Don't just read the log. Look at the addresses and ask yourself which one doesn't belong.";
    }

    if (state.challenge01_complete) {
        return "Nice work. You found the first answer. There's more to NEXUS than that one connection, though.";
    }

    return "Take your time. Look around and see what the system is trying to show you.";
}

async function updateNova() {
    const state =
        await getNovaState();

    setNovaMessage(
        getNovaMessage(state)
    );
}

function initializeNova() {
    const hintButton =
        document.getElementById("novaHint");

    const minimizeButton =
        document.getElementById("novaMinimize");

    const assistant =
        document.getElementById("novaAssistant");

    if (hintButton) {
        hintButton.addEventListener(
            "click",
            updateNova
        );
    }

    if (
        minimizeButton &&
        assistant
    ) {
        minimizeButton.addEventListener(
            "click",
            function () {
                assistant.classList.toggle(
                    "nova-minimized"
                );
            }
        );
    }

    updateNova();
}

async function loadNova() {
    try {
        const response =
            await fetch("/nova");

        if (!response.ok) {
            throw new Error(
                "NOVA failed to load."
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        initializeNova();
    } catch (error) {
        console.error(error);
    }
}

loadNova();

================================================================================
FILE: backupp\story board wokring\static\js\terminal.js
================================================================================

function initializeTerminal() {
    const input =
        document.getElementById("commandInput");

    const output =
        document.getElementById("terminalOutput");

    const body =
        document.getElementById("terminalBody");

    const prompt =
        document.getElementById("terminalPrompt");

    if (!input || !output || !body || !prompt) {
        console.error(
            "Terminal initialization failed."
        );

        return;
    }

    let currentDirectory =
        "/home/nexus";

    function displayPath() {
        if (currentDirectory === "/home/nexus") {
            return "~";
        }

        return currentDirectory;
    }

    function updatePrompt() {
        prompt.textContent =
            `nexus@node:${displayPath()}$`;
    }

    function escapeHTML(text) {
        const element =
            document.createElement("div");

        element.textContent = text;

        return element.innerHTML;
    }

    function addCommand(command, result) {
        const commandLine =
            document.createElement("div");

        commandLine.className =
            "terminal-line";

        commandLine.innerHTML =
            `<span class="terminal-prompt">nexus@node:${displayPath()}$</span> ${escapeHTML(command)}`;

        output.appendChild(commandLine);

        if (result) {
            const resultLine =
                document.createElement("div");

            resultLine.className =
                "terminal-line";

            resultLine.textContent =
                result;

            output.appendChild(resultLine);
        }

        body.scrollTop =
            body.scrollHeight;
    }

    async function runCommand(command) {
        try {
            const response =
                await fetch(
                    "/api/terminal", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json"
                        },
                        body: JSON.stringify({
                            command: command,
                            cwd: currentDirectory
                        })
                    }
                );

            if (!response.ok) {
                throw new Error(
                    "Terminal backend error."
                );
            }

            const data =
                await response.json();

            if (data.output === "__CLEAR__") {
                output.innerHTML = "";
            } else {
                addCommand(
                    command,
                    data.output
                );
            }

            currentDirectory =
                data.cwd;

            updatePrompt();
            if (data.new_message) {
                showNewMessageNotification(
                    data.message_stage
                );

                const messagesWindow =
                    document.getElementById(
                        "messagesWindow"
                    );

                if (messagesWindow) {
                    await updateMessages();
                }
            }

            input.focus();

        } catch (error) {
            console.error(error);

            addCommand(
                command,
                "NEXUS: connection to system failed."
            );

            input.focus();
        }
    }

    input.addEventListener(
        "keydown",
        function (event) {
            if (event.key !== "Enter") {
                return;
            }

            event.preventDefault();

            const command =
                input.value.trim();

            if (!command) {
                return;
            }

            input.value = "";

            runCommand(command);
        }
    );

    body.addEventListener(
        "click",
        function () {
            input.focus();
        }
    );

    updatePrompt();

    input.focus();
}


window.initializeTerminal =
    initializeTerminal;

================================================================================
FILE: backupp\story board wokring\static\js\window.js
================================================================================

function setupWindow(windowElement) {
    if (!windowElement) {
        return;
    }

    const titlebar =
        windowElement.querySelector(
            ".terminal-titlebar, .messages-titlebar, .files-titlebar"
        );

    if (!titlebar) {
        console.error("Window titlebar not found.");
        return;
    }

    let dragging = false;
    let offsetX = 0;
    let offsetY = 0;

    titlebar.addEventListener(
        "mousedown",
        function (event) {

            if (event.target.closest("button, input")) {
                return;
            }

            if (windowElement.dataset.maximized === "true") {
                return;
            }

            dragging = true;

            const rect =
                windowElement.getBoundingClientRect();

            offsetX =
                event.clientX - rect.left;

            offsetY =
                event.clientY - rect.top;

            windowElement.style.transform = "none";

            windowElement.style.left =
                `${rect.left}px`;

            windowElement.style.top =
                `${rect.top}px`;

            windowElement.style.zIndex = "3000";

            document.body.style.userSelect = "none";
        }
    );

    document.addEventListener(
        "mousemove",
        function (event) {

            if (!dragging) {
                return;
            }

            let x =
                event.clientX - offsetX;

            let y =
                event.clientY - offsetY;

            const panel =
                document.getElementById("panel");

            const panelHeight =
                panel
                    ? panel.offsetHeight
                    : 48;

            const maxX =
                window.innerWidth -
                windowElement.offsetWidth;

            const maxY =
                window.innerHeight -
                windowElement.offsetHeight;

            x = Math.max(
                0,
                Math.min(x, maxX)
            );

            y = Math.max(
                panelHeight,
                Math.min(y, maxY)
            );

            windowElement.style.left =
                `${x}px`;

            windowElement.style.top =
                `${y}px`;
        }
    );

    document.addEventListener(
        "mouseup",
        function () {

            if (!dragging) {
                return;
            }

            dragging = false;

            document.body.style.userSelect = "";
        }
    );

    const closeButton =
        windowElement.querySelector(
            "#terminalClose, .messages-close, #filesClose"
        );

    if (closeButton) {

        closeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                windowElement.remove();

            }
        );
    }

    const maximizeButton =
        windowElement.querySelector(
            "#terminalMaximize"
        );

    if (maximizeButton) {

        maximizeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                const maximized =
                    windowElement.dataset.maximized === "true";

                if (!maximized) {

                    const rect =
                        windowElement.getBoundingClientRect();

                    windowElement.dataset.oldLeft =
                        `${rect.left}px`;

                    windowElement.dataset.oldTop =
                        `${rect.top}px`;

                    windowElement.dataset.oldWidth =
                        `${rect.width}px`;

                    windowElement.dataset.oldHeight =
                        `${rect.height}px`;

                    windowElement.style.transform =
                        "none";

                    windowElement.style.left =
                        "0px";

                    windowElement.style.top =
                        "48px";

                    windowElement.style.width =
                        "100vw";

                    windowElement.style.height =
                        "calc(100vh - 48px)";

                    windowElement.dataset.maximized =
                        "true";

                } else {

                    windowElement.style.left =
                        windowElement.dataset.oldLeft;

                    windowElement.style.top =
                        windowElement.dataset.oldTop;

                    windowElement.style.width =
                        windowElement.dataset.oldWidth;

                    windowElement.style.height =
                        windowElement.dataset.oldHeight;

                    windowElement.dataset.maximized =
                        "false";
                }

            }
        );
    }

    windowElement.addEventListener(
        "mousedown",
        function () {

            const currentZ =
                parseInt(
                    windowElement.style.zIndex || "500",
                    10
                );

            if (currentZ < 3000) {
                windowElement.style.zIndex = "3000";
            }

        }
    );
}


window.setupWindow = setupWindow;


================================================================================
FILE: backupp\story board wokring\system\commands.py
================================================================================

from datetime import datetime

from challenges.challenge01 import run_challenge01
from system.filesystem import FILESYSTEM


def normalize_path(path):
    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    if not parts:
        return "/"

    return "/" + "/".join(parts)


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help       Show available commands\n"
                "clear      Clear terminal\n"
                "pwd        Show current directory\n"
                "ls         List files\n"
                "cd         Change directory\n"
                "cat        Read a file\n"
                "whoami     Show current user\n"
                "hostname   Show system hostname\n"
                "echo       Print text\n"
                "date       Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y"),
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"ls: cannot access '{target}': No such file or directory",
                "cwd": current_dir
            }

        if item["type"] == "file":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        if not args:
            target = "/home/nexus"
        else:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cd: no such file or directory: {args[0]}",
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": f"cd: not a directory: {args[0]}",
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cat: {args[0]}: No such file or directory",
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": f"cat: {args[0]}: Is a directory",
                "cwd": current_dir
            }

        challenge_result = run_challenge01(
            command,
            target
        )

        return {
            "output": item["content"],
            "cwd": current_dir,
            "challenge_complete": challenge_result["complete"],
            "notification": challenge_result["notification"]
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


================================================================================
FILE: backupp\story board wokring\system\filesystem.py
================================================================================

FILESYSTEM = {
    "/": {
        "type": "directory",
        "children": [
            "home",
            "etc",
            "logs",
            "missions"
        ]
    },

    "/home": {
        "type": "directory",
        "children": [
            "nexus"
        ]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "readme.txt",
            "notes.txt"
        ]
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to NEXUS OS."
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Check the system logs.\nSomething doesn't look right."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": "interface=eth0\naddress=192.168.1.24\ngateway=192.168.1.1"
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/system.log": {
        "type": "file",
        "content": "[03:02] system started\n[03:04] network interface eth0 online\n[03:17] security service started"
    },

    "/logs/access.log": {
        "type": "file",
        "content": "[03:02] 192.168.1.12 LOGIN SUCCESS\n[03:04] 192.168.1.18 LOGIN SUCCESS\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:12] 192.168.1.44 LOGIN SUCCESS"
    },

    "/missions": {
        "type": "directory",
        "children": [
            "mission01"
        ]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": [
            "brief.txt"
        ]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": "MISSION 01\n\nSomething is wrong with the network.\nInvestigate the access logs."
    }
}


================================================================================
FILE: backupp\story board wokring\system\state.py
================================================================================

game_state = {
    "current_challenge": 1,

    "challenge01_complete": False,
    "challenge02_complete": False,
    "challenge03_complete": False,

    "unread_messages": 0,

    "messages": []
}


================================================================================
FILE: backupp\story board wokring\templates\apps\messages.html
================================================================================

<div
    id="messagesWindow"
    class="messages-window"
>

    <div class="messages-titlebar">

        <button
            class="messages-close"
            title="Close"
        ></button>

        <h1>
            MESSAGES
        </h1>

    </div>


    <div class="messages-content">

        <aside class="message-list">

            <div class="message-item active">

                <div class="message-sender">
                    UNKNOWN
                </div>

                <div class="message-preview">
                    No new messages.
                </div>

                <span
                    id="messageListBadge"
                    class="message-unread"
                >
                    !
                </span>

            </div>

        </aside>


        <section class="message-view">

            <div class="message-header">
                UNKNOWN
            </div>

            <div class="message-time">
                JUST NOW
            </div>

            <div class="message-body">
                Loading...
            </div>

        </section>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\templates\apps\terminal.html
================================================================================

<div id="terminalWindow" class="terminal-window">

    <div id="terminalTitlebar" class="terminal-titlebar">

        <div class="terminal-buttons">

            <button
                id="terminalClose"
                class="close-button"
                title="Close"
            ></button>

            <button
                id="terminalMaximize"
                class="maximize-button"
                title="Maximize"
            ></button>

        </div>

        <h1>TERMINAL</h1>

    </div>

    <div id="terminalBody" class="terminal-body">

        <div id="terminalOutput">

            <div class="terminal-line terminal-system">
                NEXUS NODE 01
            </div>

            <div class="terminal-line terminal-system">
                SYSTEM READY.
            </div>

            <div class="terminal-line terminal-system">
                Type 'help' for available commands.
            </div>

        </div>

        <div class="terminal-input-line">

            <span
                id="terminalPrompt"
                class="terminal-prompt"
            >
                nexus@node:~$
            </span>

            <input
                id="commandInput"
                type="text"
                autocomplete="off"
                spellcheck="false"
            >

        </div>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\templates\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">

        <button id="filesIcon">
            📁
        </button>

        <p>FILES</p>

    </div>


    <div class="desktop-icon">

        <button id="browserIcon">
            🌐
        </button>

        <p>BROWSER</p>

    </div>


    <div class="desktop-icon">

        <button id="messagesIcon">

            ✉

            <span
                id="messageBadge"
                class="notification-badge hidden"
            >
                1
            </span>

        </button>

        <p>MESSAGES</p>

    </div>


    <div class="desktop-icon">

        <button id="terminalDesktopIcon">
            ▣
        </button>

        <p>TERMINAL</p>

    </div>

</div>


================================================================================
FILE: backupp\story board wokring\templates\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <!-- STORY -->
    <div id="introScreen">
        <div id="introText"></div>
    </div>

    <!-- BOOT -->
    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>


================================================================================
FILE: backupp\story board wokring\templates\nova.html
================================================================================

<div id="novaAssistant">
    <div class="nova-header">
        <div class="nova-avatar">N</div>
        <div>
            <div class="nova-name">NOVA</div>
            <div class="nova-status">ONLINE</div>
        </div>
        <button id="novaMinimize">−</button>
    </div>
    <div id="novaBody">
        <div id="novaMessage">
            Hey. I'm NOVA. I'll help you learn how NEXUS works without solving the investigation for you.
        </div>
        <button id="novaHint">GIVE ME A HINT</button>
    </div>
</div>

================================================================================
FILE: backupp\story board wokring\templates\panel.html
================================================================================

<header class="panel">

    <button
        id="menuButton"
        class="menu-button"
    >
        NEXUS
    </button>

    <div class="panel-apps">

        <button id="terminalButton">
            TERMINAL
        </button>

        <button id="messagesButton">
            MESSAGES
        </button>

    </div>

    <div class="system-tray">

        <span class="network-status">
            ● ONLINE
        </span>

        <span id="clock">
            00:00
        </span>

    </div>

</header>


================================================================================
FILE: backupp\V 0.1\app.py
================================================================================

from flask import Flask, render_template, request, jsonify
from datetime import datetime

app = Flask(__name__)

game_state = {
    "message_unlocked": False,
    "system_log_found": False,
    "challenge01_complete": False
}

filesystem = {
    "/": {
        "type": "directory",
        "children": ["home", "etc", "logs", "missions"]
    },

    "/home": {
        "type": "directory",
        "children": ["nexus"]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "notes.txt",
            "readme.txt"
        ]
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Remember to check the network logs."
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to Nexus OS."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": (
            "interface=eth0\n"
            "address=192.168.1.24\n"
            "gateway=192.168.1.1"
        )
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/access.log": {
        "type": "file",
        "content": (
            "[03:02] 192.168.1.12 LOGIN SUCCESS\n"
            "[03:04] 192.168.1.18 LOGIN SUCCESS\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:12] 192.168.1.44 LOGIN SUCCESS"
        )
    },

    "/logs/system.log": {
        "type": "file",
        "content": (
            "[03:02] system started\n"
            "[03:04] network interface eth0 online\n"
            "[03:17] security service started"
        )
    },

    "/missions": {
        "type": "directory",
        "children": ["mission01"]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": ["brief.txt"]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": (
            "MISSION 01\n\n"
            "Something is wrong with the network.\n"
            "Investigate the access logs."
        )
    }
}


def normalize_path(path):
    if not path:
        return "/"

    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    result = "/" + "/".join(parts)

    return result if result else "/"


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help        Show available commands\n"
                "clear       Clear terminal\n"
                "pwd         Show current directory\n"
                "ls          List files\n"
                "cd          Change directory\n"
                "cat         Read a file\n"
                "whoami      Show current user\n"
                "hostname    Show system hostname\n"
                "echo        Print text\n"
                "date        Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"ls: cannot access '{target}': "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        target = "/"

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cd: no such file or directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": (
                    f"cd: not a directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(
            current_dir,
            args[0]
        )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "Is a directory"
                ),
                "cwd": current_dir
            }

        return {
            "output": item["content"],
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime(
                "%a %b %d %H:%M:%S %Y"
            ),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


@app.route("/")
def index():
    return render_template("index.html")


@app.route("/panel")
def panel():
    return render_template("panel.html")


@app.route("/desktop")
def desktop():
    return render_template("desktop.html")


@app.route("/apps/terminal")
def terminal():
    return render_template("apps/terminal.html")


@app.route("/apps/messages")
def messages():
    return render_template("apps/messages.html")


@app.route("/api/messages")
def get_messages():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "system_log_found": game_state["system_log_found"],
        "challenge01_complete": game_state["challenge01_complete"]
    })


@app.route("/api/terminal", methods=["POST"])
def terminal_command():
    data = request.get_json()

    command = data.get(
        "command",
        ""
    ).strip()

    current_dir = data.get(
        "cwd",
        "/home/nexus"
    )

    result = execute_command(
        command,
        current_dir
    )

    new_message = False
    message_stage = 0

    if command == "cat /logs/access.log":

        if not game_state["message_unlocked"]:
            game_state["message_unlocked"] = True
            new_message = True
            message_stage = 1

    elif command == "cat /logs/system.log":

        if not game_state["system_log_found"]:
            game_state["system_log_found"] = True
            new_message = True
            message_stage = 2

    result["new_message"] = new_message
    result["message_stage"] = message_stage
    result["challenge_complete"] = False

    return jsonify(result)


if __name__ == "__main__":
    app.run(debug=True)

================================================================================
FILE: backupp\V 0.1\challenges\__init__.py
================================================================================



================================================================================
FILE: backupp\V 0.1\challenges\challenge01.py
================================================================================

from system.state import game_state


TITLE = "NEXUS NODE 01"


INTRO = """UNKNOWN

NEXUS NODE 01.

Remember that name.

This machine wasn't supposed to be online.

Someone brought it back.

And someone connected to it.

Find out why."""


OBJECTIVE = """CHALLENGE 01

Something is wrong with the network.

Investigate the access logs.

Find the suspicious login activity."""


SUCCESS = """CHALLENGE 01 COMPLETE.

Someone was here.

And they didn't want to be noticed."""


MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:17",
    "text": (
        "You're looking in the right place.\n\n"
        "But you're not the first person to access this machine.\n\n"
        "Keep digging."
    )
}


def run_challenge01(command, target):
    if game_state["challenge01_complete"]:
        return {
            "complete": True,
            "notification": None
        }

    if (
        command == "cat /logs/access.log"
        and target == "/logs/access.log"
    ):
        game_state["challenge01_complete"] = True
        game_state["unread_messages"] += 1
        game_state["messages"].append(MESSAGE)

        return {
            "complete": True,
            "notification": {
                "title": "NEW MESSAGE",
                "text": "A new message has been received.",
                "sender": "UNKNOWN"
            }
        }

    return {
        "complete": False,
        "notification": None
    }

================================================================================
FILE: backupp\V 0.1\challenges\challenge02.py
================================================================================

TITLE = "THE SECOND TRACE"


INTRO = """UNKNOWN

You found the first trace.

It leads somewhere deeper.

The node is hiding more than old login attempts."""


OBJECTIVE = """CHALLENGE 02

Find the next piece of evidence."""


SUCCESS = """CHALLENGE 02 COMPLETE.

The trail continues."""


def run_challenge02(command, target):
    return {
        "complete": False,
        "notification": None
    }

================================================================================
FILE: backupp\V 0.1\challenges\challenge03.py
================================================================================

TITLE = "THE WATCHER"


INTRO = """UNKNOWN

The system knows you're here now.

Be careful."""


OBJECTIVE = """CHALLENGE 03

Investigate the next clue."""


SUCCESS = """CHALLENGE 03 COMPLETE."""


def run_challenge03(command, target):
    return {
        "complete": False,
        "notification": None
    }

================================================================================
FILE: backupp\V 0.1\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>

================================================================================
FILE: backupp\V 0.1\static\css\apps\messages.css
================================================================================

.messages-window {
    position: fixed;
    top: 100px;
    left: 50%;
    z-index: 500;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #101410;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.messages-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
}

.messages-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.messages-close {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
    background: #ff5f57;
}

.messages-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100% - 42px);
}

.message-list {
    overflow-y: auto;
    background: #0c100c;
    border-right: 2px solid #303830;
}

.message-item {
    position: relative;
    padding: 18px;
    border-bottom: 1px solid #292e29;
}

.message-item.active {
    background: #171d17;
}

.message-sender {
    margin-bottom: 5px;
    color: #b8ffb8;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.message-preview {
    max-width: 210px;
    overflow: hidden;
    color: #888;
    font-size: 19px;
    white-space: nowrap;
    text-overflow: ellipsis;
}

.message-unread {
    position: absolute;
    top: 17px;
    right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #b6ff72;
    color: #111;
    font-family: "Press Start 2P", monospace;
    font-size: 7px;
}

.message-view {
    min-width: 0;
    padding: 28px;
    overflow-y: auto;
}

.message-header {
    margin-bottom: 5px;
    color: #baffba;
    font-family: "Press Start 2P", monospace;
    font-size: 11px;
}

.message-time {
    margin-bottom: 25px;
    color: #666;
    font-size: 17px;
}

.message-body {
    max-width: 650px;
    color: #d1d1d1;
    font-size: 23px;
    line-height: 1.1;
    white-space: pre-wrap;
}

================================================================================
FILE: backupp\V 0.1\static\css\apps\terminal.css
================================================================================

.terminal-window {
    position: fixed;
    top: 90px;
    left: 50%;
    z-index: 500;
    display: flex;
    flex-direction: column;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #050805;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.terminal-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    min-height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
    cursor: grab;
}

.terminal-titlebar:active {
    cursor: grabbing;
}

.terminal-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.terminal-buttons {
    display: flex;
    align-items: center;
    gap: 8px;
}

.terminal-buttons button {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
}

.close-button {
    background: #ff5f57;
}

.maximize-button {
    background: #28c840;
}

.terminal-body {
    flex: 1;
    min-height: 0;
    padding: 18px 20px;
    overflow-y: auto;
    background: #050805;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
    line-height: 1.05;
    cursor: text;
}

.terminal-body::-webkit-scrollbar {
    width: 10px;
}

.terminal-body::-webkit-scrollbar-track {
    background: #0c100c;
}

.terminal-body::-webkit-scrollbar-thumb {
    background: #384438;
}

.terminal-body::-webkit-scrollbar-thumb:hover {
    background: #526052;
}

.terminal-line {
    min-height: 24px;
    white-space: pre-wrap;
    word-break: break-word;
}

.terminal-system {
    color: #71c871;
}

.terminal-prompt {
    color: #aaffaa;
}

.terminal-input-line {
    display: flex;
    align-items: baseline;
    width: 100%;
    min-height: 27px;
}

#commandInput {
    flex: 1;
    min-width: 0;
    margin-left: 8px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #9cff9c;
    caret-color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
}

#commandInput::selection {
    background: #496949;
    color: white;
}

================================================================================
FILE: backupp\V 0.1\static\css\desktop.css
================================================================================

#desktop {
    background:
        linear-gradient(
            rgba(4, 10, 5, 0.45),
            rgba(4, 10, 5, 0.45)
        ),
        url("/static/assets/wallpapers/nexus-bg.png");

    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
}

.desktop-icons {
    display: grid;
    grid-template-columns: repeat(4, 120px);
    gap: 24px;
    padding: 38px;
}

.desktop-icon {
    position: relative;
    width: 120px;
    text-align: center;
}

.desktop-icon button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 76px;
    height: 76px;
    margin: 0 auto 8px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #baffba;
    font-size: 42px;
}

.desktop-icon button:hover {
    border-color: #405240;
    background: rgba(120, 255, 120, 0.06);
}

.desktop-icon p {
    margin: 0;
    color: #c7c7c7;
    font-size: 19px;
    letter-spacing: 1px;
}

.notification-badge {
    position: absolute;
    top: -4px;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 21px;
    height: 21px;
    border-radius: 50%;
    background: #b6ff72;
    color: #101010;
    font-family: "Press Start 2P", monospace;
    font-size: 8px;
}

.hidden {
    display: none;
}

================================================================================
FILE: backupp\V 0.1\static\css\input.css
================================================================================

@import "tailwindcss";

================================================================================
FILE: backupp\V 0.1\static\css\panel.css
================================================================================

.panel {
    position: relative;
    z-index: 1000;
    display: flex;
    align-items: center;
    width: 100%;
    height: 48px;
    padding: 0 18px;
    background: #111;
    border-bottom: 2px solid #333;
    color: #d7ffd7;
}

.menu-button {
    padding: 5px 12px;
    border: 1px solid #555;
    background: #181818;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.menu-button:hover {
    background: #222;
    border-color: #8cff8c;
}

.panel-apps {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: 20px;
}

.panel-apps button {
    padding: 5px 9px;
    border: 1px solid transparent;
    background: transparent;
    color: #aaa;
    font-family: "VT323", monospace;
    font-size: 19px;
}

.panel-apps button:hover {
    color: #9cff9c;
    border-color: #444;
    background: #181818;
}

.system-tray {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-left: auto;
    color: #888;
    font-size: 19px;
}

.network-status {
    color: #75d675;
}

================================================================================
FILE: backupp\V 0.1\static\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

html,
body {
    width: 100%;
    height: 100%;
    margin: 0;
}

body {
    background: #080b08;
    color: #d7ffd7;
    font-family: "VT323", monospace;
    overflow: hidden;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}

#bootScreen {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    background: #020502;
    color: #8cff8c;
    font-size: 24px;
    line-height: 1.25;
    text-shadow: 0 0 8px rgba(80, 255, 80, 0.35);
}

#bootText {
    max-width: 900px;
}

#bootCursor {
    display: inline-block;
    margin-top: 8px;
    animation: blink 0.8s infinite;
}

.boot-hidden {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

#panel {
    position: relative;
    z-index: 1000;
}

#desktop {
    position: relative;
    width: 100vw;
    height: calc(100vh - 48px);
}

#notificationContainer {
    position: fixed;
    top: 62px;
    right: 22px;
    z-index: 5000;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}

.system-notification {
    display: flex;
    align-items: center;
    width: 350px;
    min-height: 92px;
    padding: 15px 18px;
    background: #171b17;
    border: 2px solid #5c705c;
    box-shadow: 6px 6px 0 #000;
    color: #d8ffd8;
    cursor: pointer;
    pointer-events: auto;
    animation: notification-in 0.25s ease-out;
}

.notification-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    min-width: 48px;
    height: 48px;
    margin-right: 14px;
    color: #b6ff72;
    font-size: 27px;
}

.notification-text {
    min-width: 0;
}

.notification-title {
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.notification-from {
    margin-top: 6px;
    color: #ddd;
    font-size: 21px;
}

.notification-preview {
    margin-top: 2px;
    color: #888;
    font-size: 18px;
}

.system-notification:hover {
    background: #202620;
    border-color: #8cff8c;
}

@keyframes notification-in {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}

================================================================================
FILE: backupp\V 0.1\static\js\boot.js
================================================================================

const bootMessages = [
    "NEXUS OS",
    "",
    "INITIALIZING SYSTEM...",
    "LOADING NODE 01...",
    "CHECKING NETWORK...",
    "NETWORK: ONLINE",
    "SECURITY SERVICE: ACTIVE",
    "",
    "SYSTEM READY."
];


async function runBootSequence() {
    const bootScreen =
        document.getElementById(
            "bootScreen"
        );


    const bootText =
        document.getElementById(
            "bootText"
        );


    if (!bootScreen || !bootText) {
        return;
    }


    for (const message of bootMessages) {

        bootText.innerHTML +=
            message + "<br>";


        await new Promise(
            resolve =>
                setTimeout(
                    resolve,
                    180
                )
        );
    }


    await new Promise(
        resolve =>
            setTimeout(
                resolve,
                700
            )
    );


    bootScreen.classList.add(
        "boot-hidden"
    );


    setTimeout(
        function() {
            bootScreen.remove();
        },
        900
    );
}


runBootSequence();

================================================================================
FILE: backupp\V 0.1\static\js\desktop.js
================================================================================

async function loadHTML(elementId, url) {
    const element = document.getElementById(elementId);

    if (!element) {
        console.error(`Missing element: #${elementId}`);
        return false;
    }

    try {
        const response = await fetch(url);

        if (!response.ok) {
            throw new Error(`${url} returned ${response.status}`);
        }

        element.innerHTML = await response.text();
        return true;

    } catch (error) {
        console.error(`Failed to load ${url}:`, error);
        return false;
    }
}


function updateClock() {
    const clock = document.getElementById("clock");

    if (!clock) {
        return;
    }

    const now = new Date();

    clock.textContent =
        `${String(now.getHours()).padStart(2, "0")}:` +
        `${String(now.getMinutes()).padStart(2, "0")}`;
}


function showNewMessageNotification(messageStage = 0) {
    const container =
        document.getElementById("notificationContainer");

    const badge =
        document.getElementById("messageBadge");

    if (badge) {
        badge.classList.remove("hidden");
    }

    if (!container) {
        return;
    }

    const oldNotification =
        document.querySelector(".system-notification");

    if (oldNotification) {
        oldNotification.remove();
    }

    let preview = "You have a new message.";

    if (messageStage === 0) {
        preview = "If you're reading this...";
    }

    if (messageStage === 1) {
        preview = "You found it.";
    }

    if (messageStage === 2) {
        preview = "Look at the network.";
    }

    const notification =
        document.createElement("div");

    notification.className =
        "system-notification";

    notification.innerHTML = `
        <div class="notification-icon">✉</div>
        <div class="notification-text">
            <div class="notification-title">
                NEW MESSAGE
            </div>
            <div class="notification-from">
                UNKNOWN
            </div>
            <div class="notification-preview">
                ${preview}
            </div>
        </div>
    `;

    notification.addEventListener(
        "click",
        function () {
            notification.remove();
            openMessages();
        }
    );

    container.appendChild(notification);

    const sound =
        document.getElementById("notificationSound");

    if (sound) {
        sound.currentTime = 0;

        sound.play().catch(() => {
            console.log(
                "Browser blocked notification sound until user interaction."
            );
        });
    }

    setTimeout(() => {
        if (notification.parentElement) {
            notification.remove();
        }
    }, 7000);
}


async function openTerminal() {
    console.log("Opening Terminal...");

    const existing =
        document.getElementById("terminalWindow");

    if (existing) {
        existing.style.zIndex = "2000";

        const input =
            document.getElementById("commandInput");

        if (input) {
            input.focus();
        }

        return;
    }

    try {
        const response =
            await fetch("/apps/terminal");

        if (!response.ok) {
            throw new Error(
                `Terminal returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const terminal =
            document.getElementById("terminalWindow");

        if (!terminal) {
            throw new Error(
                "terminalWindow was not created."
            );
        }

        if (typeof setupWindow === "function") {
            setupWindow(terminal);
        }

        if (typeof initializeTerminal === "function") {
            initializeTerminal();
        } else {
            console.error(
                "initializeTerminal is not available."
            );
        }

    } catch (error) {
        console.error(
            "Terminal failed to open:",
            error
        );
    }
}


async function openMessages() {
    console.log("Opening Messages...");

    const existing =
        document.getElementById("messagesWindow");

    if (existing) {
        existing.style.zIndex = "2000";
        return;
    }

    try {
        const response =
            await fetch("/apps/messages");

        if (!response.ok) {
            throw new Error(
                `Messages returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const messagesWindow =
            document.getElementById(
                "messagesWindow"
            );

        if (!messagesWindow) {
            throw new Error(
                "messagesWindow was not created."
            );
        }

        const closeButton =
            messagesWindow.querySelector(
                ".messages-close"
            );

        if (closeButton) {
            closeButton.addEventListener(
                "click",
                function () {
                    messagesWindow.remove();
                }
            );
        }

        const badge =
            document.getElementById(
                "messageBadge"
            );

        if (badge) {
            badge.classList.add("hidden");
        }

        const listBadge =
            document.getElementById(
                "messageListBadge"
            );

        if (listBadge) {
            listBadge.classList.add("hidden");
        }

        await updateMessages();

    } catch (error) {
        console.error(
            "Messages failed to open:",
            error
        );
    }
}


async function updateMessages() {
    try {
        const response =
            await fetch("/api/messages");

        if (!response.ok) {
            throw new Error(
                "Message API failed."
            );
        }

        const data =
            await response.json();

        const messageBody =
            document.querySelector(
                ".message-body"
            );

        const messagePreview =
            document.querySelector(
                ".message-preview"
            );

        if (!messageBody) {
            return;
        }

        if (data.system_log_found) {

            messageBody.textContent =
`Why are you looking at the system log?

The important part isn't what happened at 03:17.

It's what happened before it.

Look at the network.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Look at the network.";
            }

        } else if (data.message_unlocked) {

            messageBody.textContent =
`You found it.

I was hoping you wouldn't.

Look at the last successful login.

192.168.1.44

That address shouldn't exist on this network.

Someone used it to get into this machine.

Don't touch anything else yet.

Check:

/logs/system.log

Then come back here.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "You found it.";
            }

        } else {

            messageBody.textContent =
`If you're reading this, you've connected to Nexus Node 01.

This machine was shut down.

But something brought it back online.

I need you to find out what happened.

Start here:

/logs/access.log

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Start here.";
            }
        }

    } catch (error) {
        console.error(
            "Could not update messages:",
            error
        );
    }
}


document.addEventListener(
    "click",
    function (event) {

        if (
            event.target.closest("#terminalButton") ||
            event.target.closest("#terminalDesktopIcon")
        ) {
            openTerminal();
            return;
        }

        if (
            event.target.closest("#messagesButton") ||
            event.target.closest("#messagesIcon")
        ) {
            openMessages();
            return;
        }
    }
);


async function startDesktop() {
    const panelLoaded =
        await loadHTML(
            "panel",
            "/panel"
        );

    const desktopLoaded =
        await loadHTML(
            "desktop",
            "/desktop"
        );

    if (!panelLoaded || !desktopLoaded) {
        console.error(
            "Desktop failed to initialize."
        );

        return;
    }

    updateClock();

    /*
     * Initial story notification.
     * This is the first message the player receives.
     */
    setTimeout(
        function () {
            showNewMessageNotification(0);
        },
        1200
    );
}


startDesktop();

setInterval(
    updateClock,
    1000
);

================================================================================
FILE: backupp\V 0.1\static\js\terminal.js
================================================================================

function initializeTerminal() {
    const input =
        document.getElementById("commandInput");

    const output =
        document.getElementById("terminalOutput");

    const body =
        document.getElementById("terminalBody");

    const prompt =
        document.getElementById("terminalPrompt");

    if (!input || !output || !body || !prompt) {
        console.error(
            "Terminal initialization failed."
        );

        return;
    }

    let currentDirectory =
        "/home/nexus";

    function displayPath() {
        if (currentDirectory === "/home/nexus") {
            return "~";
        }

        return currentDirectory;
    }

    function updatePrompt() {
        prompt.textContent =
            `nexus@node:${displayPath()}$`;
    }

    function escapeHTML(text) {
        const element =
            document.createElement("div");

        element.textContent = text;

        return element.innerHTML;
    }

    function addCommand(command, result) {
        const commandLine =
            document.createElement("div");

        commandLine.className =
            "terminal-line";

        commandLine.innerHTML =
            `<span class="terminal-prompt">nexus@node:${displayPath()}$</span> ${escapeHTML(command)}`;

        output.appendChild(commandLine);

        if (result) {
            const resultLine =
                document.createElement("div");

            resultLine.className =
                "terminal-line";

            resultLine.textContent =
                result;

            output.appendChild(resultLine);
        }

        body.scrollTop =
            body.scrollHeight;
    }

    async function runCommand(command) {
        try {
            const response =
                await fetch(
                    "/api/terminal",
                    {
                        method: "POST",
                        headers: {
                            "Content-Type":
                                "application/json"
                        },
                        body: JSON.stringify({
                            command: command,
                            cwd: currentDirectory
                        })
                    }
                );

            if (!response.ok) {
                throw new Error(
                    "Terminal backend error."
                );
            }

            const data =
                await response.json();

            if (data.output === "__CLEAR__") {
                output.innerHTML = "";
            } else {
                addCommand(
                    command,
                    data.output
                );
            }

            currentDirectory =
                data.cwd;

            updatePrompt();

            if (data.new_message) {
                showNewMessageNotification(
                    data.message_stage
                );
            }

            input.focus();

        } catch (error) {
            console.error(error);

            addCommand(
                command,
                "NEXUS: connection to system failed."
            );

            input.focus();
        }
    }

    input.addEventListener(
        "keydown",
        function (event) {
            if (event.key !== "Enter") {
                return;
            }

            event.preventDefault();

            const command =
                input.value.trim();

            if (!command) {
                return;
            }

            input.value = "";

            runCommand(command);
        }
    );

    body.addEventListener(
        "click",
        function () {
            input.focus();
        }
    );

    updatePrompt();

    input.focus();
}


window.initializeTerminal =
    initializeTerminal;

================================================================================
FILE: backupp\V 0.1\static\js\window.js
================================================================================

function setupWindow(windowElement) {
    if (!windowElement) {
        return;
    }

    const titlebar =
        windowElement.querySelector(
            ".terminal-titlebar"
        );

    if (!titlebar) {
        return;
    }

    const closeButton =
        windowElement.querySelector(
            "#terminalClose"
        );

    const maximizeButton =
        windowElement.querySelector(
            "#terminalMaximize"
        );

    let dragging = false;
    let offsetX = 0;
    let offsetY = 0;

    titlebar.addEventListener(
        "mousedown",
        function (event) {
            if (
                event.target.closest(
                    "button"
                )
            ) {
                return;
            }

            dragging = true;

            const rect =
                windowElement.getBoundingClientRect();

            offsetX =
                event.clientX - rect.left;

            offsetY =
                event.clientY - rect.top;

            windowElement.style.transform =
                "none";
        }
    );

    document.addEventListener(
        "mousemove",
        function (event) {
            if (!dragging) {
                return;
            }

            let x =
                event.clientX - offsetX;

            let y =
                event.clientY - offsetY;

            const maxX =
                window.innerWidth -
                windowElement.offsetWidth;

            const maxY =
                window.innerHeight -
                windowElement.offsetHeight;

            x = Math.max(
                0,
                Math.min(x, maxX)
            );

            y = Math.max(
                48,
                Math.min(y, maxY)
            );

            windowElement.style.left =
                `${x}px`;

            windowElement.style.top =
                `${y}px`;
        }
    );

    document.addEventListener(
        "mouseup",
        function () {
            dragging = false;
        }
    );

    if (closeButton) {
        closeButton.addEventListener(
            "click",
            function () {
                windowElement.remove();
            }
        );
    }

    if (maximizeButton) {
        maximizeButton.addEventListener(
            "click",
            function () {
                const maximized =
                    windowElement.dataset
                        .maximized === "true";

                if (!maximized) {
                    windowElement.dataset.oldLeft =
                        windowElement.style.left;

                    windowElement.dataset.oldTop =
                        windowElement.style.top;

                    windowElement.dataset.oldWidth =
                        windowElement.style.width;

                    windowElement.dataset.oldHeight =
                        windowElement.style.height;

                    windowElement.style.left =
                        "0";

                    windowElement.style.top =
                        "48px";

                    windowElement.style.width =
                        "100vw";

                    windowElement.style.height =
                        "calc(100vh - 48px)";

                    windowElement.style.transform =
                        "none";

                    windowElement.dataset.maximized =
                        "true";

                } else {
                    windowElement.style.left =
                        windowElement.dataset.oldLeft;

                    windowElement.style.top =
                        windowElement.dataset.oldTop;

                    windowElement.style.width =
                        windowElement.dataset.oldWidth;

                    windowElement.style.height =
                        windowElement.dataset.oldHeight;

                    windowElement.dataset.maximized =
                        "false";
                }
            }
        );
    }
}


window.setupWindow =
    setupWindow;

================================================================================
FILE: backupp\V 0.1\system\commands.py
================================================================================

from datetime import datetime

from challenges.challenge01 import run_challenge01
from system.filesystem import FILESYSTEM


def normalize_path(path):
    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    if not parts:
        return "/"

    return "/" + "/".join(parts)


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help       Show available commands\n"
                "clear      Clear terminal\n"
                "pwd        Show current directory\n"
                "ls         List files\n"
                "cd         Change directory\n"
                "cat        Read a file\n"
                "whoami     Show current user\n"
                "hostname   Show system hostname\n"
                "echo       Print text\n"
                "date       Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y"),
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"ls: cannot access '{target}': No such file or directory",
                "cwd": current_dir
            }

        if item["type"] == "file":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        if not args:
            target = "/home/nexus"
        else:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cd: no such file or directory: {args[0]}",
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": f"cd: not a directory: {args[0]}",
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cat: {args[0]}: No such file or directory",
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": f"cat: {args[0]}: Is a directory",
                "cwd": current_dir
            }

        challenge_result = run_challenge01(
            command,
            target
        )

        return {
            "output": item["content"],
            "cwd": current_dir,
            "challenge_complete": challenge_result["complete"],
            "notification": challenge_result["notification"]
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }

================================================================================
FILE: backupp\V 0.1\system\filesystem.py
================================================================================

FILESYSTEM = {
    "/": {
        "type": "directory",
        "children": [
            "home",
            "etc",
            "logs",
            "missions"
        ]
    },

    "/home": {
        "type": "directory",
        "children": [
            "nexus"
        ]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "readme.txt",
            "notes.txt"
        ]
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to NEXUS OS."
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Check the system logs.\nSomething doesn't look right."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": "interface=eth0\naddress=192.168.1.24\ngateway=192.168.1.1"
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/system.log": {
        "type": "file",
        "content": "[03:02] system started\n[03:04] network interface eth0 online\n[03:17] security service started"
    },

    "/logs/access.log": {
        "type": "file",
        "content": "[03:02] 192.168.1.12 LOGIN SUCCESS\n[03:04] 192.168.1.18 LOGIN SUCCESS\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:12] 192.168.1.44 LOGIN SUCCESS"
    },

    "/missions": {
        "type": "directory",
        "children": [
            "mission01"
        ]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": [
            "brief.txt"
        ]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": "MISSION 01\n\nSomething is wrong with the network.\nInvestigate the access logs."
    }
}

================================================================================
FILE: backupp\V 0.1\system\state.py
================================================================================

game_state = {
    "current_challenge": 1,

    "challenge01_complete": False,
    "challenge02_complete": False,
    "challenge03_complete": False,

    "unread_messages": 0,

    "messages": []
}

================================================================================
FILE: backupp\V 0.1\templates\apps\messages.html
================================================================================

<div
    id="messagesWindow"
    class="messages-window"
>

    <div class="messages-titlebar">

        <button
            class="messages-close"
            title="Close"
        ></button>

        <h1>
            MESSAGES
        </h1>

    </div>


    <div class="messages-content">

        <aside class="message-list">

            <div class="message-item active">

                <div class="message-sender">
                    UNKNOWN
                </div>

                <div class="message-preview">
                    No new messages.
                </div>

                <span
                    id="messageListBadge"
                    class="message-unread"
                >
                    !
                </span>

            </div>

        </aside>


        <section class="message-view">

            <div class="message-header">
                UNKNOWN
            </div>

            <div class="message-time">
                JUST NOW
            </div>

            <div class="message-body">
                Loading...
            </div>

        </section>

    </div>

</div>

================================================================================
FILE: backupp\V 0.1\templates\apps\terminal.html
================================================================================

<div id="terminalWindow" class="terminal-window">

    <div id="terminalTitlebar" class="terminal-titlebar">

        <div class="terminal-buttons">

            <button
                id="terminalClose"
                class="close-button"
                title="Close"
            ></button>

            <button
                id="terminalMaximize"
                class="maximize-button"
                title="Maximize"
            ></button>

        </div>

        <h1>TERMINAL</h1>

    </div>

    <div id="terminalBody" class="terminal-body">

        <div id="terminalOutput">

            <div class="terminal-line terminal-system">
                NEXUS NODE 01
            </div>

            <div class="terminal-line terminal-system">
                SYSTEM READY.
            </div>

            <div class="terminal-line terminal-system">
                Type 'help' for available commands.
            </div>

        </div>

        <div class="terminal-input-line">

            <span
                id="terminalPrompt"
                class="terminal-prompt"
            >
                nexus@node:~$
            </span>

            <input
                id="commandInput"
                type="text"
                autocomplete="off"
                spellcheck="false"
            >

        </div>

    </div>

</div>

================================================================================
FILE: backupp\V 0.1\templates\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">

        <button id="filesIcon">
            📁
        </button>

        <p>FILES</p>

    </div>


    <div class="desktop-icon">

        <button id="browserIcon">
            🌐
        </button>

        <p>BROWSER</p>

    </div>


    <div class="desktop-icon">

        <button id="messagesIcon">

            ✉

            <span
                id="messageBadge"
                class="notification-badge hidden"
            >
                1
            </span>

        </button>

        <p>MESSAGES</p>

    </div>


    <div class="desktop-icon">

        <button id="terminalDesktopIcon">
            ▣
        </button>

        <p>TERMINAL</p>

    </div>

</div>

================================================================================
FILE: backupp\V 0.1\templates\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>

================================================================================
FILE: backupp\V 0.1\templates\panel.html
================================================================================

<header class="panel">

    <button
        id="menuButton"
        class="menu-button"
    >
        NEXUS
    </button>

    <div class="panel-apps">

        <button id="terminalButton">
            TERMINAL
        </button>

        <button id="messagesButton">
            MESSAGES
        </button>

    </div>

    <div class="system-tray">

        <span class="network-status">
            ● ONLINE
        </span>

        <span id="clock">
            00:00
        </span>

    </div>

</header>

================================================================================
FILE: challenges\__init__.py
================================================================================




================================================================================
FILE: challenges\challenge01.py
================================================================================

from system.state import game_state

TITLE = "NEXUS NODE 01"

INTRO = """UNKNOWN

If you're seeing this, then NEXUS came back online.

It wasn't supposed to.

The last shutdown was recorded.

The restart wasn't.

I don't know how long you've been connected,
so I'm going to keep this short.

Don't trust what the desktop tells you.

Something happened before you arrived.

There are traces of it all over this machine.

Logs.
Connections.
Old system files.

Start by looking around.

If you know how to use the terminal,
you already know where to begin.

If you don't...

That's okay.

Open it.

I'll tell you what you need to know when you need it.

— UNKNOWN"""

SECOND_MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:11",
    "text": """You found the part I was worried about.

192.168.1.44

Don't assume it's an intruder yet.

That's the mistake people make.

An unfamiliar address isn't proof of anything.

We need something to compare it with.

Find out what address belongs to NEXUS itself.

You won't find the answer in the access log.

Look through the system configuration.

Start with:

/etc

If you're not familiar with the terminal,
ask NOVA.

She'll keep you from getting lost.

— UNKNOWN"""
}

SUCCESS_MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:17",
    "text": """So.

Now you know.

192.168.1.44 wasn't NEXUS.

Someone else was connecting to this machine.

And they tried three times
before they got in.

That's the part that bothers me.

But there's something else.

I need you to find out
what happened at 03:17.

Don't open the system log yet.

There's another way in.

I'll contact you again.

— UNKNOWN"""
}

OBJECTIVE = """CHALLENGE 01

Find out who connected to NEXUS.

Start by investigating the access logs.

Then compare the suspicious address
with NEXUS's own network configuration."""

SUCCESS = """CHALLENGE 01 COMPLETE.

192.168.1.44 does not belong to NEXUS.

Someone else was connecting to this machine."""

def run_challenge01(command, target):
    if game_state["challenge01_complete"]:
        return {
            "complete": True,
            "notification": None
        }

    if (
        command == "cat /logs/access.log"
        and target == "/logs/access.log"
    ):
        if not game_state["access_log_found"]:
            game_state["access_log_found"] = True
            game_state["message_unlocked"] = True
            game_state["unread_messages"] += 1
            game_state["messages"].append(
                SECOND_MESSAGE
            )

            return {
                "complete": False,
                "notification": {
                    "title": "NEW MESSAGE",
                    "text": "UNKNOWN sent you another message.",
                    "sender": "UNKNOWN",
                    "stage": 1
                }
            }

    if (
        command == "cat /etc/network.conf"
        and target == "/etc/network.conf"
    ):
        if game_state["access_log_found"]:
            if not game_state["network_config_found"]:
                game_state["network_config_found"] = True
                game_state["challenge01_complete"] = True
                game_state["current_challenge"] = 2
                game_state["unread_messages"] += 1
                game_state["messages"].append(
                    SUCCESS_MESSAGE
                )

                return {
                    "complete": True,
                    "notification": {
                        "title": "CHALLENGE 01 COMPLETE",
                        "text": "UNKNOWN sent you another message.",
                        "sender": "UNKNOWN",
                        "stage": 2
                    }
                }

    return {
        "complete": game_state["challenge01_complete"],
        "notification": None
    }

================================================================================
FILE: challenges\challenge02.py
================================================================================

TITLE = "THE SECOND TRACE"


INTRO = """UNKNOWN

You found the first trace.

It leads somewhere deeper.

The node is hiding more than old login attempts."""


OBJECTIVE = """CHALLENGE 02

Find the next piece of evidence."""


SUCCESS = """CHALLENGE 02 COMPLETE.

The trail continues."""


def run_challenge02(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: challenges\challenge03.py
================================================================================

TITLE = "THE WATCHER"


INTRO = """UNKNOWN

The system knows you're here now.

Be careful."""


OBJECTIVE = """CHALLENGE 03

Investigate the next clue."""


SUCCESS = """CHALLENGE 03 COMPLETE."""


def run_challenge03(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: export_project.py
================================================================================


from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()



================================================================================
FILE: extract_projects.py
================================================================================

from pathlib import Path
import re

SOURCE_FILE = Path("project_code.txt")
OUTPUT_FOLDER = Path("extracted_project")


def extract_files():
    if not SOURCE_FILE.exists():
        print("ERROR: project_code.txt was not found.")
        return

    text = SOURCE_FILE.read_text(
        encoding="utf-8"
    )

    pattern = re.compile(
        r"^FILE:\s*(.+?)\s*$"
        r"\n^=+\s*$"
        r"\n(.*?)"
        r"(?=^=+\s*$\n^FILE:|\Z)",
        re.MULTILINE | re.DOTALL
    )

    matches = list(
        pattern.finditer(text)
    )

    if not matches:
        print("ERROR: No project files found.")
        return

    created = 0

    for match in matches:
        original_path = match.group(1).strip()
        content = match.group(2)

        original_path = original_path.replace(
            "\\",
            "/"
        )

        if original_path.startswith(
            "backupp/V 0.1/"
        ):
            relative_path = original_path[
                len("backupp/V 0.1/"):
            ]

        elif original_path.startswith(
            "V 0.1/"
        ):
            relative_path = original_path[
                len("V 0.1/"):
            ]

        elif original_path.startswith(
            "backupp/"
        ):
            relative_path = original_path[
                len("backupp/"):
            ]

        else:
            relative_path = original_path

        relative_path = relative_path.strip("/")

        if not relative_path:
            continue

        destination = (
            OUTPUT_FOLDER /
            relative_path
        )

        destination.parent.mkdir(
            parents=True,
            exist_ok=True
        )

        content = content.rstrip()

        destination.write_text(
            content + "\n",
            encoding="utf-8"
        )

        print(
            f"Created: {destination}"
        )

        created += 1

    print()
    print(
        f"Done. Created {created} files."
    )

    print(
        f"Project folder: "
        f"{OUTPUT_FOLDER.resolve()}"
    )


if __name__ == "__main__":
    extract_files()

================================================================================
FILE: extracted_project\app.py
================================================================================

from flask import Flask, render_template, request, jsonify
from datetime import datetime

app = Flask(__name__)

game_state = {
    "message_unlocked": False,
    "system_log_found": False,
    "challenge01_complete": False
}

filesystem = {
    "/": {
        "type": "directory",
        "children": ["home", "etc", "logs", "missions"]
    },

    "/home": {
        "type": "directory",
        "children": ["nexus"]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "notes.txt",
            "readme.txt"
        ]
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Remember to check the network logs."
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to Nexus OS."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": (
            "interface=eth0\n"
            "address=192.168.1.24\n"
            "gateway=192.168.1.1"
        )
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/access.log": {
        "type": "file",
        "content": (
            "[03:02] 192.168.1.12 LOGIN SUCCESS\n"
            "[03:04] 192.168.1.18 LOGIN SUCCESS\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:11] 192.168.1.44 LOGIN FAILED\n"
            "[03:12] 192.168.1.44 LOGIN SUCCESS"
        )
    },

    "/logs/system.log": {
        "type": "file",
        "content": (
            "[03:02] system started\n"
            "[03:04] network interface eth0 online\n"
            "[03:17] security service started"
        )
    },

    "/missions": {
        "type": "directory",
        "children": ["mission01"]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": ["brief.txt"]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": (
            "MISSION 01\n\n"
            "Something is wrong with the network.\n"
            "Investigate the access logs."
        )
    }
}


def normalize_path(path):
    if not path:
        return "/"

    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    result = "/" + "/".join(parts)

    return result if result else "/"


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help        Show available commands\n"
                "clear       Clear terminal\n"
                "pwd         Show current directory\n"
                "ls          List files\n"
                "cd          Change directory\n"
                "cat         Read a file\n"
                "whoami      Show current user\n"
                "hostname    Show system hostname\n"
                "echo        Print text\n"
                "date        Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"ls: cannot access '{target}': "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        target = "/"

        if args:
            target = resolve_path(
                current_dir,
                args[0]
            )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cd: no such file or directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": (
                    f"cd: not a directory: "
                    f"{args[0]}"
                ),
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(
            current_dir,
            args[0]
        )

        item = filesystem.get(target)

        if not item:
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "No such file or directory"
                ),
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": (
                    f"cat: {args[0]}: "
                    "Is a directory"
                ),
                "cwd": current_dir
            }

        return {
            "output": item["content"],
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime(
                "%a %b %d %H:%M:%S %Y"
            ),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


@app.route("/")
def index():
    return render_template("index.html")


@app.route("/panel")
def panel():
    return render_template("panel.html")


@app.route("/desktop")
def desktop():
    return render_template("desktop.html")


@app.route("/apps/terminal")
def terminal():
    return render_template("apps/terminal.html")


@app.route("/apps/messages")
def messages():
    return render_template("apps/messages.html")


@app.route("/api/messages")
def get_messages():
    return jsonify({
        "message_unlocked": game_state["message_unlocked"],
        "system_log_found": game_state["system_log_found"],
        "challenge01_complete": game_state["challenge01_complete"]
    })


@app.route("/api/terminal", methods=["POST"])
def terminal_command():
    data = request.get_json()

    command = data.get(
        "command",
        ""
    ).strip()

    current_dir = data.get(
        "cwd",
        "/home/nexus"
    )

    result = execute_command(
        command,
        current_dir
    )

    new_message = False
    message_stage = 0

    if command == "cat /logs/access.log":

        if not game_state["message_unlocked"]:
            game_state["message_unlocked"] = True
            new_message = True
            message_stage = 1

    elif command == "cat /logs/system.log":

        if not game_state["system_log_found"]:
            game_state["system_log_found"] = True
            new_message = True
            message_stage = 2

    result["new_message"] = new_message
    result["message_stage"] = message_stage
    result["challenge_complete"] = False

    return jsonify(result)


if __name__ == "__main__":
    app.run(debug=True)


================================================================================
FILE: extracted_project\backupp\export_project.py
================================================================================

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\css\desktop.css
================================================================================

#desktop {
    height: calc(100vh - 40px);

    background: #d7d7d7;

    position: relative;
}

.desktop-icons {
    display: flex;
    gap: 25px;
    flex-direction: column;
    padding: 20px;
}

.desktop-icon {
    width: 70px;
    display: flex;
    flex-direction: column;
    align-items: center;

    user-select: none;
}

.desktop-icon button {
    background: none;
    border: none;

    font-size: 35px;
    cursor: pointer;
}

.desktop-icon p {
    margin: 4px 0 0;

    font-size: 14px;
}

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\css\panel.css
================================================================================

.panel {
    height: 40px;
    background: #333;
    color: white;

    display: flex;
    align-items: center;

    padding: 0 20px;
}

.menu-button {
    height: 30px;
    padding: 0 15px;

    background: #444;
    color: white;

    border: none;
}

.panel-apps {
    display: flex;
    margin-left: 10px;
}

.panel-apps button {
    height: 30px;

    padding: 0 12px;

    background: transparent;
    color: white;

    border: none;
}

.panel-apps button:hover {
    background: #555;
}

.system-tray {
    margin-left: auto;

    display: flex;
    gap: 15px;
    align-items: center;
}

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
}

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\html\apps\terminal.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-700 flex items-center justify-center h-[1000px] flex-col">
    <div class="w-[700px] h-[450px] bg-black border rounded-xl overflow-hidden">
        <div class="h-8 border-b flex items-center gap-2 bg-gray-400">

            <button class="w-3 h-3 rounded-full bg-[#ff5f57] flex items-center justify-center text-[9px]">×</button>
            <button class="h-3 w-3 rounded-full bg-[#febc2e] flex items-center justify-center text-[9px]">×</button>
            <button class="w-3 h-3 rounded-full bg-[#28c840] flex items-center justify-center text-[9px]">×</button>
            <h1 class="absolute left-1/2 -translate-x-1/2 flex items-center justify-center">Terminal</h1>
        </div>
        <div id="terminalOutput" class=" font-mono text-green-500 pl-3">
        </div>
        <div class="flex gap-2 ">
            <p class=" text-green-500 font-mono pl-3">user@nexus:~$</p>
            <input id="commandInput" class="bg-black text-green-500 caret-green-500 outline-none border-none" type="text">
        </div>
    </div>
</body>
    <script src="terminal.js"></script>

</html>


================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\html\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">
        <button>📁</button>
        <p>Files</p>
    </div>

    <div class="desktop-icon">
        <button>🌐</button>
        <p>Browser</p>
    </div>

</div>

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\html\panel.html
================================================================================

<div class="panel">

    <button id="menuButton" class="menu-button">
        Menu
    </button>

    <div class="panel-apps">

        <button>
            Terminal
        </button>

        <button>
            Files
        </button>

        <button>
            Browser
        </button>

    </div>

    <div class="system-tray">

        <span>WiFi</span>
        <span>🔊</span>
        <span id="clock">00:00</span>

    </div>

</div>

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Nexus OS</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/desktop.css">
    <link rel="stylesheet" href="css/panel.css">
</head>

<body>

    <div id="panel"></div>

    <main id="desktop"></main>

    <script src="js/desktop.js"></script>

</body>

</html>

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\js\apps\terminal.js
================================================================================

const output = document.getElementById("terminalOutput");
const input = document.getElementById("commandInput");

input.addEventListener("keydown", async function (event) {

    if (event.key == "Enter") {

        const response = await fetch("/command", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                command: input.value
            })
        });
        if (output.innerHTML === "") {
            output.innerHTML = "user@nexus:~$ " + input.value;
        } else {
            output.innerHTML += "<br>user@nexus:~$ " + input.value;
        }

        const data = await response.json();
        if(data.clear) {
            output.innerHTML = ""
            input.value = "";
            return;
        }
        if (data.commands) {

            data.commands.forEach(command => {

                output.innerHTML += `
                    <br>
                    <span class="text-green-400">${command.usage}</span>
                    - ${command.description}
                `;

            });

            input.value = "";
            return;
        }
        console.log(data);
        output.innerHTML += "<br>" + data.output;
        input.value = "";

    }

});

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\js\desktop.js
================================================================================

async function loadHTML(element, file) {

    const response = await fetch(file);

    const html = await response.text();

    document.getElementById(element).innerHTML = html;
}


async function startDesktop() {

    await loadHTML("panel", "html/panel.html");

    await loadHTML("desktop", "html/desktop.html");

}


function updateClock() {

    const clock = document.getElementById("clock");

    if (!clock) return;

    const now = new Date();

    const hours = now.getHours().toString().padStart(2, "0");
    const minutes = now.getMinutes().toString().padStart(2, "0");

    clock.textContent = `${hours}:${minutes}`;
}


startDesktop();

setInterval(updateClock, 1000);

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\python\system\main.py
================================================================================

from flask import Flask, request, jsonify, send_from_directory
import os
from datetime import datetime

app = Flask(__name__)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

current_path = "/home/nexus"


@app.route("/")
def index():
    return send_from_directory(BASE_DIR, "index.html")


@app.route("/terminal.js")
def terminal_js():
    return send_from_directory(BASE_DIR, "terminal.js")


@app.route("/command", methods=["POST"])
def command():

    global current_path

    data = request.get_json()

    cmd = data.get("command", "")

    command = cmd.split()

    if not command:
        return jsonify({
            "output": ""
        })

    name = command[0]
    arguments = command[1:]

    # clear
    if name == "clear":

        if len(arguments) > 0:
            return jsonify({
                "output": "clear: too many arguments"
            })

        return jsonify({
            "output": "",
            "clear": True
        })

    # help
    elif name == "help":

        if len(arguments) > 0:
            return jsonify({
                "output": "help: too many arguments"
            })

        return jsonify({
            "commands": [
                {
                    "name": "clear",
                    "usage": "clear",
                    "description": "Clear the terminal screen"
                },
                {
                    "name": "help",
                    "usage": "help",
                    "description": "Show available commands"
                },
                {
                    "name": "pwd",
                    "usage": "pwd",
                    "description": "Print the current working directory"
                },
                {
                    "name": "cd",
                    "usage": "cd <directory>",
                    "description": "Change the current directory"
                },
                {
                    "name": "date",
                    "usage": "date",
                    "description": "Display the current date and time"
                },
                {
                    "name": "whoami",
                    "usage": "whoami",
                    "description": "Display the current user"
                }
            ]
        })

    # pwd
    elif name == "pwd":

        if len(arguments) > 0:
            return jsonify({
                "output": "pwd: too many arguments"
            })

        return jsonify({
            "output": current_path
        })

    # date
    elif name == "date":

        if len(arguments) > 0:
            return jsonify({
                "output": "date: too many arguments"
            })

        return jsonify({
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y")
        })

    # cd
    elif name == "cd":

        if len(arguments) == 0:
            return jsonify({
                "output": "cd: missing argument"
            })

        if len(arguments) > 1:
            return jsonify({
                "output": "cd: too many arguments"
            })

        path = arguments[0]

        if path.startswith("/"):
            current_path = path
        else:
            current_path = current_path.rstrip("/") + "/" + path

        return jsonify({
            "output": current_path
        })

    # whoami
    elif name == "whoami":

        if len(arguments) > 0:
            return jsonify({
                "output": "whoami: too many arguments"
            })

        return jsonify({
            "output": "nexus"
        })

    # unknown command
    else:

        return jsonify({
            "output": f"{name}: command not found"
        })


if __name__ == "__main__":
    app.run(
        host="127.0.0.1",
        port=2026,
        debug=False
    )

================================================================================


================================================================================
FILE: extracted_project\backupp\FIRST\static\css\input.css
================================================================================

@import "tailwindcss";

================================================================================


================================================================================
FILE: extracted_project\challenges\__init__.py
================================================================================




================================================================================
FILE: extracted_project\challenges\challenge01.py
================================================================================

from system.state import game_state


TITLE = "NEXUS NODE 01"


INTRO = """UNKNOWN

NEXUS NODE 01.

Remember that name.

This machine wasn't supposed to be online.

Someone brought it back.

And someone connected to it.

Find out why."""


OBJECTIVE = """CHALLENGE 01

Something is wrong with the network.

Investigate the access logs.

Find the suspicious login activity."""


SUCCESS = """CHALLENGE 01 COMPLETE.

Someone was here.

And they didn't want to be noticed."""


MESSAGE = {
    "sender": "UNKNOWN",
    "time": "03:17",
    "text": (
        "You're looking in the right place.\n\n"
        "But you're not the first person to access this machine.\n\n"
        "Keep digging."
    )
}


def run_challenge01(command, target):
    if game_state["challenge01_complete"]:
        return {
            "complete": True,
            "notification": None
        }

    if (
        command == "cat /logs/access.log"
        and target == "/logs/access.log"
    ):
        game_state["challenge01_complete"] = True
        game_state["unread_messages"] += 1
        game_state["messages"].append(MESSAGE)

        return {
            "complete": True,
            "notification": {
                "title": "NEW MESSAGE",
                "text": "A new message has been received.",
                "sender": "UNKNOWN"
            }
        }

    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: extracted_project\challenges\challenge02.py
================================================================================

TITLE = "THE SECOND TRACE"


INTRO = """UNKNOWN

You found the first trace.

It leads somewhere deeper.

The node is hiding more than old login attempts."""


OBJECTIVE = """CHALLENGE 02

Find the next piece of evidence."""


SUCCESS = """CHALLENGE 02 COMPLETE.

The trail continues."""


def run_challenge02(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: extracted_project\challenges\challenge03.py
================================================================================

TITLE = "THE WATCHER"


INTRO = """UNKNOWN

The system knows you're here now.

Be careful."""


OBJECTIVE = """CHALLENGE 03

Investigate the next clue."""


SUCCESS = """CHALLENGE 03 COMPLETE."""


def run_challenge03(command, target):
    return {
        "complete": False,
        "notification": None
    }


================================================================================
FILE: extracted_project\export_project.py
================================================================================

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_FILE = PROJECT_ROOT / "project_code.txt"

EXTENSIONS = {
    ".html",
    ".css",
    ".js",
    ".py"
}

EXCLUDED_DIRS = {
    ".git",
    "__pycache__",
    "venv",
    ".venv",
    "node_modules"
}


def should_include(path):
    if path.suffix.lower() not in EXTENSIONS:
        return False

    for part in path.parts:
        if part in EXCLUDED_DIRS:
            return False

    return True


def main():
    files = []

    for path in PROJECT_ROOT.rglob("*"):
        if path.is_file() and should_include(path):
            files.append(path)

    files.sort()

    with open(OUTPUT_FILE, "w", encoding="utf-8") as output:
        output.write("NEXUS OS PROJECT FILES\n")
        output.write("=" * 80 + "\n\n")

        for path in files:
            relative_path = path.relative_to(PROJECT_ROOT)

            output.write("\n")
            output.write("=" * 80 + "\n")
            output.write(f"FILE: {relative_path}\n")
            output.write("=" * 80 + "\n\n")

            try:
                content = path.read_text(
                    encoding="utf-8",
                    errors="replace"
                )

                output.write(content)
                output.write("\n")

            except Exception as error:
                output.write(
                    f"[Could not read file: {error}]\n"
                )

    print(f"Exported {len(files)} files.")
    print(f"Saved to: {OUTPUT_FILE}")


if __name__ == "__main__":
    main()


================================================================================
FILE: extracted_project\FIRST\css\desktop.css
================================================================================

#desktop {
    height: calc(100vh - 40px);

    background: #d7d7d7;

    position: relative;
}

.desktop-icons {
    display: flex;
    gap: 25px;
    flex-direction: column;
    padding: 20px;
}

.desktop-icon {
    width: 70px;
    display: flex;
    flex-direction: column;
    align-items: center;

    user-select: none;
}

.desktop-icon button {
    background: none;
    border: none;

    font-size: 35px;
    cursor: pointer;
}

.desktop-icon p {
    margin: 4px 0 0;

    font-size: 14px;
}


================================================================================
FILE: extracted_project\FIRST\css\panel.css
================================================================================

.panel {
    height: 40px;
    background: #333;
    color: white;

    display: flex;
    align-items: center;

    padding: 0 20px;
}

.menu-button {
    height: 30px;
    padding: 0 15px;

    background: #444;
    color: white;

    border: none;
}

.panel-apps {
    display: flex;
    margin-left: 10px;
}

.panel-apps button {
    height: 30px;

    padding: 0 12px;

    background: transparent;
    color: white;

    border: none;
}

.panel-apps button:hover {
    background: #555;
}

.system-tray {
    margin-left: auto;

    display: flex;
    gap: 15px;
    align-items: center;
}


================================================================================
FILE: extracted_project\FIRST\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    height: 100vh;
    overflow: hidden;
}


================================================================================
FILE: extracted_project\FIRST\html\apps\terminal.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-700 flex items-center justify-center h-[1000px] flex-col">
    <div class="w-[700px] h-[450px] bg-black border rounded-xl overflow-hidden">
        <div class="h-8 border-b flex items-center gap-2 bg-gray-400">

            <button class="w-3 h-3 rounded-full bg-[#ff5f57] flex items-center justify-center text-[9px]">×</button>
            <button class="h-3 w-3 rounded-full bg-[#febc2e] flex items-center justify-center text-[9px]">×</button>
            <button class="w-3 h-3 rounded-full bg-[#28c840] flex items-center justify-center text-[9px]">×</button>
            <h1 class="absolute left-1/2 -translate-x-1/2 flex items-center justify-center">Terminal</h1>
        </div>
        <div id="terminalOutput" class=" font-mono text-green-500 pl-3">
        </div>
        <div class="flex gap-2 ">
            <p class=" text-green-500 font-mono pl-3">user@nexus:~$</p>
            <input id="commandInput" class="bg-black text-green-500 caret-green-500 outline-none border-none" type="text">
        </div>
    </div>
</body>
    <script src="terminal.js"></script>

</html>


================================================================================
FILE: extracted_project\FIRST\html\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">
        <button>📁</button>
        <p>Files</p>
    </div>

    <div class="desktop-icon">
        <button>🌐</button>
        <p>Browser</p>
    </div>

</div>


================================================================================
FILE: extracted_project\FIRST\html\panel.html
================================================================================

<div class="panel">

    <button id="menuButton" class="menu-button">
        Menu
    </button>

    <div class="panel-apps">

        <button>
            Terminal
        </button>

        <button>
            Files
        </button>

        <button>
            Browser
        </button>

    </div>

    <div class="system-tray">

        <span>WiFi</span>
        <span>🔊</span>
        <span id="clock">00:00</span>

    </div>

</div>


================================================================================
FILE: extracted_project\FIRST\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Nexus OS</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/desktop.css">
    <link rel="stylesheet" href="css/panel.css">
</head>

<body>

    <div id="panel"></div>

    <main id="desktop"></main>

    <script src="js/desktop.js"></script>

</body>

</html>


================================================================================
FILE: extracted_project\FIRST\js\apps\terminal.js
================================================================================

const output = document.getElementById("terminalOutput");
const input = document.getElementById("commandInput");

input.addEventListener("keydown", async function (event) {

    if (event.key == "Enter") {

        const response = await fetch("/command", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                command: input.value
            })
        });
        if (output.innerHTML === "") {
            output.innerHTML = "user@nexus:~$ " + input.value;
        } else {
            output.innerHTML += "<br>user@nexus:~$ " + input.value;
        }

        const data = await response.json();
        if(data.clear) {
            output.innerHTML = ""
            input.value = "";
            return;
        }
        if (data.commands) {

            data.commands.forEach(command => {

                output.innerHTML += `
                    <br>
                    <span class="text-green-400">${command.usage}</span>
                    - ${command.description}
                `;

            });

            input.value = "";
            return;
        }
        console.log(data);
        output.innerHTML += "<br>" + data.output;
        input.value = "";

    }

});


================================================================================
FILE: extracted_project\FIRST\js\desktop.js
================================================================================

async function loadHTML(element, file) {

    const response = await fetch(file);

    const html = await response.text();

    document.getElementById(element).innerHTML = html;
}


async function startDesktop() {

    await loadHTML("panel", "html/panel.html");

    await loadHTML("desktop", "html/desktop.html");

}


function updateClock() {

    const clock = document.getElementById("clock");

    if (!clock) return;

    const now = new Date();

    const hours = now.getHours().toString().padStart(2, "0");
    const minutes = now.getMinutes().toString().padStart(2, "0");

    clock.textContent = `${hours}:${minutes}`;
}


startDesktop();

setInterval(updateClock, 1000);


================================================================================
FILE: extracted_project\FIRST\python\system\main.py
================================================================================

from flask import Flask, request, jsonify, send_from_directory
import os
from datetime import datetime

app = Flask(__name__)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))

current_path = "/home/nexus"


@app.route("/")
def index():
    return send_from_directory(BASE_DIR, "index.html")


@app.route("/terminal.js")
def terminal_js():
    return send_from_directory(BASE_DIR, "terminal.js")


@app.route("/command", methods=["POST"])
def command():

    global current_path

    data = request.get_json()

    cmd = data.get("command", "")

    command = cmd.split()

    if not command:
        return jsonify({
            "output": ""
        })

    name = command[0]
    arguments = command[1:]

    # clear
    if name == "clear":

        if len(arguments) > 0:
            return jsonify({
                "output": "clear: too many arguments"
            })

        return jsonify({
            "output": "",
            "clear": True
        })

    # help
    elif name == "help":

        if len(arguments) > 0:
            return jsonify({
                "output": "help: too many arguments"
            })

        return jsonify({
            "commands": [
                {
                    "name": "clear",
                    "usage": "clear",
                    "description": "Clear the terminal screen"
                },
                {
                    "name": "help",
                    "usage": "help",
                    "description": "Show available commands"
                },
                {
                    "name": "pwd",
                    "usage": "pwd",
                    "description": "Print the current working directory"
                },
                {
                    "name": "cd",
                    "usage": "cd <directory>",
                    "description": "Change the current directory"
                },
                {
                    "name": "date",
                    "usage": "date",
                    "description": "Display the current date and time"
                },
                {
                    "name": "whoami",
                    "usage": "whoami",
                    "description": "Display the current user"
                }
            ]
        })

    # pwd
    elif name == "pwd":

        if len(arguments) > 0:
            return jsonify({
                "output": "pwd: too many arguments"
            })

        return jsonify({
            "output": current_path
        })

    # date
    elif name == "date":

        if len(arguments) > 0:
            return jsonify({
                "output": "date: too many arguments"
            })

        return jsonify({
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y")
        })

    # cd
    elif name == "cd":

        if len(arguments) == 0:
            return jsonify({
                "output": "cd: missing argument"
            })

        if len(arguments) > 1:
            return jsonify({
                "output": "cd: too many arguments"
            })

        path = arguments[0]

        if path.startswith("/"):
            current_path = path
        else:
            current_path = current_path.rstrip("/") + "/" + path

        return jsonify({
            "output": current_path
        })

    # whoami
    elif name == "whoami":

        if len(arguments) > 0:
            return jsonify({
                "output": "whoami: too many arguments"
            })

        return jsonify({
            "output": "nexus"
        })

    # unknown command
    else:

        return jsonify({
            "output": f"{name}: command not found"
        })


if __name__ == "__main__":
    app.run(
        host="127.0.0.1",
        port=2026,
        debug=False
    )


================================================================================
FILE: extracted_project\FIRST\static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: extracted_project\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>


================================================================================
FILE: extracted_project\static\css\apps\messages.css
================================================================================

.messages-window {
    position: fixed;
    top: 100px;
    left: 50%;
    z-index: 500;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #101410;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.messages-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
}

.messages-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.messages-close {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
    background: #ff5f57;
}

.messages-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100% - 42px);
}

.message-list {
    overflow-y: auto;
    background: #0c100c;
    border-right: 2px solid #303830;
}

.message-item {
    position: relative;
    padding: 18px;
    border-bottom: 1px solid #292e29;
}

.message-item.active {
    background: #171d17;
}

.message-sender {
    margin-bottom: 5px;
    color: #b8ffb8;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.message-preview {
    max-width: 210px;
    overflow: hidden;
    color: #888;
    font-size: 19px;
    white-space: nowrap;
    text-overflow: ellipsis;
}

.message-unread {
    position: absolute;
    top: 17px;
    right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #b6ff72;
    color: #111;
    font-family: "Press Start 2P", monospace;
    font-size: 7px;
}

.message-view {
    min-width: 0;
    padding: 28px;
    overflow-y: auto;
}

.message-header {
    margin-bottom: 5px;
    color: #baffba;
    font-family: "Press Start 2P", monospace;
    font-size: 11px;
}

.message-time {
    margin-bottom: 25px;
    color: #666;
    font-size: 17px;
}

.message-body {
    max-width: 650px;
    color: #d1d1d1;
    font-size: 23px;
    line-height: 1.1;
    white-space: pre-wrap;
}


================================================================================
FILE: extracted_project\static\css\apps\terminal.css
================================================================================

.terminal-window {
    position: fixed;
    top: 90px;
    left: 50%;
    z-index: 500;
    display: flex;
    flex-direction: column;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #050805;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.terminal-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    min-height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
    cursor: grab;
}

.terminal-titlebar:active {
    cursor: grabbing;
}

.terminal-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.terminal-buttons {
    display: flex;
    align-items: center;
    gap: 8px;
}

.terminal-buttons button {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
}

.close-button {
    background: #ff5f57;
}

.maximize-button {
    background: #28c840;
}

.terminal-body {
    flex: 1;
    min-height: 0;
    padding: 18px 20px;
    overflow-y: auto;
    background: #050805;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
    line-height: 1.05;
    cursor: text;
}

.terminal-body::-webkit-scrollbar {
    width: 10px;
}

.terminal-body::-webkit-scrollbar-track {
    background: #0c100c;
}

.terminal-body::-webkit-scrollbar-thumb {
    background: #384438;
}

.terminal-body::-webkit-scrollbar-thumb:hover {
    background: #526052;
}

.terminal-line {
    min-height: 24px;
    white-space: pre-wrap;
    word-break: break-word;
}

.terminal-system {
    color: #71c871;
}

.terminal-prompt {
    color: #aaffaa;
}

.terminal-input-line {
    display: flex;
    align-items: baseline;
    width: 100%;
    min-height: 27px;
}

#commandInput {
    flex: 1;
    min-width: 0;
    margin-left: 8px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #9cff9c;
    caret-color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
}

#commandInput::selection {
    background: #496949;
    color: white;
}


================================================================================
FILE: extracted_project\static\css\desktop.css
================================================================================

#desktop {
    background:
        linear-gradient(
            rgba(4, 10, 5, 0.45),
            rgba(4, 10, 5, 0.45)
        ),
        url("/static/assets/wallpapers/nexus-bg.png");

    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
}

.desktop-icons {
    display: grid;
    grid-template-columns: repeat(4, 120px);
    gap: 24px;
    padding: 38px;
}

.desktop-icon {
    position: relative;
    width: 120px;
    text-align: center;
}

.desktop-icon button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 76px;
    height: 76px;
    margin: 0 auto 8px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #baffba;
    font-size: 42px;
}

.desktop-icon button:hover {
    border-color: #405240;
    background: rgba(120, 255, 120, 0.06);
}

.desktop-icon p {
    margin: 0;
    color: #c7c7c7;
    font-size: 19px;
    letter-spacing: 1px;
}

.notification-badge {
    position: absolute;
    top: -4px;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 21px;
    height: 21px;
    border-radius: 50%;
    background: #b6ff72;
    color: #101010;
    font-family: "Press Start 2P", monospace;
    font-size: 8px;
}

.hidden {
    display: none;
}


================================================================================
FILE: extracted_project\static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: extracted_project\static\css\panel.css
================================================================================

.panel {
    position: relative;
    z-index: 1000;
    display: flex;
    align-items: center;
    width: 100%;
    height: 48px;
    padding: 0 18px;
    background: #111;
    border-bottom: 2px solid #333;
    color: #d7ffd7;
}

.menu-button {
    padding: 5px 12px;
    border: 1px solid #555;
    background: #181818;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.menu-button:hover {
    background: #222;
    border-color: #8cff8c;
}

.panel-apps {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: 20px;
}

.panel-apps button {
    padding: 5px 9px;
    border: 1px solid transparent;
    background: transparent;
    color: #aaa;
    font-family: "VT323", monospace;
    font-size: 19px;
}

.panel-apps button:hover {
    color: #9cff9c;
    border-color: #444;
    background: #181818;
}

.system-tray {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-left: auto;
    color: #888;
    font-size: 19px;
}

.network-status {
    color: #75d675;
}


================================================================================
FILE: extracted_project\static\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

html,
body {
    width: 100%;
    height: 100%;
    margin: 0;
}

body {
    background: #080b08;
    color: #d7ffd7;
    font-family: "VT323", monospace;
    overflow: hidden;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}

#bootScreen {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    background: #020502;
    color: #8cff8c;
    font-size: 24px;
    line-height: 1.25;
    text-shadow: 0 0 8px rgba(80, 255, 80, 0.35);
}

#bootText {
    max-width: 900px;
}

#bootCursor {
    display: inline-block;
    margin-top: 8px;
    animation: blink 0.8s infinite;
}

.boot-hidden {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

#panel {
    position: relative;
    z-index: 1000;
}

#desktop {
    position: relative;
    width: 100vw;
    height: calc(100vh - 48px);
}

#notificationContainer {
    position: fixed;
    top: 62px;
    right: 22px;
    z-index: 5000;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}

.system-notification {
    display: flex;
    align-items: center;
    width: 350px;
    min-height: 92px;
    padding: 15px 18px;
    background: #171b17;
    border: 2px solid #5c705c;
    box-shadow: 6px 6px 0 #000;
    color: #d8ffd8;
    cursor: pointer;
    pointer-events: auto;
    animation: notification-in 0.25s ease-out;
}

.notification-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    min-width: 48px;
    height: 48px;
    margin-right: 14px;
    color: #b6ff72;
    font-size: 27px;
}

.notification-text {
    min-width: 0;
}

.notification-title {
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.notification-from {
    margin-top: 6px;
    color: #ddd;
    font-size: 21px;
}

.notification-preview {
    margin-top: 2px;
    color: #888;
    font-size: 18px;
}

.system-notification:hover {
    background: #202620;
    border-color: #8cff8c;
}

@keyframes notification-in {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}


================================================================================
FILE: extracted_project\static\js\boot.js
================================================================================

const bootMessages = [
    "NEXUS OS",
    "",
    "INITIALIZING SYSTEM...",
    "LOADING NODE 01...",
    "CHECKING NETWORK...",
    "NETWORK: ONLINE",
    "SECURITY SERVICE: ACTIVE",
    "",
    "SYSTEM READY."
];


async function runBootSequence() {
    const bootScreen =
        document.getElementById(
            "bootScreen"
        );


    const bootText =
        document.getElementById(
            "bootText"
        );


    if (!bootScreen || !bootText) {
        return;
    }


    for (const message of bootMessages) {

        bootText.innerHTML +=
            message + "<br>";


        await new Promise(
            resolve =>
                setTimeout(
                    resolve,
                    180
                )
        );
    }


    await new Promise(
        resolve =>
            setTimeout(
                resolve,
                700
            )
    );


    bootScreen.classList.add(
        "boot-hidden"
    );


    setTimeout(
        function() {
            bootScreen.remove();
        },
        900
    );
}


runBootSequence();


================================================================================
FILE: extracted_project\static\js\desktop.js
================================================================================

async function loadHTML(elementId, url) {
    const element =
        document.getElementById(elementId);

    if (!element) {
        console.error(`Missing element: #${elementId}`);
        return false;
    }

    try {

        const response =
            await fetch(url);

        if (!response.ok) {
            throw new Error(
                `${url} returned ${response.status}`
            );
        }

        element.innerHTML =
            await response.text();

        return true;

    } catch (error) {

        console.error(
            `Failed to load ${url}:`,
            error
        );

        return false;
    }
}


function updateClock() {

    const clock =
        document.getElementById("clock");

    if (!clock) {
        return;
    }

    const now =
        new Date();

    clock.textContent =
        `${String(now.getHours()).padStart(2, "0")}:` +
        `${String(now.getMinutes()).padStart(2, "0")}`;
}


function showNewMessageNotification(messageStage = 0) {

    const container =
        document.getElementById(
            "notificationContainer"
        );

    const badge =
        document.getElementById(
            "messageBadge"
        );

    if (badge) {
        badge.classList.remove("hidden");
    }

    if (!container) {
        return;
    }

    const oldNotification =
        document.querySelector(
            ".system-notification"
        );

    if (oldNotification) {
        oldNotification.remove();
    }

    let preview =
        "You have a new message.";

    if (messageStage === 0) {
        preview =
            "If you're reading this...";
    }

    if (messageStage === 1) {
        preview =
            "You found it.";
    }

    if (messageStage === 2) {
        preview =
            "Look at the network.";
    }

    const notification =
        document.createElement("div");

    notification.className =
        "system-notification";

    notification.innerHTML = `
        <div class="notification-icon">✉</div>

        <div class="notification-text">

            <div class="notification-title">
                NEW MESSAGE
            </div>

            <div class="notification-from">
                UNKNOWN
            </div>

            <div class="notification-preview">
                ${preview}
            </div>

        </div>
    `;

    notification.addEventListener(
        "click",
        function () {

            notification.remove();

            openMessages();

        }
    );

    container.appendChild(
        notification
    );

    const sound =
        document.getElementById(
            "notificationSound"
        );

    if (sound) {

        sound.currentTime = 0;

        sound.play().catch(() => {});

    }

    setTimeout(
        () => {

            if (notification.parentElement) {
                notification.remove();
            }

        },
        7000
    );
}


async function openTerminal() {

    const existing =
        document.getElementById(
            "terminalWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        const input =
            document.getElementById(
                "commandInput"
            );

        if (input) {
            input.focus();
        }

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/terminal"
            );

        if (!response.ok) {
            throw new Error(
                `Terminal returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const terminal =
            document.getElementById(
                "terminalWindow"
            );

        if (!terminal) {
            throw new Error(
                "terminalWindow was not created."
            );
        }

        setupWindow(terminal);

        initializeTerminal();

    } catch (error) {

        console.error(
            "Terminal failed to open:",
            error
        );
    }
}


async function openMessages() {

    const existing =
        document.getElementById(
            "messagesWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/messages"
            );

        if (!response.ok) {
            throw new Error(
                `Messages returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const messagesWindow =
            document.getElementById(
                "messagesWindow"
            );

        if (!messagesWindow) {
            throw new Error(
                "messagesWindow was not created."
            );
        }

        setupWindow(
            messagesWindow
        );

        await updateMessages();

        const badge =
            document.getElementById(
                "messageBadge"
            );

        if (badge) {
            badge.classList.add("hidden");
        }

        const listBadge =
            document.getElementById(
                "messageListBadge"
            );

        if (listBadge) {
            listBadge.classList.add("hidden");
        }

    } catch (error) {

        console.error(
            "Messages failed to open:",
            error
        );
    }
}


async function openFiles() {

    const existing =
        document.getElementById(
            "filesWindow"
        );

    if (existing) {

        existing.style.zIndex = "3000";

        return;
    }

    try {

        const response =
            await fetch(
                "/apps/files"
            );

        if (!response.ok) {
            throw new Error(
                `Files returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const filesWindow =
            document.getElementById(
                "filesWindow"
            );

        if (!filesWindow) {
            throw new Error(
                "filesWindow was not created."
            );
        }

        setupWindow(
            filesWindow
        );

        if (
            typeof initializeFiles ===
            "function"
        ) {
            initializeFiles();
        }

    } catch (error) {

        console.error(
            "Files failed to open:",
            error
        );
    }
}


async function updateMessages() {

    try {

        const response =
            await fetch(
                "/api/messages"
            );

        if (!response.ok) {
            throw new Error(
                "Message API failed."
            );
        }

        const data =
            await response.json();

        const messageBody =
            document.querySelector(
                ".message-body"
            );

        const messagePreview =
            document.querySelector(
                ".message-preview"
            );

        if (!messageBody) {
            return;
        }

        if (data.system_log_found) {

            messageBody.textContent =
`Why are you looking at the system log?

The important part isn't what happened at 03:17.

It's what happened before it.

Look at the network.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Look at the network.";
            }

        } else if (data.message_unlocked) {

            messageBody.textContent =
`You found it.

I was hoping you wouldn't.

Look at the last successful login.

192.168.1.44

That address shouldn't exist on this network.

Someone used it to get into this machine.

Don't touch anything else yet.

Check:

/logs/system.log

Then come back here.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "You found it.";
            }

        } else {

            messageBody.textContent =
`If you're reading this, you've connected to Nexus Node 01.

This machine was shut down.

But something brought it back online.

I need you to find out what happened.

Start here:

/logs/access.log

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Start here.";
            }
        }

    } catch (error) {

        console.error(
            "Could not update messages:",
            error
        );
    }
}


document.addEventListener(
    "click",
    function (event) {

        if (
            event.target.closest(
                "#terminalButton"
            ) ||
            event.target.closest(
                "#terminalDesktopIcon"
            )
        ) {

            openTerminal();

            return;
        }

        if (
            event.target.closest(
                "#messagesButton"
            ) ||
            event.target.closest(
                "#messagesIcon"
            )
        ) {

            openMessages();

            return;
        }

        if (
            event.target.closest(
                "#filesIcon"
            )
        ) {

            openFiles();

            return;
        }
    }
);


async function startDesktop() {

    const panelLoaded =
        await loadHTML(
            "panel",
            "/panel"
        );

    const desktopLoaded =
        await loadHTML(
            "desktop",
            "/desktop"
        );

    if (!panelLoaded || !desktopLoaded) {

        console.error(
            "Desktop failed to initialize."
        );

        return;
    }

    updateClock();

    setTimeout(
        function () {
            showNewMessageNotification(0);
        },
        1200
    );
}


startDesktop();


setInterval(
    updateClock,
    1000
);


================================================================================
FILE: extracted_project\static\js\terminal.js
================================================================================

function initializeTerminal() {
    const input =
        document.getElementById("commandInput");

    const output =
        document.getElementById("terminalOutput");

    const body =
        document.getElementById("terminalBody");

    const prompt =
        document.getElementById("terminalPrompt");

    if (!input || !output || !body || !prompt) {
        console.error(
            "Terminal initialization failed."
        );

        return;
    }

    let currentDirectory =
        "/home/nexus";

    function displayPath() {
        if (currentDirectory === "/home/nexus") {
            return "~";
        }

        return currentDirectory;
    }

    function updatePrompt() {
        prompt.textContent =
            `nexus@node:${displayPath()}$`;
    }

    function escapeHTML(text) {
        const element =
            document.createElement("div");

        element.textContent = text;

        return element.innerHTML;
    }

    function addCommand(command, result) {
        const commandLine =
            document.createElement("div");

        commandLine.className =
            "terminal-line";

        commandLine.innerHTML =
            `<span class="terminal-prompt">nexus@node:${displayPath()}$</span> ${escapeHTML(command)}`;

        output.appendChild(commandLine);

        if (result) {
            const resultLine =
                document.createElement("div");

            resultLine.className =
                "terminal-line";

            resultLine.textContent =
                result;

            output.appendChild(resultLine);
        }

        body.scrollTop =
            body.scrollHeight;
    }

    async function runCommand(command) {
        try {
            const response =
                await fetch(
                    "/api/terminal",
                    {
                        method: "POST",
                        headers: {
                            "Content-Type":
                                "application/json"
                        },
                        body: JSON.stringify({
                            command: command,
                            cwd: currentDirectory
                        })
                    }
                );

            if (!response.ok) {
                throw new Error(
                    "Terminal backend error."
                );
            }

            const data =
                await response.json();

            if (data.output === "__CLEAR__") {
                output.innerHTML = "";
            } else {
                addCommand(
                    command,
                    data.output
                );
            }

            currentDirectory =
                data.cwd;

            updatePrompt();

            if (data.new_message) {
                showNewMessageNotification(
                    data.message_stage
                );
            }

            input.focus();

        } catch (error) {
            console.error(error);

            addCommand(
                command,
                "NEXUS: connection to system failed."
            );

            input.focus();
        }
    }

    input.addEventListener(
        "keydown",
        function (event) {
            if (event.key !== "Enter") {
                return;
            }

            event.preventDefault();

            const command =
                input.value.trim();

            if (!command) {
                return;
            }

            input.value = "";

            runCommand(command);
        }
    );

    body.addEventListener(
        "click",
        function () {
            input.focus();
        }
    );

    updatePrompt();

    input.focus();
}


window.initializeTerminal =
    initializeTerminal;


================================================================================
FILE: extracted_project\static\js\window.js
================================================================================

function setupWindow(windowElement) {
    if (!windowElement) {
        return;
    }

    const titlebar =
        windowElement.querySelector(
            ".terminal-titlebar, .messages-titlebar, .files-titlebar"
        );

    if (!titlebar) {
        console.error("Window titlebar not found.");
        return;
    }

    let dragging = false;
    let offsetX = 0;
    let offsetY = 0;

    titlebar.addEventListener(
        "mousedown",
        function (event) {

            if (event.target.closest("button, input")) {
                return;
            }

            if (windowElement.dataset.maximized === "true") {
                return;
            }

            dragging = true;

            const rect =
                windowElement.getBoundingClientRect();

            offsetX =
                event.clientX - rect.left;

            offsetY =
                event.clientY - rect.top;

            windowElement.style.transform = "none";

            windowElement.style.left =
                `${rect.left}px`;

            windowElement.style.top =
                `${rect.top}px`;

            windowElement.style.zIndex = "3000";

            document.body.style.userSelect = "none";
        }
    );

    document.addEventListener(
        "mousemove",
        function (event) {

            if (!dragging) {
                return;
            }

            let x =
                event.clientX - offsetX;

            let y =
                event.clientY - offsetY;

            const panel =
                document.getElementById("panel");

            const panelHeight =
                panel
                    ? panel.offsetHeight
                    : 48;

            const maxX =
                window.innerWidth -
                windowElement.offsetWidth;

            const maxY =
                window.innerHeight -
                windowElement.offsetHeight;

            x = Math.max(
                0,
                Math.min(x, maxX)
            );

            y = Math.max(
                panelHeight,
                Math.min(y, maxY)
            );

            windowElement.style.left =
                `${x}px`;

            windowElement.style.top =
                `${y}px`;
        }
    );

    document.addEventListener(
        "mouseup",
        function () {

            if (!dragging) {
                return;
            }

            dragging = false;

            document.body.style.userSelect = "";
        }
    );

    const closeButton =
        windowElement.querySelector(
            "#terminalClose, .messages-close, #filesClose"
        );

    if (closeButton) {

        closeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                windowElement.remove();

            }
        );
    }

    const maximizeButton =
        windowElement.querySelector(
            "#terminalMaximize"
        );

    if (maximizeButton) {

        maximizeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                const maximized =
                    windowElement.dataset.maximized === "true";

                if (!maximized) {

                    const rect =
                        windowElement.getBoundingClientRect();

                    windowElement.dataset.oldLeft =
                        `${rect.left}px`;

                    windowElement.dataset.oldTop =
                        `${rect.top}px`;

                    windowElement.dataset.oldWidth =
                        `${rect.width}px`;

                    windowElement.dataset.oldHeight =
                        `${rect.height}px`;

                    windowElement.style.transform =
                        "none";

                    windowElement.style.left =
                        "0px";

                    windowElement.style.top =
                        "48px";

                    windowElement.style.width =
                        "100vw";

                    windowElement.style.height =
                        "calc(100vh - 48px)";

                    windowElement.dataset.maximized =
                        "true";

                } else {

                    windowElement.style.left =
                        windowElement.dataset.oldLeft;

                    windowElement.style.top =
                        windowElement.dataset.oldTop;

                    windowElement.style.width =
                        windowElement.dataset.oldWidth;

                    windowElement.style.height =
                        windowElement.dataset.oldHeight;

                    windowElement.dataset.maximized =
                        "false";
                }

            }
        );
    }

    windowElement.addEventListener(
        "mousedown",
        function () {

            const currentZ =
                parseInt(
                    windowElement.style.zIndex || "500",
                    10
                );

            if (currentZ < 3000) {
                windowElement.style.zIndex = "3000";
            }

        }
    );
}


window.setupWindow = setupWindow;


================================================================================
FILE: extracted_project\system\commands.py
================================================================================

from datetime import datetime

from challenges.challenge01 import run_challenge01
from system.filesystem import FILESYSTEM


def normalize_path(path):
    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    if not parts:
        return "/"

    return "/" + "/".join(parts)


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help       Show available commands\n"
                "clear      Clear terminal\n"
                "pwd        Show current directory\n"
                "ls         List files\n"
                "cd         Change directory\n"
                "cat        Read a file\n"
                "whoami     Show current user\n"
                "hostname   Show system hostname\n"
                "echo       Print text\n"
                "date       Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y"),
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"ls: cannot access '{target}': No such file or directory",
                "cwd": current_dir
            }

        if item["type"] == "file":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        if not args:
            target = "/home/nexus"
        else:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cd: no such file or directory: {args[0]}",
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": f"cd: not a directory: {args[0]}",
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cat: {args[0]}: No such file or directory",
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": f"cat: {args[0]}: Is a directory",
                "cwd": current_dir
            }

        challenge_result = run_challenge01(
            command,
            target
        )

        return {
            "output": item["content"],
            "cwd": current_dir,
            "challenge_complete": challenge_result["complete"],
            "notification": challenge_result["notification"]
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


================================================================================
FILE: extracted_project\system\filesystem.py
================================================================================

FILESYSTEM = {
    "/": {
        "type": "directory",
        "children": [
            "home",
            "etc",
            "logs",
            "missions"
        ]
    },

    "/home": {
        "type": "directory",
        "children": [
            "nexus"
        ]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "readme.txt",
            "notes.txt"
        ]
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to NEXUS OS."
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Check the system logs.\nSomething doesn't look right."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": "interface=eth0\naddress=192.168.1.24\ngateway=192.168.1.1"
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/system.log": {
        "type": "file",
        "content": "[03:02] system started\n[03:04] network interface eth0 online\n[03:17] security service started"
    },

    "/logs/access.log": {
        "type": "file",
        "content": "[03:02] 192.168.1.12 LOGIN SUCCESS\n[03:04] 192.168.1.18 LOGIN SUCCESS\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:12] 192.168.1.44 LOGIN SUCCESS"
    },

    "/missions": {
        "type": "directory",
        "children": [
            "mission01"
        ]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": [
            "brief.txt"
        ]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": "MISSION 01\n\nSomething is wrong with the network.\nInvestigate the access logs."
    }
}


================================================================================
FILE: extracted_project\system\state.py
================================================================================

game_state = {
    "current_challenge": 1,

    "challenge01_complete": False,
    "challenge02_complete": False,
    "challenge03_complete": False,

    "unread_messages": 0,

    "messages": []
}


================================================================================
FILE: extracted_project\templates\apps\messages.html
================================================================================

<div
    id="messagesWindow"
    class="messages-window"
>

    <div class="messages-titlebar">

        <button
            class="messages-close"
            title="Close"
        ></button>

        <h1>
            MESSAGES
        </h1>

    </div>


    <div class="messages-content">

        <aside class="message-list">

            <div class="message-item active">

                <div class="message-sender">
                    UNKNOWN
                </div>

                <div class="message-preview">
                    No new messages.
                </div>

                <span
                    id="messageListBadge"
                    class="message-unread"
                >
                    !
                </span>

            </div>

        </aside>


        <section class="message-view">

            <div class="message-header">
                UNKNOWN
            </div>

            <div class="message-time">
                JUST NOW
            </div>

            <div class="message-body">
                Loading...
            </div>

        </section>

    </div>

</div>


================================================================================
FILE: extracted_project\templates\apps\terminal.html
================================================================================

<div id="terminalWindow" class="terminal-window">

    <div id="terminalTitlebar" class="terminal-titlebar">

        <div class="terminal-buttons">

            <button
                id="terminalClose"
                class="close-button"
                title="Close"
            ></button>

            <button
                id="terminalMaximize"
                class="maximize-button"
                title="Maximize"
            ></button>

        </div>

        <h1>TERMINAL</h1>

    </div>

    <div id="terminalBody" class="terminal-body">

        <div id="terminalOutput">

            <div class="terminal-line terminal-system">
                NEXUS NODE 01
            </div>

            <div class="terminal-line terminal-system">
                SYSTEM READY.
            </div>

            <div class="terminal-line terminal-system">
                Type 'help' for available commands.
            </div>

        </div>

        <div class="terminal-input-line">

            <span
                id="terminalPrompt"
                class="terminal-prompt"
            >
                nexus@node:~$
            </span>

            <input
                id="commandInput"
                type="text"
                autocomplete="off"
                spellcheck="false"
            >

        </div>

    </div>

</div>


================================================================================
FILE: extracted_project\templates\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">

        <button id="filesIcon">
            📁
        </button>

        <p>FILES</p>

    </div>


    <div class="desktop-icon">

        <button id="browserIcon">
            🌐
        </button>

        <p>BROWSER</p>

    </div>


    <div class="desktop-icon">

        <button id="messagesIcon">

            ✉

            <span
                id="messageBadge"
                class="notification-badge hidden"
            >
                1
            </span>

        </button>

        <p>MESSAGES</p>

    </div>


    <div class="desktop-icon">

        <button id="terminalDesktopIcon">
            ▣
        </button>

        <p>TERMINAL</p>

    </div>

</div>


================================================================================
FILE: extracted_project\templates\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>

</body>

</html>


================================================================================
FILE: extracted_project\templates\panel.html
================================================================================

<header class="panel">

    <button
        id="menuButton"
        class="menu-button"
    >
        NEXUS
    </button>

    <div class="panel-apps">

        <button id="terminalButton">
            TERMINAL
        </button>

        <button id="messagesButton">
            MESSAGES
        </button>

    </div>

    <div class="system-tray">

        <span class="network-status">
            ● ONLINE
        </span>

        <span id="clock">
            00:00
        </span>

    </div>

</header>


================================================================================
FILE: index.html
================================================================================

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>NEXUS NODE 01</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap" rel="stylesheet">

<link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/panel.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/desktop.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/apps/terminal.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/apps/messages.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/nova.css') }}">
</head>

<body>

<div id="introScreen" class="intro-screen">
<div id="introText" class="intro-text"></div>
</div>

<div id="bootScreen" class="boot-screen">
<div id="bootText" class="boot-text"></div>
</div>

<div id="panel" class="panel-container"></div>

<div id="desktop" class="desktop-container"></div>

<div id="notificationContainer" class="notification-container"></div>

<audio id="notificationSound" preload="auto">
<source src="{{ url_for('static', filename='audio/notification.wav') }}" type="audio/wav">
</audio>

<script src="{{ url_for('static', filename='js/window.js') }}"></script>
<script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
<script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
<script src="{{ url_for('static', filename='js/boot.js') }}"></script>
<script src="{{ url_for('static', filename='js/nova.js') }}"></script>

</body>
</html>

================================================================================
FILE: static\css\apps\messages.css
================================================================================

.messages-window {
    position: fixed;
    top: 100px;
    left: 50%;
    z-index: 500;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #101410;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.messages-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
}

.messages-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.messages-close {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
    background: #ff5f57;
}

.messages-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100% - 42px);
}

.message-list {
    overflow-y: auto;
    background: #0c100c;
    border-right: 2px solid #303830;
}

.message-item {
    position: relative;
    padding: 18px;
    border-bottom: 1px solid #292e29;
}

.message-item.active {
    background: #171d17;
}

.message-sender {
    margin-bottom: 5px;
    color: #b8ffb8;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.message-preview {
    max-width: 210px;
    overflow: hidden;
    color: #888;
    font-size: 19px;
    white-space: nowrap;
    text-overflow: ellipsis;
}

.message-unread {
    position: absolute;
    top: 17px;
    right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #b6ff72;
    color: #111;
    font-family: "Press Start 2P", monospace;
    font-size: 7px;
}

.message-view {
    min-width: 0;
    padding: 28px;
    overflow-y: auto;
}

.message-header {
    margin-bottom: 5px;
    color: #baffba;
    font-family: "Press Start 2P", monospace;
    font-size: 11px;
}

.message-time {
    margin-bottom: 25px;
    color: #666;
    font-size: 17px;
}

.message-body {
    max-width: 650px;
    color: #d1d1d1;
    font-size: 23px;
    line-height: 1.1;
    white-space: pre-wrap;
}


================================================================================
FILE: static\css\apps\terminal.css
================================================================================

.terminal-window {
    position: fixed;
    top: 90px;
    left: 50%;
    z-index: 500;
    display: flex;
    flex-direction: column;
    width: min(900px, 90vw);
    height: min(600px, 78vh);
    transform: translateX(-50%);
    overflow: hidden;
    background: #050805;
    border: 2px solid #465346;
    box-shadow: 8px 8px 0 #000;
}

.terminal-titlebar {
    position: relative;
    display: flex;
    align-items: center;
    height: 42px;
    min-height: 42px;
    padding: 0 14px;
    background: #171b17;
    border-bottom: 2px solid #394139;
    cursor: grab;
}

.terminal-titlebar:active {
    cursor: grabbing;
}

.terminal-titlebar h1 {
    position: absolute;
    left: 50%;
    margin: 0;
    transform: translateX(-50%);
    color: #aaa;
    font-family: "Press Start 2P", monospace;
    font-size: 10px;
    white-space: nowrap;
}

.terminal-buttons {
    display: flex;
    align-items: center;
    gap: 8px;
}

.terminal-buttons button {
    width: 14px;
    height: 14px;
    padding: 0;
    border: 1px solid #777;
    border-radius: 50%;
}

.close-button {
    background: #ff5f57;
}

.maximize-button {
    background: #28c840;
}

.terminal-body {
    flex: 1;
    min-height: 0;
    padding: 18px 20px;
    overflow-y: auto;
    background: #050805;
    color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
    line-height: 1.05;
    cursor: text;
}

.terminal-body::-webkit-scrollbar {
    width: 10px;
}

.terminal-body::-webkit-scrollbar-track {
    background: #0c100c;
}

.terminal-body::-webkit-scrollbar-thumb {
    background: #384438;
}

.terminal-body::-webkit-scrollbar-thumb:hover {
    background: #526052;
}

.terminal-line {
    min-height: 24px;
    white-space: pre-wrap;
    word-break: break-word;
}

.terminal-system {
    color: #71c871;
}

.terminal-prompt {
    color: #aaffaa;
}

.terminal-input-line {
    display: flex;
    align-items: baseline;
    width: 100%;
    min-height: 27px;
}

#commandInput {
    flex: 1;
    min-width: 0;
    margin-left: 8px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #9cff9c;
    caret-color: #9cff9c;
    font-family: "VT323", monospace;
    font-size: 23px;
}

#commandInput::selection {
    background: #496949;
    color: white;
}


================================================================================
FILE: static\css\desktop.css
================================================================================

#desktop {
    background:
        linear-gradient(
            rgba(4, 10, 5, 0.45),
            rgba(4, 10, 5, 0.45)
        ),
        url("/static/assets/wallpapers/nexus-bg.png");

    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
}

.desktop-icons {
    display: grid;
    grid-template-columns: repeat(4, 120px);
    gap: 24px;
    padding: 38px;
}

.desktop-icon {
    position: relative;
    width: 120px;
    text-align: center;
}

.desktop-icon button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 76px;
    height: 76px;
    margin: 0 auto 8px;
    padding: 0;
    border: 1px solid transparent;
    background: transparent;
    color: #baffba;
    font-size: 42px;
}

.desktop-icon button:hover {
    border-color: #405240;
    background: rgba(120, 255, 120, 0.06);
}

.desktop-icon p {
    margin: 0;
    color: #c7c7c7;
    font-size: 19px;
    letter-spacing: 1px;
}

.notification-badge {
    position: absolute;
    top: -4px;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 21px;
    height: 21px;
    border-radius: 50%;
    background: #b6ff72;
    color: #101010;
    font-family: "Press Start 2P", monospace;
    font-size: 8px;
}

.hidden {
    display: none;
}


================================================================================
FILE: static\css\input.css
================================================================================

@import "tailwindcss";


================================================================================
FILE: static\css\nova.css
================================================================================

#novaMessageBox {
position: fixed;
right: 28px;
bottom: 70px;
width: 300px;
padding: 12px 14px;
background: rgba(8, 12, 9, 0.94);
border-left: 2px solid #7cff7c;
box-shadow: 0 4px 18px rgba(0, 0, 0, 0.45);
z-index: 99999;
font-family: "VT323", monospace;
opacity: 0;
transform: translateY(12px);
transition: opacity 0.35s ease, transform 0.35s ease;
pointer-events: none;
}

#novaMessageBox.nova-show {
opacity: 1;
transform: translateY(0);
}

#novaName {
color: #b6ff72;
font-family: "Press Start 2P", monospace;
font-size: 8px;
margin-bottom: 8px;
}

#novaText {
color: #d8ded8;
font-size: 19px;
line-height: 1.15;
white-space: pre-line;
}

================================================================================
FILE: static\css\panel.css
================================================================================

.panel {
    position: relative;
    z-index: 1000;
    display: flex;
    align-items: center;
    width: 100%;
    height: 48px;
    padding: 0 18px;
    background: #111;
    border-bottom: 2px solid #333;
    color: #d7ffd7;
}

.menu-button {
    padding: 5px 12px;
    border: 1px solid #555;
    background: #181818;
    color: #9cff9c;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.menu-button:hover {
    background: #222;
    border-color: #8cff8c;
}

.panel-apps {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: 20px;
}

.panel-apps button {
    padding: 5px 9px;
    border: 1px solid transparent;
    background: transparent;
    color: #aaa;
    font-family: "VT323", monospace;
    font-size: 19px;
}

.panel-apps button:hover {
    color: #9cff9c;
    border-color: #444;
    background: #181818;
}

.system-tray {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-left: auto;
    color: #888;
    font-size: 19px;
}

.network-status {
    color: #75d675;
}


================================================================================
FILE: static\css\style.css
================================================================================

* {
    box-sizing: border-box;
}

html,
body {
    width: 100%;
    height: 100%;
    margin: 0;
}

body {
    background: #080b08;
    color: #d7ffd7;
    font-family: "VT323", monospace;
    overflow: hidden;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}
/* =========================================================
   CINEMATIC INTRO
   ========================================================= */

#introScreen {
    position: fixed;
    inset: 0;
    z-index: 10000;

    display: flex;
    align-items: center;
    justify-content: center;

    background: #000000;
    color: #e8e8e8;

    padding: 40px;

    opacity: 1;

    transition:
        opacity 1.8s ease;
}


#introText {
    width: min(850px, 80vw);

    text-align: center;

    font-family: "VT323", monospace;

    font-size: clamp(
        24px,
        3vw,
        38px
    );

    line-height: 1.5;

    letter-spacing: 1px;

    color: #dedede;

    text-shadow:
        0 0 12px rgba(
            255,
            255,
            255,
            0.08
        );
}


.intro-line {
    opacity: 0;

    transform:
        translateY(8px);

    filter: blur(2px);

    transition:
        opacity 1.6s ease,
        transform 1.6s ease,
        filter 1.6s ease;
}


.intro-line.visible {
    opacity: 1;

    transform:
        translateY(0);

    filter: blur(0);
}


.intro-line.fade {
    opacity: 0;

    transition:
        opacity 1.2s ease;
}


#introScreen.intro-hidden {
    opacity: 0;

    pointer-events: none;
}

#bootScreen {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    background: #020502;
    color: #8cff8c;
    font-size: 24px;
    line-height: 1.25;
    text-shadow: 0 0 8px rgba(80, 255, 80, 0.35);
}

#bootText {
    max-width: 900px;
}

.boot-line {
    min-height: 1.25em;
    margin-bottom: 4px;
    white-space: pre-wrap;
}

.boot-title {
    margin-bottom: 20px;
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 32px;
    letter-spacing: 2px;
    text-shadow:
        0 0 8px rgba(80, 255, 80, 0.45),
        0 0 18px rgba(80, 255, 80, 0.2);
}

.boot-story {
    color: #a8d8a8;
}

.boot-system {
    color: #75d675;
    font-family: "VT323", monospace;
}

.boot-warning {
    color: #d8ff9b;
    text-shadow:
        0 0 8px rgba(180, 255, 100, 0.35);
}

.boot-node {
    margin: 12px 0;
    color: #ffffff;
    font-family: "Press Start 2P", monospace;
    font-size: 18px;
    letter-spacing: 1px;
    text-shadow:
        0 0 10px rgba(255, 255, 255, 0.25);
}

#bootCursor {
    display: inline-block;
    margin-top: 8px;
    animation: blink 0.8s infinite;
}

.boot-hidden {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.8s ease;
}

@keyframes blink {
    50% {
        opacity: 0;
    }
}

#panel {
    position: relative;
    z-index: 1000;
}

#desktop {
    position: relative;
    width: 100vw;
    height: calc(100vh - 48px);
}

#notificationContainer {
    position: fixed;
    top: 62px;
    right: 22px;
    z-index: 5000;
    display: flex;
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
}

.system-notification {
    display: flex;
    align-items: center;
    width: 350px;
    min-height: 92px;
    padding: 15px 18px;
    background: #171b17;
    border: 2px solid #5c705c;
    box-shadow: 6px 6px 0 #000;
    color: #d8ffd8;
    cursor: pointer;
    pointer-events: auto;
    animation: notification-in 0.25s ease-out;
}

.notification-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    min-width: 48px;
    height: 48px;
    margin-right: 14px;
    color: #b6ff72;
    font-size: 27px;
}

.notification-text {
    min-width: 0;
}

.notification-title {
    color: #b6ff72;
    font-family: "Press Start 2P", monospace;
    font-size: 9px;
}

.notification-from {
    margin-top: 6px;
    color: #ddd;
    font-size: 21px;
}

.notification-preview {
    margin-top: 2px;
    color: #888;
    font-size: 18px;
}

.system-notification:hover {
    background: #202620;
    border-color: #8cff8c;
}

@keyframes notification-in {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}


================================================================================
FILE: static\js\boot.js
================================================================================

let skipIntro = false;

const keysDown = new Set();

document.addEventListener(
    "keydown",
    function (event) {
        keysDown.add(
            event.key.toLowerCase()
        );

        if (
            keysDown.has("z") &&
            keysDown.has("9")
        ) {
            skipIntro = true;
        }
    }
);

document.addEventListener(
    "keyup",
    function (event) {
        keysDown.delete(
            event.key.toLowerCase()
        );
    }
);

const introMessages = [
    "In 2018, something went wrong.",
    "A system was built to watch.",
    "It learned.",
    "It remembered.",
    "Then they shut it down.",
    "Or so they thought.",
    "Eight years passed.",
    "The machine stayed silent.",
    "Until tonight."
];

const bootMessages = [
    "NEXUS NODE 01",
    "----------------",
    "> POWER ........ ONLINE",
    "> STORAGE ...... OK",
    "> NETWORK ...... CONNECTED",
    "> SYSTEM ....... READY",
    "",
    "> LAST USER .... UNKNOWN",
    "> LAST SHUTDOWN  NOT FOUND",
    "",
    "> BOOT COMPLETE"
];

function sleep(milliseconds) {
    return new Promise(
        resolve =>
            setTimeout(
                resolve,
                milliseconds
            )
    );
}

async function runIntroSequence() {
    const introScreen =
        document.getElementById(
            "introScreen"
        );

    const introText =
        document.getElementById(
            "introText"
        );

    if (!introScreen || !introText) {
        return;
    }

    for (const message of introMessages) {
        if (skipIntro) {
            break;
        }

        const line =
            document.createElement(
                "div"
            );

        line.className =
            "intro-line";

        line.textContent =
            message;

        introText.appendChild(
            line
        );

        await sleep(350);

        if (skipIntro) {
            break;
        }

        line.classList.add(
            "visible"
        );

        await sleep(1900);

        if (skipIntro) {
            break;
        }

        line.classList.add(
            "fade"
        );

        await sleep(900);

        if (line.parentNode) {
            line.parentNode.removeChild(
                line
            );
        }
    }

    introText.innerHTML = "";

    await sleep(
        skipIntro ? 200 : 900
    );

    introScreen.classList.add(
        "intro-hidden"
    );

    await sleep(900);

    if (introScreen.parentNode) {
        introScreen.remove();
    }
}

async function runBootSequence() {
    const bootScreen =
        document.getElementById(
            "bootScreen"
        );

    const bootText =
        document.getElementById(
            "bootText"
        );

    if (!bootScreen || !bootText) {
        return;
    }

    for (const message of bootMessages) {
        bootText.innerHTML +=
            message + "<br>";

        await sleep(180);
    }

    await sleep(700);

    bootScreen.classList.add(
        "boot-hidden"
    );

    await sleep(900);

    if (bootScreen.parentNode) {
        bootScreen.remove();
    }

    showNewMessageNotification(0);
}

async function startOpening() {
    await runIntroSequence();

    await runBootSequence();
}

startOpening();

================================================================================
FILE: static\js\desktop.js
================================================================================

async function loadHTML(elementId, url) {
    const element =
        document.getElementById(elementId);

    if (!element) {
        console.error(`Missing element: #${elementId}`);
        return false;
    }

    try {
        const response =
            await fetch(url);

        if (!response.ok) {
            throw new Error(
                `${url} returned ${response.status}`
            );
        }

        element.innerHTML =
            await response.text();

        return true;

    } catch (error) {
        console.error(
            `Failed to load ${url}:`,
            error
        );

        return false;
    }
}

function updateClock() {
    const clock =
        document.getElementById("clock");

    if (!clock) {
        return;
    }

    const now =
        new Date();

    clock.textContent =
        `${String(now.getHours()).padStart(2, "0")}:` +
        `${String(now.getMinutes()).padStart(2, "0")}`;
}

function playMessageSound() {
    const sound =
        document.getElementById(
            "notificationSound"
        );

    if (!sound) {
        return;
    }

    sound.currentTime = 0;

    sound.play().catch(() => {
        const playSound =
            function () {
                sound.currentTime = 0;

                sound.play().catch(() => {});
            };

        document.addEventListener(
            "click",
            playSound,
            { once: true }
        );

        document.addEventListener(
            "keydown",
            playSound,
            { once: true }
        );
    });
}

function showNewMessageNotification(
    messageStage = 0
) {
    const container =
        document.getElementById(
            "notificationContainer"
        );

    const badge =
        document.getElementById(
            "messageBadge"
        );

    if (badge) {
        badge.classList.remove("hidden");
    }

    if (!container) {
        return;
    }

    const oldNotification =
        document.querySelector(
            ".system-notification"
        );

    if (oldNotification) {
        oldNotification.remove();
    }

    let preview =
        "You have a new message.";

    if (messageStage === 0) {
        preview =
            "If you're seeing this...";
    }

    if (messageStage === 1) {
        preview =
            "You found the part I was worried about.";
    }

    if (messageStage === 2) {
        preview =
            "So. Now you know.";
    }

    const notification =
        document.createElement("div");

    notification.className =
        "system-notification";

    notification.innerHTML = `
        <div class="notification-icon">✉</div>

        <div class="notification-text">

            <div class="notification-title">
                NEW MESSAGE
            </div>

            <div class="notification-from">
                UNKNOWN
            </div>

            <div class="notification-preview">
                ${preview}
            </div>

        </div>
    `;

    notification.addEventListener(
        "click",
        function () {
            notification.remove();

            openMessages();
        }
    );

    container.appendChild(
        notification
    );

    playMessageSound();

    setTimeout(
        function () {
            if (notification.parentElement) {
                notification.remove();
            }
        },
        7000
    );
}

async function openTerminal() {
    const existing =
        document.getElementById(
            "terminalWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";

        const input =
            document.getElementById(
                "commandInput"
            );

        if (input) {
            input.focus();
        }

        return;
    }

    try {
        const response =
            await fetch(
                "/apps/terminal"
            );

        if (!response.ok) {
            throw new Error(
                `Terminal returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const terminal =
            document.getElementById(
                "terminalWindow"
            );

        if (!terminal) {
            throw new Error(
                "terminalWindow was not created."
            );
        }

        setupWindow(terminal);

        initializeTerminal();

    } catch (error) {
        console.error(
            "Terminal failed to open:",
            error
        );
    }
}

async function openMessages() {
    const existing =
        document.getElementById(
            "messagesWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";

        await updateMessages();

        return;
    }

    try {
        const response =
            await fetch(
                "/apps/messages"
            );

        if (!response.ok) {
            throw new Error(
                `Messages returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const messagesWindow =
            document.getElementById(
                "messagesWindow"
            );

        if (!messagesWindow) {
            throw new Error(
                "messagesWindow was not created."
            );
        }

        setupWindow(
            messagesWindow
        );

        await updateMessages();

        const badge =
            document.getElementById(
                "messageBadge"
            );

        if (badge) {
            badge.classList.add("hidden");
        }

        const listBadge =
            document.getElementById(
                "messageListBadge"
            );

        if (listBadge) {
            listBadge.classList.add("hidden");
        }

    } catch (error) {
        console.error(
            "Messages failed to open:",
            error
        );
    }
}

async function openFiles() {
    const existing =
        document.getElementById(
            "filesWindow"
        );

    if (existing) {
        existing.style.zIndex = "3000";

        return;
    }

    try {
        const response =
            await fetch(
                "/apps/files"
            );

        if (!response.ok) {
            throw new Error(
                `Files returned ${response.status}`
            );
        }

        const html =
            await response.text();

        document.body.insertAdjacentHTML(
            "beforeend",
            html
        );

        const filesWindow =
            document.getElementById(
                "filesWindow"
            );

        if (!filesWindow) {
            throw new Error(
                "filesWindow was not created."
            );
        }

        setupWindow(
            filesWindow
        );

        if (
            typeof initializeFiles ===
            "function"
        ) {
            initializeFiles();
        }

    } catch (error) {
        console.error(
            "Files failed to open:",
            error
        );
    }
}

async function updateMessages() {
    try {
        const response =
            await fetch(
                "/api/messages"
            );

        if (!response.ok) {
            throw new Error(
                "Message API failed."
            );
        }

        const data =
            await response.json();

        const messageBody =
            document.querySelector(
                ".message-body"
            );

        const messagePreview =
            document.querySelector(
                ".message-preview"
            );

        if (!messageBody) {
            return;
        }

        if (data.challenge01_complete) {
            messageBody.textContent =
                `So.

Now you know.

192.168.1.44 wasn't NEXUS.

Someone else was connecting to this machine.

And they tried three times
before they got in.

That's the part that bothers me.

But there's something else.

I need you to find out
what happened at 03:17.

Don't open the system log yet.

There's another way in.

I'll contact you again.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "So. Now you know.";
            }

        } else if (data.access_log_found) {
            messageBody.textContent =
                `You found the part I was worried about.

192.168.1.44

Don't assume it's an intruder yet.

That's the mistake people make.

An unfamiliar address isn't proof of anything.

We need something to compare it with.

Find out what address belongs to NEXUS itself.

You won't find the answer in the access log.

Look through the system configuration.

Start with:

/etc

If you're not familiar with the terminal,
ask NOVA.

She'll keep you from getting lost.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "You found the part I was worried about.";
            }

        } else {
            messageBody.textContent =
                `If you're seeing this, then NEXUS came back online.

It wasn't supposed to.

The last shutdown was recorded.

The restart wasn't.

I don't know how long you've been connected,
so I'm going to keep this short.

Don't trust what the desktop tells you.

Something happened before you arrived.

There are traces of it all over this machine.

Logs.
Connections.
Old system files.

Start by looking around.

If you know how to use the terminal,
you already know where to begin.

If you don't...

That's okay.

Open it.

I'll tell you what you need to know when you need it.

— UNKNOWN`;

            if (messagePreview) {
                messagePreview.textContent =
                    "Open it.";
            }
        }

    } catch (error) {
        console.error(
            "Could not update messages:",
            error
        );
    }
}

document.addEventListener(
    "click",
    function (event) {

        if (
            event.target.closest(
                "#terminalButton"
            ) ||
            event.target.closest(
                "#terminalDesktopIcon"
            )
        ) {
            openTerminal();

            return;
        }

        if (
            event.target.closest(
                "#messagesButton"
            ) ||
            event.target.closest(
                "#messagesIcon"
            )
        ) {
            openMessages();

            return;
        }

        if (
            event.target.closest(
                "#filesIcon"
            )
        ) {
            openFiles();

            return;
        }
    }
);

async function startDesktop() {
    const panelLoaded =
        await loadHTML(
            "panel",
            "/panel"
        );

    const desktopLoaded =
        await loadHTML(
            "desktop",
            "/desktop"
        );

    if (!panelLoaded || !desktopLoaded) {
        console.error(
            "Desktop failed to initialize."
        );

        return;
    }

    updateClock();
}

startDesktop();

setInterval(
    updateClock,
    1000
);

================================================================================
FILE: static\js\nova.js
================================================================================

let novaBox = null;
let novaText = null;
let novaTimeout = null;

async function initializeNova() {
try {
const response = await fetch("/nova");

if (!response.ok) {
throw new Error("NOVA failed to load.");
}

const html = await response.text();

document.body.insertAdjacentHTML(
"beforeend",
html
);

novaBox =
document.getElementById("novaMessageBox");

novaText =
document.getElementById("novaText");

} catch (error) {
console.error(
"NOVA failed to initialize:",
error
);
}
}

function novaSay(message, duration = 6000) {
if (!novaBox || !novaText) {
return;
}

clearTimeout(novaTimeout);

novaText.textContent = message;

novaBox.classList.add(
"nova-show"
);

novaTimeout = setTimeout(
function () {
novaBox.classList.remove(
"nova-show"
);
},
duration
);
}

function novaProgress(stage) {
if (stage === "terminal") {
novaSay(
"Ah, there you are.\n\nDon't worry about knowing Linux. Just look around for a minute.\n\nTry ls."
);
return;
}

if (stage === "logs") {
novaSay(
"See that logs folder?\n\nThat's probably worth a look."
);
return;
}

if (stage === "access_log") {
novaSay(
"Two logs.\n\nI'd start with access.log.\n\nSomeone has been connecting to this machine."
);
return;
}

if (stage === "suspicious_ip") {
novaSay(
"Hmm...\n\n192.168.1.44.\n\nDon't jump to conclusions yet. We need something to compare it against."
);
return;
}

if (stage === "network_config") {
novaSay(
"There it is.\n\nNEXUS is 192.168.1.24.\n\nSo... who was .44?"
);
return;
}

if (stage === "challenge_complete") {
novaSay(
"Yeah...\n\nYou figured it out.\n\nBut I don't think that's the whole story."
);
}
}

window.initializeNova =
initializeNova;

window.novaSay =
novaSay;

window.novaProgress =
novaProgress;

window.addEventListener(
"load",
function () {
initializeNova();
}
);

================================================================================
FILE: static\js\terminal.js
================================================================================

function initializeTerminal() {
    const input =
        document.getElementById("commandInput");

    const output =
        document.getElementById("terminalOutput");

    const body =
        document.getElementById("terminalBody");

    const prompt =
        document.getElementById("terminalPrompt");

    if (!input || !output || !body || !prompt) {
        console.error(
            "Terminal initialization failed."
        );

        return;
    }

    let currentDirectory =
        "/home/nexus";

    function displayPath() {
        if (currentDirectory === "/home/nexus") {
            return "~";
        }

        return currentDirectory;
    }

    function updatePrompt() {
        prompt.textContent =
            `nexus@node:${displayPath()}$`;
    }

    function escapeHTML(text) {
        const element =
            document.createElement("div");

        element.textContent = text;

        return element.innerHTML;
    }

    function addCommand(command, result) {
        const commandLine =
            document.createElement("div");

        commandLine.className =
            "terminal-line";

        commandLine.innerHTML =
            `<span class="terminal-prompt">nexus@node:${displayPath()}$</span> ${escapeHTML(command)}`;

        output.appendChild(commandLine);

        if (result) {
            const resultLine =
                document.createElement("div");

            resultLine.className =
                "terminal-line";

            resultLine.textContent =
                result;

            output.appendChild(resultLine);
        }

        body.scrollTop =
            body.scrollHeight;
    }

    async function runCommand(command) {
        try {
            const response =
                await fetch(
                    "/api/terminal", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json"
                        },
                        body: JSON.stringify({
                            command: command,
                            cwd: currentDirectory
                        })
                    }
                );

            if (!response.ok) {
                throw new Error(
                    "Terminal backend error."
                );
            }

            const data =
                await response.json();

            if (data.output === "__CLEAR__") {
                output.innerHTML = "";
            } else {
                addCommand(
                    command,
                    data.output
                );
            }

            currentDirectory =
                data.cwd;

            updatePrompt();
            if (data.new_message) {
                showNewMessageNotification(
                    data.message_stage
                );

                const messagesWindow =
                    document.getElementById(
                        "messagesWindow"
                    );

                if (messagesWindow) {
                    await updateMessages();
                }
            }

            input.focus();

        } catch (error) {
            console.error(error);

            addCommand(
                command,
                "NEXUS: connection to system failed."
            );

            input.focus();
        }
    }

    input.addEventListener(
        "keydown",
        function (event) {
            if (event.key !== "Enter") {
                return;
            }

            event.preventDefault();

            const command =
                input.value.trim();

            if (!command) {
                return;
            }

            input.value = "";

            runCommand(command);
        }
    );

    body.addEventListener(
        "click",
        function () {
            input.focus();
        }
    );

    updatePrompt();

    input.focus();
}


window.initializeTerminal =
    initializeTerminal;

================================================================================
FILE: static\js\window.js
================================================================================

function setupWindow(windowElement) {
    if (!windowElement) {
        return;
    }

    const titlebar =
        windowElement.querySelector(
            ".terminal-titlebar, .messages-titlebar, .files-titlebar"
        );

    if (!titlebar) {
        console.error("Window titlebar not found.");
        return;
    }

    let dragging = false;
    let offsetX = 0;
    let offsetY = 0;

    titlebar.addEventListener(
        "mousedown",
        function (event) {

            if (event.target.closest("button, input")) {
                return;
            }

            if (windowElement.dataset.maximized === "true") {
                return;
            }

            dragging = true;

            const rect =
                windowElement.getBoundingClientRect();

            offsetX =
                event.clientX - rect.left;

            offsetY =
                event.clientY - rect.top;

            windowElement.style.transform = "none";

            windowElement.style.left =
                `${rect.left}px`;

            windowElement.style.top =
                `${rect.top}px`;

            windowElement.style.zIndex = "3000";

            document.body.style.userSelect = "none";
        }
    );

    document.addEventListener(
        "mousemove",
        function (event) {

            if (!dragging) {
                return;
            }

            let x =
                event.clientX - offsetX;

            let y =
                event.clientY - offsetY;

            const panel =
                document.getElementById("panel");

            const panelHeight =
                panel
                    ? panel.offsetHeight
                    : 48;

            const maxX =
                window.innerWidth -
                windowElement.offsetWidth;

            const maxY =
                window.innerHeight -
                windowElement.offsetHeight;

            x = Math.max(
                0,
                Math.min(x, maxX)
            );

            y = Math.max(
                panelHeight,
                Math.min(y, maxY)
            );

            windowElement.style.left =
                `${x}px`;

            windowElement.style.top =
                `${y}px`;
        }
    );

    document.addEventListener(
        "mouseup",
        function () {

            if (!dragging) {
                return;
            }

            dragging = false;

            document.body.style.userSelect = "";
        }
    );

    const closeButton =
        windowElement.querySelector(
            "#terminalClose, .messages-close, #filesClose"
        );

    if (closeButton) {

        closeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                windowElement.remove();

            }
        );
    }

    const maximizeButton =
        windowElement.querySelector(
            "#terminalMaximize"
        );

    if (maximizeButton) {

        maximizeButton.addEventListener(
            "click",
            function (event) {

                event.stopPropagation();

                const maximized =
                    windowElement.dataset.maximized === "true";

                if (!maximized) {

                    const rect =
                        windowElement.getBoundingClientRect();

                    windowElement.dataset.oldLeft =
                        `${rect.left}px`;

                    windowElement.dataset.oldTop =
                        `${rect.top}px`;

                    windowElement.dataset.oldWidth =
                        `${rect.width}px`;

                    windowElement.dataset.oldHeight =
                        `${rect.height}px`;

                    windowElement.style.transform =
                        "none";

                    windowElement.style.left =
                        "0px";

                    windowElement.style.top =
                        "48px";

                    windowElement.style.width =
                        "100vw";

                    windowElement.style.height =
                        "calc(100vh - 48px)";

                    windowElement.dataset.maximized =
                        "true";

                } else {

                    windowElement.style.left =
                        windowElement.dataset.oldLeft;

                    windowElement.style.top =
                        windowElement.dataset.oldTop;

                    windowElement.style.width =
                        windowElement.dataset.oldWidth;

                    windowElement.style.height =
                        windowElement.dataset.oldHeight;

                    windowElement.dataset.maximized =
                        "false";
                }

            }
        );
    }

    windowElement.addEventListener(
        "mousedown",
        function () {

            const currentZ =
                parseInt(
                    windowElement.style.zIndex || "500",
                    10
                );

            if (currentZ < 3000) {
                windowElement.style.zIndex = "3000";
            }

        }
    );
}


window.setupWindow = setupWindow;


================================================================================
FILE: system\commands.py
================================================================================

from datetime import datetime

from challenges.challenge01 import run_challenge01
from system.filesystem import FILESYSTEM


def normalize_path(path):
    parts = []

    for part in path.split("/"):
        if part in ("", "."):
            continue

        if part == "..":
            if parts:
                parts.pop()
        else:
            parts.append(part)

    if not parts:
        return "/"

    return "/" + "/".join(parts)


def resolve_path(current_dir, path):
    if path.startswith("/"):
        return normalize_path(path)

    return normalize_path(
        current_dir.rstrip("/") + "/" + path
    )


def execute_command(command, current_dir):
    command = command.strip()

    if not command:
        return {
            "output": "",
            "cwd": current_dir
        }

    parts = command.split()
    cmd = parts[0]
    args = parts[1:]

    if cmd == "help":
        return {
            "output": (
                "Available commands:\n"
                "\n"
                "help       Show available commands\n"
                "clear      Clear terminal\n"
                "pwd        Show current directory\n"
                "ls         List files\n"
                "cd         Change directory\n"
                "cat        Read a file\n"
                "whoami     Show current user\n"
                "hostname   Show system hostname\n"
                "echo       Print text\n"
                "date       Show system date"
            ),
            "cwd": current_dir
        }

    if cmd == "pwd":
        return {
            "output": current_dir,
            "cwd": current_dir
        }

    if cmd == "whoami":
        return {
            "output": "nexus",
            "cwd": current_dir
        }

    if cmd == "hostname":
        return {
            "output": "nexus-node-01",
            "cwd": current_dir
        }

    if cmd == "date":
        return {
            "output": datetime.now().strftime("%a %b %d %H:%M:%S %Y"),
            "cwd": current_dir
        }

    if cmd == "echo":
        return {
            "output": " ".join(args),
            "cwd": current_dir
        }

    if cmd == "clear":
        return {
            "output": "__CLEAR__",
            "cwd": current_dir
        }

    if cmd == "ls":
        target = current_dir

        if args:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"ls: cannot access '{target}': No such file or directory",
                "cwd": current_dir
            }

        if item["type"] == "file":
            return {
                "output": target,
                "cwd": current_dir
            }

        return {
            "output": "\n".join(item["children"]),
            "cwd": current_dir
        }

    if cmd == "cd":
        if not args:
            target = "/home/nexus"
        else:
            target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cd: no such file or directory: {args[0]}",
                "cwd": current_dir
            }

        if item["type"] != "directory":
            return {
                "output": f"cd: not a directory: {args[0]}",
                "cwd": current_dir
            }

        return {
            "output": "",
            "cwd": target
        }

    if cmd == "cat":
        if not args:
            return {
                "output": "cat: missing file operand",
                "cwd": current_dir
            }

        target = resolve_path(current_dir, args[0])

        item = FILESYSTEM.get(target)

        if not item:
            return {
                "output": f"cat: {args[0]}: No such file or directory",
                "cwd": current_dir
            }

        if item["type"] != "file":
            return {
                "output": f"cat: {args[0]}: Is a directory",
                "cwd": current_dir
            }

        challenge_result = run_challenge01(
            command,
            target
        )

        return {
            "output": item["content"],
            "cwd": current_dir,
            "challenge_complete": challenge_result["complete"],
            "notification": challenge_result["notification"]
        }

    return {
        "output": (
            f"{cmd}: command not found\n"
            "Type 'help' for available commands."
        ),
        "cwd": current_dir
    }


================================================================================
FILE: system\filesystem.py
================================================================================

FILESYSTEM = {
    "/": {
        "type": "directory",
        "children": [
            "home",
            "etc",
            "logs",
            "missions"
        ]
    },

    "/home": {
        "type": "directory",
        "children": [
            "nexus"
        ]
    },

    "/home/nexus": {
        "type": "directory",
        "children": [
            "readme.txt",
            "notes.txt"
        ]
    },

    "/home/nexus/readme.txt": {
        "type": "file",
        "content": "Welcome to NEXUS OS."
    },

    "/home/nexus/notes.txt": {
        "type": "file",
        "content": "Check the system logs.\nSomething doesn't look right."
    },

    "/etc": {
        "type": "directory",
        "children": [
            "hostname",
            "network.conf"
        ]
    },

    "/etc/hostname": {
        "type": "file",
        "content": "nexus-node-01"
    },

    "/etc/network.conf": {
        "type": "file",
        "content": "interface=eth0\naddress=192.168.1.24\ngateway=192.168.1.1"
    },

    "/logs": {
        "type": "directory",
        "children": [
            "system.log",
            "access.log"
        ]
    },

    "/logs/system.log": {
        "type": "file",
        "content": "[03:02] system started\n[03:04] network interface eth0 online\n[03:17] security service started"
    },

    "/logs/access.log": {
        "type": "file",
        "content": "[03:02] 192.168.1.12 LOGIN SUCCESS\n[03:04] 192.168.1.18 LOGIN SUCCESS\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:11] 192.168.1.44 LOGIN FAILED\n[03:12] 192.168.1.44 LOGIN SUCCESS"
    },

    "/missions": {
        "type": "directory",
        "children": [
            "mission01"
        ]
    },

    "/missions/mission01": {
        "type": "directory",
        "children": [
            "brief.txt"
        ]
    },

    "/missions/mission01/brief.txt": {
        "type": "file",
        "content": "MISSION 01\n\nSomething is wrong with the network.\nInvestigate the access logs."
    }
}


================================================================================
FILE: system\state.py
================================================================================

game_state = {
    "current_challenge": 1,
    "message_unlocked": False,
    "access_log_found": False,
    "network_config_found": False,
    "system_log_found": False,
    "challenge01_complete": False,
    "challenge02_complete": False,
    "challenge03_complete": False,
    "unread_messages": 0,
    "messages": []
}

================================================================================
FILE: templates\apps\messages.html
================================================================================

<div
    id="messagesWindow"
    class="messages-window"
>

    <div class="messages-titlebar">

        <button
            class="messages-close"
            title="Close"
        ></button>

        <h1>
            MESSAGES
        </h1>

    </div>


    <div class="messages-content">

        <aside class="message-list">

            <div class="message-item active">

                <div class="message-sender">
                    UNKNOWN
                </div>

                <div class="message-preview">
                    No new messages.
                </div>

                <span
                    id="messageListBadge"
                    class="message-unread"
                >
                    !
                </span>

            </div>

        </aside>


        <section class="message-view">

            <div class="message-header">
                UNKNOWN
            </div>

            <div class="message-time">
                JUST NOW
            </div>

            <div class="message-body">
                Loading...
            </div>

        </section>

    </div>

</div>


================================================================================
FILE: templates\apps\terminal.html
================================================================================

<div id="terminalWindow" class="terminal-window">

    <div id="terminalTitlebar" class="terminal-titlebar">

        <div class="terminal-buttons">

            <button
                id="terminalClose"
                class="close-button"
                title="Close"
            ></button>

            <button
                id="terminalMaximize"
                class="maximize-button"
                title="Maximize"
            ></button>

        </div>

        <h1>TERMINAL</h1>

    </div>

    <div id="terminalBody" class="terminal-body">

        <div id="terminalOutput">

            <div class="terminal-line terminal-system">
                NEXUS NODE 01
            </div>

            <div class="terminal-line terminal-system">
                SYSTEM READY.
            </div>

            <div class="terminal-line terminal-system">
                Type 'help' for available commands.
            </div>

        </div>

        <div class="terminal-input-line">

            <span
                id="terminalPrompt"
                class="terminal-prompt"
            >
                nexus@node:~$
            </span>

            <input
                id="commandInput"
                type="text"
                autocomplete="off"
                spellcheck="false"
            >

        </div>

    </div>

</div>


================================================================================
FILE: templates\desktop.html
================================================================================

<div class="desktop-icons">

    <div class="desktop-icon">

        <button id="filesIcon">
            📁
        </button>

        <p>FILES</p>

    </div>


    <div class="desktop-icon">

        <button id="browserIcon">
            🌐
        </button>

        <p>BROWSER</p>

    </div>


    <div class="desktop-icon">

        <button id="messagesIcon">

            ✉

            <span
                id="messageBadge"
                class="notification-badge hidden"
            >
                1
            </span>

        </button>

        <p>MESSAGES</p>

    </div>


    <div class="desktop-icon">

        <button id="terminalDesktopIcon">
            ▣
        </button>

        <p>TERMINAL</p>

    </div>

</div>


================================================================================
FILE: templates\index.html
================================================================================

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Nexus OS</title>

    <link
        rel="preconnect"
        href="https://fonts.googleapis.com"
    >

    <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin
    >

    <link
        href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/style.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/panel.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/desktop.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/terminal.css') }}"
    >

    <link
        rel="stylesheet"
        href="{{ url_for('static', filename='css/apps/messages.css') }}"
    >
</head>

<body>

    <!-- STORY -->
    <div id="introScreen">
        <div id="introText"></div>
    </div>

    <!-- BOOT -->
    <div id="bootScreen">
        <div id="bootText"></div>
        <div id="bootCursor">_</div>
    </div>

    <div id="panel"></div>

    <main id="desktop"></main>

    <div id="notificationContainer"></div>

    <audio
        id="notificationSound"
        src="{{ url_for('static', filename='audio/notification.wav') }}"
        preload="auto"
    ></audio>

    <script src="{{ url_for('static', filename='js/window.js') }}"></script>
    <script src="{{ url_for('static', filename='js/terminal.js') }}"></script>
    <script src="{{ url_for('static', filename='js/desktop.js') }}"></script>
    <script src="{{ url_for('static', filename='js/boot.js') }}"></script>
<script src="{{ url_for('static', filename='js/nova.js') }}"></script>

</body>

</html>


================================================================================
FILE: templates\nova.html
================================================================================

<div id="novaMessageBox">
<div id="novaName">NOVA</div>
<div id="novaText"></div>
</div>

================================================================================
FILE: templates\panel.html
================================================================================

<header class="panel">

    <button
        id="menuButton"
        class="menu-button"
    >
        NEXUS
    </button>

    <div class="panel-apps">

        <button id="terminalButton">
            TERMINAL
        </button>

        <button id="messagesButton">
            MESSAGES
        </button>

    </div>

    <div class="system-tray">

        <span class="network-status">
            ● ONLINE
        </span>

        <span id="clock">
            00:00
        </span>

    </div>

</header>

